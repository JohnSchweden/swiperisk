---
phase: 02-live-api
plan: 03
type: TDD
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - services/roastService.ts
  - src/App.tsx
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Roast.exe uses streaming audio when Live API available"
    - "Roast.exe falls back to standard TTS when Live API fails"
  artifacts:
    - path: "services/roastService.ts"
      provides: "Roast service with streaming audio and fallback logic"
      exports: ["getRoastWithStreaming", "getRoastWithFallback"]
  key_links:
    - from: "services/roastService.ts"
      to: "hooks/useLiveAudio"
      via: "useLiveAudio hook"
      pattern: "startLiveSession"
    - from: "services/roastService.ts"
      to: "services/geminiService"
      via: "speak function (fallback)"
      pattern: "speak.*fallback"
---

<objective>
Integrate streaming audio into Roast.exe workflow with fallback to standard TTS (direct browser connection).

Purpose: Enable real-time streaming audio in Roast.exe while maintaining reliability through fallback.

Output: Updated roast service with streaming and fallback support.
</objective>

<execution_context>
@/Users/yevgenschweden/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/02-live-api/02-01-SUMMARY.md
@.planning/phases/02-live-api/02-02-SUMMARY.md
@services/geminiService.ts
@hooks/useLiveAudio.ts
@services/geminiLive.ts

# Reference:
# - getRoast() in geminiService.ts generates text (non-blocking)
# - speak() in geminiService.ts plays TTS (blocking - waits for full audio)
# - useLiveAudio hook provides streaming playback (non-blocking)
# - geminiLive.ts provides direct browser connection to Live API

# Voice names for personalities (from current implementation):
# - ROASTER: 'Kore' (deep voice)
# - ZEN_MASTER: 'Puck' (calm voice)
# - LOVEBOMBER: 'Enceladus' (energetic voice)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create roast service with streaming and fallback</name>
  <files>services/roastService.ts</files>
  <action>
Create service at `services/roastService.ts` that combines text generation with streaming audio and fallback (direct browser connection).

Implementation must include:
1. Import existing getRoast() from geminiService.ts for text generation
2. Import speak() from geminiService.ts for fallback TTS
3. Import useLiveAudio hook (or create client-side wrapper)
4. Create getRoastWithStreaming(workflow: string, personality: PersonalityType):
   - First call getRoast() to get the roast text
   - Then use useLiveAudio.startSession() to stream audio of that text
   - Audio should start playing before full text is available (if possible)
   
5. Create getRoastWithFallback(workflow: string, personality: PersonalityType):
   - Attempts streaming audio via Live API (getRoastWithStreaming)
   - Catches errors (connection failure, rate limit 429, timeout, auth errors)
   - Falls back to standard TTS:
     a. Call getRoast() to get text
     b. Call speak() with the text
   - Logs warning: "Live API unavailable, falling back to standard TTS"
   
6. Map personalities to voice names for TTS fallback:
   - PersonalityType.ROASTER: 'Kore'
   - PersonalityType.ZEN_MASTER: 'Puck'
   - PersonalityType.LOVEBOMBER: 'Enceladus'

Error types that should trigger fallback:
- WebSocket connection errors
- 429 rate limit errors  
- Timeout errors (>10 seconds)
- API key errors (401)
- Network errors (fetch failed)
- Token expiry errors
  </action>
  <verify>
Service file created at services/roastService.ts with:
- getRoastWithStreaming function
- getRoastWithFallback function with proper error handling
- Personality-based voice configuration
- Imports from geminiService.ts and hooks/useLiveAudio
</verify>
  <done>
Roast service created with streaming audio support (direct browser) and automatic fallback to standard TTS
</done>
</task>

<task type="auto">
  <name>Task 2: Update Roast.exe to use streaming service</name>
  <files>src/App.tsx</files>
  <action>
Update the Roast.exe workflow in App.tsx to use the new streaming service (direct browser connection).

1. Find where getRoast() and speak() are currently called
2. Replace calls with getRoastWithFallback() or getRoastWithStreaming()
3. Ensure personality is passed correctly to the service
4. Add UI feedback states:
   - "Streaming..." while audio is streaming (Live API)
   - "Loading..." while generating roast
   - "Speaking..." while using fallback TTS
5. Keep existing error handling

If using getRoastWithStreaming:
- Show streaming indicator while audio plays
- Audio should start playing before text is fully generated
- Display text as it's generated (if using Live API with text output)

If using getRoastWithFallback:
- Works similarly to current but with automatic fallback
- Falls back silently to standard TTS on errors
- User shouldn't notice difference except faster first-audio
  </action>
  <verify>
App.tsx updated to use new roast service functions
Check that:
- getRoastWithFallback or getRoastWithStreaming is called
- Personality is passed correctly
- UI shows appropriate loading states
</verify>
  <done>
Roast.exe now uses streaming audio (direct browser) with automatic fallback to standard TTS
</done>
</task>

</tasks>

<verification>
Roast.exe functionality preserved with streaming audio as primary and fallback to TTS
</verification>

<success_criteria>
1. Streaming audio works in Roast.exe when Live API available (direct browser connection)
2. Falls back to standard TTS when Live API fails
3. User experience is smooth (no visible breakage on fallback)
</success_criteria>

<output>
After completion, create `.planning/phases/02-live-api/02-03-SUMMARY.md`
</output>
