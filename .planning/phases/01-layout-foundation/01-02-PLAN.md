---
phase: 01-layout-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [App.tsx]
autonomous: true

must_haves:
  truths:
    - "Intro screen uses LayoutShell with consistent layout"
    - "Personality Select screen uses LayoutShell with consistent layout"
    - "Role Select screen uses LayoutShell with consistent layout"
  artifacts:
    - path: "App.tsx"
      provides: "renderIntro, renderPersonalitySelect, renderRoleSelect refactored"
      contains: ["import.*LayoutShell", "LayoutShell"]
  key_links:
    - from: "App.tsx render methods"
      to: "LayoutShell component"
      via: "import and JSX usage"
      pattern: "<LayoutShell>.*</LayoutShell>"
---

<objective>
Refactor Intro, Personality Select, and Role Select screens to use LayoutShell component.

Purpose: Begin the unification of layout across all 8 game stages, starting with the first 3 non-gameplay screens. These screens have similar centered content and are the easiest to convert.

Output: Updated App.tsx with renderIntro, renderPersonalitySelect, and renderRoleSelect using LayoutShell.
</objective>

<execution_context>
@/Users/yevgenschweden/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/yevgenschweden/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-layout-foundation/01-01-SUMMARY.md
@/Users/yevgenschweden/hyperscale/App.tsx

**LayoutShell Component:**
- Located at: components/LayoutShell.tsx
- Import: `import LayoutShell from './components/LayoutShell';`
- Props: children (required), header?, footer?, className?
- Desktop: lg:items-center lg:justify-center (centered)
- Mobile: items-start pt-20 (top-anchored)
- Background: bg-black

**Current renderIntro (lines 364-387):**
Uses: `<div className="flex flex-col items-center justify-center min-h-screen p-4 md:p-6 text-center bg-black safe-area-top safe-area-bottom">`

**Current renderPersonalitySelect (lines 389-437):**
Uses: `<div className="flex flex-col items-center justify-center min-h-screen p-4 md:p-6 bg-black safe-area-top safe-area-bottom">`

**Current renderRoleSelect (lines 439-489):**
Uses: `<div className="flex flex-col items-center justify-center min-h-screen p-4 md:p-6 bg-black safe-area-top safe-area-bottom">`

**Refactoring Strategy:**
- Replace outer div with LayoutShell
- Keep all internal content exactly the same
- LayoutShell handles the responsive behavior
- Remove duplicate classes that LayoutShell provides (min-h, bg, safe-area)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor renderIntro to use LayoutShell</name>
  <files>App.tsx</files>
  <action>Refactor the renderIntro method to use LayoutShell:

1. Add import at top of App.tsx:
```tsx
import LayoutShell from './components/LayoutShell';
```

2. Replace the renderIntro method (lines 364-387). Current structure:
```tsx
const renderIntro = () => (
  <div className="flex flex-col items-center justify-center min-h-screen p-4 md:p-6 text-center bg-black safe-area-top safe-area-bottom">
    {/* content */}
  </div>
);
```

New structure:
```tsx
const renderIntro = () => (
  <LayoutShell className="p-4 md:p-6 text-center">
    <div className="mb-8 md:mb-12 relative">
      <h1 className="text-6xl md:text-7xl font-bold glitch-text tracking-tighter mb-2">hyperscale</h1>
      <div className="text-red-600 font-bold mono text-xs md:text-sm animate-pulse tracking-[0.4em]">incident_response_terminal // os_v0.92</div>
    </div>
    <p className="max-w-xl text-slate-300 mb-8 md:mb-10 text-base md:text-lg px-4 leading-relaxed">
      <span className="font-bold">Tinder for AI Risk, Governance, and Compliance.</span>
      <br />
      <span className="text-[#B8962E] mono text-sm md:text-base">[NOTICE: Made for people who hate f*cking boring governance training]</span>
    </p>
    <p className="max-w-xl text-slate-300 mb-12 md:mb-16 text-base md:text-lg px-4">
      <span className="text-slate-300 font-bold block mb-2">Project Icarus</span>
      <span className="text-slate-400">The CEO integrated an unhinged AI into every department.</span><br className="hidden md:inline" /><span className="text-slate-300"> Move fast, break laws, and try to survive the final audit. <span className="cursor-blink text-slate-300">_</span></span> 
    </p>
    <button 
      onClick={handleStart}
      className="px-6 py-3 md:px-12 md:py-4 text-base md:text-xl font-bold tracking-wide hover:bg-cyan-400 transition-all duration-300 transform hover:scale-105 min-h-[40px] md:min-h-[48px] bg-white text-black"
    >
      Boot system
    </button>
    <div className="mt-8 md:mt-12 mono text-xs text-red-500 px-4 text-center">WARNING: PREVIOUS COMPLIANCE OFFICER CURRENTLY PENDING LITIGATION</div>
  </LayoutShell>
);
```

Key changes:
- Replace outer div with LayoutShell
- Remove duplicate classes: min-h-screen, items-center, justify-center, bg-black, safe-area-top, safe-area-bottom (LayoutShell handles these)
- Keep: p-4 md:p-6, text-center (passed as className)
- All internal content stays exactly the same</action>
  <verify>Verify renderIntro uses LayoutShell:
```bash
grep -A 5 "const renderIntro" App.tsx | grep -E "(LayoutShell|return)"
```</verify>
  <done>renderIntro uses LayoutShell component and removed duplicate layout classes.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor renderPersonalitySelect to use LayoutShell</name>
  <files>App.tsx</files>
  <action>Refactor the renderPersonalitySelect method to use LayoutShell:

Current structure (lines 389-437):
```tsx
const renderPersonalitySelect = () => (
  <div className="flex flex-col items-center justify-center min-h-screen p-4 md:p-6 bg-black safe-area-top safe-area-bottom">
    <div className="w-full max-w-5xl">
      {/* content */}
    </div>
  </div>
);
```

New structure:
```tsx
const renderPersonalitySelect = () => (
  <LayoutShell className="p-4 md:p-6">
    <div className="w-full max-w-5xl">
      <div className="text-center mb-6 md:mb-10">
        <div className="text-red-600 mb-3 mono text-[10px] md:text-xs tracking-[0.3em]">
          step_01 // chaos_handler
        </div>
        <h2 className="text-3xl md:text-5xl font-black tracking-tight fade-in px-4">
          Select your emotional support
        </h2>
        <p className="mt-4 md:mt-6 max-w-xl mx-auto text-slate-400 text-sm md:text-base leading-relaxed px-4">
          Pick the unhinged co-pilot that will narrate your compliance spiral, hype your bad ideas,
          and occasionally try to keep you out of prison.
        </p>
      </div>

      <div className="grid grid-cols-1 md:grid-cols-3 gap-4 md:gap-8 w-full px-4">
        {Object.entries(PERSONALITIES).map(([type, p], index) => (
          <button
            key={type}
            onClick={() => selectPersonality(type as PersonalityType)}
            className="group p-6 md:p-10 bg-slate-900/60 border border-slate-800 hover:border-cyan-500 flex flex-col hover-scale shadow-2xl transition-colors w-full text-left"
            style={{ animationDelay: `${index * 0.1}s` }}
          >
            <div className="flex flex-col items-center text-center mb-4 md:mb-6">
              <div className="text-slate-400 group-hover:text-cyan-500 transition-colors mb-2 md:mb-3">
                <i
                  className={`fa-solid ${
                    type === PersonalityType.ROASTER
                      ? 'fa-user-ninja'
                      : type === PersonalityType.ZEN_MASTER
                      ? 'fa-leaf'
                      : 'fa-rocket'
                  } text-2xl md:text-4xl`}
                  aria-hidden
                ></i>
              </div>
              <div className="text-xl md:text-2xl font-black">{p.name}</div>
              <div className="text-cyan-400 text-xs font-black tracking-wide">{p.title}</div>
            </div>
            <p className="text-slate-400 text-xs md:text-sm leading-relaxed w-full mt-auto text-center">
              {p.description}
            </p>
          </button>
        ))}
      </div>
    </div>
  </LayoutShell>
);
```

Key changes:
- Replace outer div with LayoutShell
- Keep inner div with max-w-5xl for content width constraint
- Remove: min-h-screen, items-center, justify-center, bg-black, safe-area classes
- Keep: p-4 md:p-6 (passed as className)</action>
  <verify>Verify renderPersonalitySelect uses LayoutShell:
```bash
grep -A 5 "const renderPersonalitySelect" App.tsx | grep -E "(LayoutShell|return)"
```</verify>
  <done>renderPersonalitySelect uses LayoutShell component and removed duplicate layout classes.</done>
</task>

<task type="auto">
  <name>Task 3: Refactor renderRoleSelect to use LayoutShell</name>
  <files>App.tsx</files>
  <action>Refactor the renderRoleSelect method to use LayoutShell:

Current structure (lines 439-489):
```tsx
const renderRoleSelect = () => (
  <div className="flex flex-col items-center justify-center min-h-screen p-4 md:p-6 bg-black safe-area-top safe-area-bottom">
    <div className="w-full max-w-4xl">
      {/* content */}
    </div>
  </div>
);
```

New structure:
```tsx
const renderRoleSelect = () => (
  <LayoutShell className="p-4 md:p-6">
    <div className="w-full max-w-4xl">
      <div className="text-center mb-6 md:mb-10">
        <div className="text-red-600 mb-2 md:mb-3 mono text-[10px] md:text-xs tracking-[0.3em] fade-in px-4">
          step_02 // damage_control
        </div>
        <h2 className="text-3xl md:text-5xl font-black mb-3 md:mb-4 tracking-tight fade-in px-4">
          Select your clearance level
        </h2>
        <p className="max-w-2xl mx-auto text-slate-400 text-sm md:text-base leading-relaxed px-4">
          Choose which part of the company you want to endanger first. Each role changes the kinds of
          incidents, heat you attract, and creative ways to lose that $10M budget.
        </p>
      </div>

      <div className="grid grid-cols-2 md:grid-cols-3 gap-4 md:gap-6 w-full px-4">
        {Object.values(RoleType).map((role, index) => (
          <button
            key={role}
            onClick={() => selectRole(role)}
            className="group p-6 md:p-8 bg-slate-900/60 border border-slate-800 hover:border-cyan-500 transition-all text-center hover-scale shadow-2xl"
            style={{ animationDelay: `${index * 0.08}s` }}
          >
            <div className="text-3xl md:text-4xl mb-3 md:mb-4 text-slate-400 group-hover:text-cyan-400 transition-colors">
              <i
                className={`fa-solid ${
                  role === RoleType.DEVELOPMENT
                    ? 'fa-code'
                    : role === RoleType.MARKETING
                    ? 'fa-bullhorn'
                    : role === RoleType.MANAGEMENT
                    ? 'fa-briefcase'
                    : role === RoleType.HR
                    ? 'fa-users'
                    : role === RoleType.FINANCE
                    ? 'fa-vault'
                    : 'fa-broom'
                }`}
                aria-hidden
              ></i>
            </div>
            <div className="font-black text-xs md:text-sm tracking-wide text-slate-300 group-hover:text-white transition-colors">
              {formatLabel(role)}
            </div>
          </button>
        ))}
      </div>
    </div>
  </LayoutShell>
);
```

Key changes:
- Replace outer div with LayoutShell
- Keep inner div with max-w-4xl for content width constraint
- Remove: min-h-screen, items-center, justify-center, bg-black, safe-area classes
- Keep: p-4 md:p-6 (passed as className)</action>
  <verify>Verify renderRoleSelect uses LayoutShell:
```bash
grep -A 5 "const renderRoleSelect" App.tsx | grep -E "(LayoutShell|return)"
```</verify>
  <done>renderRoleSelect uses LayoutShell component and removed duplicate layout classes.</done>
</task>

</tasks>

<verification>
After completing all tasks:
1. Check import statement exists: `import LayoutShell from './components/LayoutShell';`
2. Verify renderIntro uses LayoutShell
3. Verify renderPersonalitySelect uses LayoutShell
4. Verify renderRoleSelect uses LayoutShell
5. Ensure no remaining min-h-screen references in these 3 methods
6. Verify app still compiles
</verification>

<success_criteria>
- LayoutShell imported in App.tsx
- renderIntro uses LayoutShell
- renderPersonalitySelect uses LayoutShell
- renderRoleSelect uses LayoutShell
- All 3 methods removed duplicate layout classes (min-h-screen, bg-black, safe-area)
- Internal content unchanged and functional
</success_criteria>

<output>
After completion, create `.planning/phases/01-layout-foundation/01-02-SUMMARY.md`
</output>
