---
phase: 17-shuffle-aware-feedback-tts-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - types.ts
  - lib/deck.ts
  - lib/feedbackAudioChoice.ts
  - lib/deck.test.ts
  - unit/feedbackAudioChoice.test.ts
autonomous: true
requirements:
  - FA-01

must_haves:
  truths:
    - "Shuffled deck cards record whether onLeft/onRight payloads were swapped from authoring order"
    - "authoringFeedbackStem maps the chosen presentation slot (which visible choice) to the authoring file suffix (left|right) used in pre-baked filenames"
  artifacts:
    - path: "types.ts"
      provides: "optional choiceSidesSwapped on Card"
    - path: "lib/deck.ts"
      provides: "shuffleDeck sets choiceSidesSwapped true/false on every card"
    - path: "lib/feedbackAudioChoice.ts"
      provides: "authoringFeedbackStem(card, selectedPresentationSlot)"
    - path: "lib/deck.test.ts"
      provides: "swap sets flag; no swap clears flag"
    - path: "unit/feedbackAudioChoice.test.ts"
      provides: "chosen slot + swapped vs not → correct authoring stem"
  key_links:
    - from: "lib/deck.ts"
      to: "types.ts"
      via: "Card.choiceSidesSwapped"
---

<objective>
Establish data model and pure mapping so pre-baked `feedback_*_left|right` files match the **authoring arm** of the **outcome the player chose** (visible left vs right choice), not a loose notion of “audio follows swipe direction.”
</objective>

<execution_context>
@/Users/yevgenschweden/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yevgenschweden/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-shuffle-aware-feedback-tts-fixes/17-CONTEXT.md
@.planning/phases/17-shuffle-aware-feedback-tts-fixes/17-RESEARCH.md
@types.ts
@lib/deck.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Card flag + shuffleDeck</name>
  <files>types.ts, lib/deck.ts, lib/deck.test.ts</files>
  <behavior>
    - Extend `Card` with optional `choiceSidesSwapped?: boolean`.
    - After Fisher-Yates in `shuffleDeck`, for each card: if left/right payloads were swapped, set `choiceSidesSwapped: true`; else `choiceSidesSwapped: false`.
    - Tests: mock `Math.random` to force swap and no-swap paths; assert flag and that `onLeft`/`onRight` payloads match expected swap (each slot still holds a full `ChoiceOutcome`).
  </behavior>
  <action>
    1. Add `choiceSidesSwapped?: boolean` to `Card` in `types.ts`.
    2. In `lib/deck.ts` after the position shuffle loop, iterate shuffled cards; on 50% branch assign swapped `onLeft`/`onRight` and `choiceSidesSwapped: true`, else copy card with `choiceSidesSwapped: false`.
    3. Extend `lib/deck.test.ts` with deterministic `Math.random` mocks covering swap and no-swap.
  </action>
  <done>
    - Every card emitted from `shuffleDeck` has boolean `choiceSidesSwapped`.
    - When swap runs, `onLeft`/`onRight` match swapped outcomes and flag is `true`.
    - When swap does not run for a card, flag is `false` and sides match source.
  </done>
  <verify>
    <automated>bun run vitest:run -- lib/deck.test.ts</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: authoringFeedbackStem helper</name>
  <files>lib/feedbackAudioChoice.ts, unit/feedbackAudioChoice.test.ts</files>
  <behavior>
    - Export `authoringFeedbackStem(card, selectedPresentationSlot)` where `selectedPresentationSlot` is `LEFT` | `RIGHT` meaning **which visible choice** the player took (same enum as game `choice` / history: the left vs right **slot**, each slot showing one `ChoiceOutcome`).
    - Return `'left' | 'right'` — the **authoring** suffix for `feedback_${cardId}_${suffix}`, i.e. which branch the baked line was recorded for (`onLeft` vs `onRight` in source content).
    - If `choiceSidesSwapped === true`, the outcome in the left slot came from authoring-right and vice versa — map chosen slot to the correct authoring stem. If false/undefined, chosen LEFT → `left`, chosen RIGHT → `right`.
    - Unit tests: table-driven cases for swapped/not × LEFT/RIGHT; assert stem matches the **outcome arm** the player selected, not “invert direction” as the mental model in comments (document choice→outcome→stem).
  </behavior>
  <action>
    1. Add `lib/feedbackAudioChoice.ts` with pure function (no React imports).
    2. Add `unit/feedbackAudioChoice.test.ts` with table-driven cases: (swapped, selectedPresentationSlot) → expected `'left'|'right'`.
  </action>
  <done>
    - With `choiceSidesSwapped: true`, choosing the **left visible choice** yields stem `right` iff that slot displays the outcome that was authoring-right (and symmetric cases).
    - `choiceSidesSwapped: false` and `undefined`: LEFT → `left`, RIGHT → `right`.
  </done>
  <verify>
    <automated>bun run vitest:run -- unit/feedbackAudioChoice.test.ts</automated>
  </verify>
</task>

</tasks>
