---
phase: 10-background-audio
plan: "02"
type: TDD
wave: 2
depends_on: ["01"]
files_modified:
  - App.tsx
  - components/game/IntroScreen.tsx
  - components/game/PersonalitySelect.tsx
  - components/game/RoleSelect.tsx
  - components/game/InitializingScreen.tsx
  - components/game/GameOver.tsx
  - components/game/SummaryScreen.tsx
  - tests/menu-audio.spec.ts
autonomous: true
must_haves:
  truths:
    - "Players hear an ambient loop across the game flow stages instead of a silent UI."
    - "Menu and navigation actions emit lightweight non-voice cues when the user commits an action."
    - "Stage transitions swap or stop the active music bed instead of layering multiple loops together."
  artifacts:
    - path: "App.tsx"
      provides: "Top-level stage-to-audio orchestration for game-flow music."
      contains: "useGameAudio"
    - path: "components/game/IntroScreen.tsx"
      provides: "Boot CTA wired to a semantic navigation cue."
      contains: "onStart"
    - path: "components/game/PersonalitySelect.tsx"
      provides: "Personality selection wired to a semantic confirm cue."
      contains: "onSelect"
    - path: "components/game/RoleSelect.tsx"
      provides: "Role selection wired to a semantic confirm cue."
      contains: "onSelect"
    - path: "tests/menu-audio.spec.ts"
      provides: "Playwright coverage for music swaps and navigation cues."
  key_links:
    - from: "App.tsx"
      to: "hooks/useGameAudio.ts"
      via: "top-level hook call keyed from the current stage"
      pattern: "useGameAudio"
    - from: "components/game/IntroScreen.tsx"
      to: "services/gameAudio.ts"
      via: "Boot action triggers a semantic navigation cue before stage advance"
      pattern: "playCue|boot|navigate"
    - from: "components/game/PersonalitySelect.tsx"
      to: "services/gameAudio.ts"
      via: "selection click triggers a confirm cue only when the action is accepted"
      pattern: "playCue|confirm"
---

<objective>
Add the audible stage layer for the overall game flow: ambient music that changes with stage context plus navigation/menu sounds for major user actions.

Purpose: Deliver AUDIO-01 and AUDIO-02 on top of the shared mixer without spreading audio orchestration logic across unrelated files.
Output: App-level stage music orchestration, screen-level navigation cues, and Playwright coverage for menu/game-flow audio behavior.
</objective>

<execution_context>
@/Users/yevgenschweden/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/yevgenschweden/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-background-audio/10-CONTEXT.md
@.planning/phases/10-background-audio/10-01-PLAN.md
@App.tsx
@components/game/IntroScreen.tsx
@components/game/PersonalitySelect.tsx
@components/game/RoleSelect.tsx
@components/game/InitializingScreen.tsx
@components/game/GameOver.tsx
@components/game/SummaryScreen.tsx
</context>

<tdd_config>
Use `type: execute`.

This plan is UI wiring plus end-to-end verification of audible state changes, not a pure input/output feature that benefits from a separate TDD cycle.
</tdd_config>

<tasks>

<task type="auto">
  <name>Task 1: Orchestrate stage music from the app shell</name>
  <files>App.tsx, components/game/InitializingScreen.tsx, components/game/GameOver.tsx, components/game/SummaryScreen.tsx</files>
  <action>Update `App.tsx` to mount the shared `useGameAudio` hook and map `GameStage` values to the correct ambient loop behavior. Ensure intro/select/initializing/playing/game-over/summary stages start the intended loop, and that resets or stage changes stop the previous bed before the next one begins. If any stage components need small props or stable event boundaries to support clean stage transitions, make those changes there rather than baking component knowledge into the audio service. Preserve current render behavior and existing voice triggers.</action>
  <verify>`bun run typecheck && bun run build`</verify>
  <done>Stage changes now drive one active ambient loop at a time, transitions do not stack loops, and the app root owns the music lifecycle instead of scattered components.</done>
</task>

<task type="auto">
  <name>Task 2: Add semantic menu and navigation cues to committed actions</name>
  <files>components/game/IntroScreen.tsx, components/game/PersonalitySelect.tsx, components/game/RoleSelect.tsx, components/game/GameOver.tsx, components/game/SummaryScreen.tsx</files>
  <action>Wire semantic cue playback into the main menu and navigation actions: boot/start, personality select, role select, restart, and log off. Trigger sounds only for real committed actions that advance or reset state; do not attach noisy hover spam or duplicate cues to blocked interactions. Reuse existing click handlers and data-testid structure so test selectors stay stable.</action>
  <verify>`bun run typecheck && bunx playwright test tests/menu-audio.spec.ts`</verify>
  <done>Primary non-gameplay actions emit lightweight cues, and silent transitions between major menu screens are replaced by intentional audio feedback.</done>
</task>

<task type="auto">
  <name>Task 3: Add end-to-end coverage for stage-loop swaps and menu cues</name>
  <files>tests/menu-audio.spec.ts</files>
  <action>Create a focused Playwright suite that exercises intro-to-role flow plus restart/log-off transitions, and verifies the audio layer through observable behavior such as stable console markers, exposed semantic events, or other deterministic test hooks added by the implementation. Cover at least one full loop swap path and at least two distinct menu/navigation cues. Avoid brittle timing assertions that depend on hearing actual audio in CI.</action>
  <verify>`bunx playwright test tests/menu-audio.spec.ts`</verify>
  <done>The menu/game-flow audio behavior is regression-tested through deterministic browser automation, including music swapping and navigation cue triggering.</done>
</task>

</tasks>

<verification>
1. Run `bun run typecheck`.
2. Run `bun run build`.
3. Run `bunx playwright test tests/menu-audio.spec.ts`.
4. Run `bunx playwright test` after this plan if executed in isolation from the rest of the phase.
</verification>

<success_criteria>
- AUDIO-01 is satisfied through stage-aware ambient looping that remains optional under browser autoplay rules.
- AUDIO-02 is satisfied through committed menu/navigation cues on the main flow.
- Music lifecycle ownership lives at the app shell, so later UI work does not need to duplicate stage logic.
</success_criteria>

<output>
After completion, create `.planning/phases/10-background-audio/10-02-SUMMARY.md`
</output>
