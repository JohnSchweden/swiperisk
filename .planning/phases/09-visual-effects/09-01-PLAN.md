---
phase: 09-visual-effects
plan: "01"
type: TDD
wave: 1
depends_on: []
files_modified:
  - hooks/usePrefersReducedMotion.ts
  - hooks/useSwipeGestures.ts
  - components/game/CardStack.tsx
  - components/game/GameScreen.tsx
  - components/game/SwipeFxLayer.tsx
autonomous: true
must_haves:
  truths:
    - "Dragging a card gives direction-aware visual feedback before the swipe commits."
    - "A committed left or right swipe produces a short flourish that matches the swipe direction instead of the card simply disappearing."
    - "Swipe effects stay lightweight enough that desktop and mobile interaction still feels responsive."
    - "prefers-reduced-motion respected: SwipeFxLayer and decorative flourishes gate behind usePrefersReducedMotion; core UX (card exit, feedback) stays when reduced."
  artifacts:
    - path: hooks/useSwipeGestures.ts
      provides: "Swipe state extended with bounded visual-effect metadata for drag and commit phases."
      exports: ["useSwipeGestures", "SwipeState"]
    - path: components/game/SwipeFxLayer.tsx
      provides: "Dedicated render layer for trails, particles, or pulse flourishes that stays separate from card markup."
      exports: ["SwipeFxLayer"]
    - path: components/game/CardStack.tsx
      provides: "Card markup wired to preview and commit visual effects with stable DOM hooks."
      contains: "data-testid"
  key_links:
    - from: hooks/useSwipeGestures.ts
      to: components/game/SwipeFxLayer.tsx
      via: "hook exposes effect payload for drag-preview and swipe-commit states"
      pattern: "trail|particle|effect"
    - from: components/game/GameScreen.tsx
      to: components/game/CardStack.tsx
      via: "playing stage passes swipe state through without recreating conflicting gesture logic"
      pattern: "swipeOffset|swipeDirection|exitPosition"
    - from: components/game/CardStack.tsx
      to: components/game/SwipeFxLayer.tsx
      via: "card position and direction drive a separate visual-effects layer"
      pattern: "SwipeFxLayer"
---

<objective>
Add the card-swipe flourish system for the phase: direction-aware drag feedback, motion trails, and a short commit burst that makes swipe outcomes feel intentional without changing the core game rules.

Purpose: Deliver VFX-01 on the app's hottest interaction path while keeping the implementation bounded and performant.
Output: A dedicated swipe-effects layer wired into the existing gesture flow, with stable hooks for later verification.
</objective>

<execution_context>
@/Users/yevgenschweden/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/yevgenschweden/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@package.json
@hooks/useSwipeGestures.ts
@components/game/CardStack.tsx
@components/game/GameScreen.tsx
@tests/swipe-interactions.spec.ts
@tests/exit-animation.spec.ts
@tests/drag-tracking.spec.ts
</context>

<tdd_config>
Use `type: execute`.

This plan targets animation behavior on an existing UI path. Add deterministic DOM hooks where needed, but do not split into a dedicated TDD plan.
</tdd_config>

<tasks>

<task type="auto">
  <name>Task 1: Extend swipe gesture state with bounded effect metadata and performance guards</name>
  <files>hooks/useSwipeGestures.ts</files>
  <action>Create `hooks/usePrefersReducedMotion.ts` using matchMedia('(prefers-reduced-motion: reduce)') with change listener; Task 2 consumes it for SwipeFxLayer. Update the existing gesture hook so it emits the minimum extra state needed for visual flourishes during drag preview, threshold crossing, and committed swipe exit. Keep the hook's current responsibility as the source of truth for swipe direction and timing; do not move gesture physics into component-local state. Add explicit bounds: trails capped at 15 segments, bursts at 20 particles max; evict oldest when over cap. Preserve the current requestAnimationFrame throttling approach.</action>
  <verify>`bun run typecheck && bun run build`</verify>
  <done>The swipe hook still owns gesture timing, exposes bounded effect metadata for preview and commit phases, and retains responsive drag behavior without introducing a new animation library.</done>
</task>

<task type="auto">
  <name>Task 2: Render direction-aware swipe flourishes as a separate layer on the gameplay card stack</name>
  <files>components/game/CardStack.tsx, components/game/GameScreen.tsx, components/game/SwipeFxLayer.tsx</files>
  <action>Create `components/game/SwipeFxLayer.tsx` as the dedicated visual layer (consume usePrefersReducedMotion from Task 1) for card trails, short directional bursts, and any glow/pulse treatment tied to swipe state. Gate SwipeFxLayer: if (prefersReducedMotion) return null — core UX (card exit, feedback) stays when reduced. Enforce caps: trails max 15 segments, bursts max 20 particles. Animate only transform and opacity; avoid layout-heavy properties (width, height, left, top, box-shadow, background). Mount through the existing playing/card stack path so pointer drags, threshold previews, and keyboard-triggered programmatic swipes all drive the same effect system. Add stable `data-testid` or `data-*` hooks on the effect container.</action>
  <verify>`bun run typecheck && bun run build && bun run test -- tests/swipe-interactions.spec.ts tests/exit-animation.spec.ts tests/drag-tracking.spec.ts`</verify>
  <done>The active card shows direction-aware preview flourishes while dragging, committed swipes emit a short directional burst, keyboard swipes use the same effect path, and the effect layer is test-observable without hijacking core card rendering.</done>
</task>

</tasks>

<verification>
1. Run `bun run typecheck`.
2. Run `bun run build`.
3. Run `bun run test -- tests/swipe-interactions.spec.ts tests/exit-animation.spec.ts tests/drag-tracking.spec.ts`.
4. Confirm the implementation uses bounded effect counts (15 trail segments, 20 burst particles) plus transform/opacity-only rendering — no layout-heavy properties (width, height, left, top, box-shadow, background). No framer-motion or react-spring.
5. Confirm SwipeFxLayer gates behind usePrefersReducedMotion; core card exit and feedback remain functional when reduced motion is preferred.
</verification>

<success_criteria>
- VFX-01 is satisfied through visible swipe trails or bursts on both drag preview and committed swipe.
- The swipe path remains responsive because the flourish system is bounded and driven from existing gesture timing.
- Stable effect-layer hooks exist so later verification can assert the feature without fragile CSS scraping.
</success_criteria>

<output>
After completion, create `.planning/phases/09-visual-effects/09-01-SUMMARY.md`
</output>
