---
phase: "01-live-api-stt-research"
plan: "02"
type: "execute"
wave: 1
depends_on: []
files_modified:
  - "hooks/useLiveAPISpeechRecognition.ts"
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Debug logging shows audio chunks being captured"
    - "Debug logging shows audio chunks being sent to API"
    - "session.sendRealtimeInput method is verified to work"
    - "User speech is transcribed in real-time"
  artifacts:
    - path: "hooks/useLiveAPISpeechRecognition.ts"
      provides: "Debug logging added, potential API fix applied"
  key_links:
    - from: "RoastTerminal.tsx"
      to: "useLiveAPISpeechRecognition"
      via: "hook call - unchanged"
---

<objective>
Diagnose and fix speech-to-text implementation - recording activates but no transcription appears

Purpose: Add debug logging to verify audio is being captured and sent, fix any issues found
Output: Working transcription with debug verification
</objective>

<execution_context>
@/Users/yevgenschweden/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/01-live-api-stt-research/01-UAT.md

**UAT Diagnosis:**
- Recording shows active (mic button changes state)
- But no transcription appears
- Root cause: AudioWorklet might not be processing/sending audio, or session.sendRealtimeInput might not work

**Current code issues identified:**
1. No debug logging to verify audio chunks
2. AudioContext created at 16kHz but browser may provide 48kHz - potential mismatch
3. session.sendRealtimeInput signature might need verification

</context>

<tasks>

<task type="auto">
  <name>Task 1: Add comprehensive debug logging to audio capture</name>
  <files>hooks/useLiveAPISpeechRecognition.ts</files>
  <action>
    Add debug logging throughout the audio capture pipeline to diagnose where audio is lost:

    1. In startMicCapture function:
       - Log when getUserMedia succeeds
       - Log actual sampleRate from mediaStream.getAudioTracks()[0].getSettings().sampleRate
       - Log when AudioWorklet receives audio (inside worklet.port.onmessage)
       - Log each audio chunk size before sending

    2. In startRecording callback:
       - Log session connection success
       - Log each sendRealtimeInput call with chunk size
       - Log any errors from the session

    Use format: console.log('[STT DEBUG] [component] message', data)
  </action>
  <verify>grep -q "STT DEBUG" hooks/useLiveAPISpeechRecognition.ts && echo "Debug logging added"</verify>
  <done>Debug logging shows audio chunks captured and sent</done>
</task>

<task type="auto">
  <name>Task 2: Fix audio sample rate handling</name>
  <files>hooks/useLiveAPISpeechRecognition.ts</files>
  <action>
    The current code requests 16kHz but browser typically gives 48kHz. Fix the AudioWorklet handling:

    1. Instead of creating AudioContext at fixed 16kHz, detect the actual sample rate from the stream
    2. Modify the AudioWorklet to handle whatever sample rate the browser provides
    3. OR: Use a ScriptProcessorNode (deprecated but simpler) or MediaRecorder approach as fallback

    Key fix: Get actual sampleRate from stream.getAudioTracks()[0].getSettings().sampleRate, then:
    - Either create AudioContext at that rate and send as-is (API expects 16kHz but may resample)
    - OR implement proper downsampling in the worklet

    Add debug log showing actual sampleRate vs expected 16000
  </action>
  <verify>grep -q "sampleRate" hooks/useLiveAPISpeechRecognition.ts</verify>
  <done>Audio captured at correct sample rate</done>
</task>

<task type="auto">
  <name>Task 3: Verify and fix sendRealtimeInput API call</name>
  <files>hooks/useLiveAPISpeechRecognition.ts</files>
  <action>
    Verify the session.sendRealtimeInput method signature. Based on research, there are two patterns:

    Pattern 1 (current): session.sendRealtimeInput({ audio: { data, mimeType } })
    Pattern 2: session.sendRealtimeInput({ media: { data, mimeType } })

    Add debug logging before and after the sendRealtimeInput call to verify:
    1. Method exists on session object
    2. Data is being passed correctly
    3. No errors occur during send

    If method doesn't exist or errors, try alternative pattern or log exact error.
  </action>
  <verify>grep -q "sendRealtimeInput" hooks/useLiveAPISpeechRecognition.ts && grep -q "DEBUG"</verify>
  <done>Audio successfully sent to Gemini API</done>
</task>

</tasks>

<verification>
1. Open browser devtools console
2. Click mic button in RoastTerminal
3. Look for debug logs:
   - "[STT DEBUG] getUserMedia success" - mic accessible
   - "[STT DEBUG] sampleRate: XXXX" - actual rate detected  
   - "[STT DEBUG] Audio chunk: XXX bytes" - audio being captured
   - "[STT DEBUG] Sending audio: XXX bytes" - audio being sent to API
4. Speak and verify transcription appears
5. If errors appear in console, note them for next iteration
</verification>

<success_criteria>
- [ ] Debug logging added showing audio capture pipeline
- [ ] Actual sample rate logged and handled correctly
- [ ] sendRealtimeInput confirmed working (no method errors)
- [ ] User speech transcribed in real-time
- [ ] Final transcript populates input field on stop
</success_criteria>

<output>
After completion, create `.planning/phases/01-live-api-stt-research/01-02-SUMMARY.md`
</output>
