---
phase: 02-live-api
plan: 02
type: TDD
wave: 2
depends_on: ["02-01"]
files_modified:
  - hooks/useLiveAudio.ts
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Live audio hook initializes AudioWorklet and streams audio chunks from direct browser connection"
    - "Audio plays as chunks arrive (not waiting for full response)"
  artifacts:
    - path: "hooks/useLiveAudio.ts"
      provides: "React hook for streaming audio with Gemini Live API"
      exports: ["useLiveAudio", "startLiveSession", "stopLiveSession"]
  key_links:
    - from: "hooks/useLiveAudio.ts"
      to: "public/audio-processor.worklet.js"
      via: "AudioWorkletNode"
      pattern: "audioWorklet.addModule.*audio-processor"
    - from: "hooks/useLiveAudio.ts"
      to: "services/geminiLive"
      via: "direct service call"
      pattern: "connectToLiveSession"
---

<objective>
Create React hook for streaming audio using Gemini Live API (direct browser connection).

Purpose: Enable Roast.exe to stream audio chunks as they arrive, reducing time-to-first-audio.

Output: useLiveAudio hook that handles connection, streaming, and playback.
</objective>

<execution_context>
@/Users/yevgenschweden/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/02-live-api/02-01-SUMMARY.md
@public/audio-processor.worklet.js
@services/geminiService.ts
@services/geminiLive.ts

# Reference the direct browser service created in Plan 1:
# - Located at services/geminiLive.ts
# - Uses getEphemeralToken() for authentication
# - connectToLiveSession() returns ReadableStream of audio chunks

# Reference the AudioWorklet created in Plan 1:
# - Located at public/audio-processor.worklet.js
# - Registered as 'streaming-audio-processor'
# - Handles 24kHz→48kHz conversion internally

# Current TTS implementation to understand:
# - speak() function in services/geminiService.ts
# - Uses AudioContext at 24kHz (no conversion needed for TTS API)
# - This is the fallback when Live API fails
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create useLiveAudio hook</name>
  <files>hooks/useLiveAudio.ts</files>
  <action>
Create React hook at `hooks/useLiveAudio.ts` for streaming audio with Gemini Live API (direct browser connection).

Implementation must include:
1. Initialize AudioContext at 48kHz sample rate (NOT 24kHz - worklet handles conversion)
2. Load AudioWorklet from '/audio-processor.worklet.js'
3. Create AudioWorkletNode connected to destination
4. Implement startLiveSession(prompt: string, personality: PersonalityType) function:
   - Call services/geminiLive.connectToLiveSession() directly (NOT via API route)
   - Receive streaming audio chunks via ReadableStream
   - For each chunk:
     a. Get base64 audio data from stream
     b. Decode base64 to Uint8Array
     c. Convert Int16 to Float32 (divide by 32768.0)
     d. The AudioWorklet handles 24kHz→48kHz conversion internally
     e. Send to AudioWorklet via postMessage
5. Implement stopLiveSession() to abort stream and close AudioContext if needed
6. Handle cleanup on unmount
7. Return { startSession, stopSession, isPlaying, error }

Key details:
- AudioContext must be created with { sampleRate: 48000 } for browser playback
- Base64 decode: atob() then Uint8Array
- Int16→Float32: sample / 32768.0
- The worklet does resampling - send 24kHz data as-is
- Send to worklet: workletNode.port.postMessage({ type: 'chunk', data: float32Array })

Note: Direct browser-to-Gemini connection (no backend API route needed). Import connectToLiveSession directly from services/geminiLive.ts.
  </action>
  <verify>
Hook file exists at hooks/useLiveAudio.ts with:
- AudioContext initialization at 48kHz
- AudioWorklet loading from /audio-processor.worklet.js
- startLiveSession function that calls services/geminiLive.connectToLiveSession() directly
- stopLiveSession function for cleanup
- Proper Int16→Float32 conversion
</verify>
  <done>
useLiveAudio hook created that streams audio chunks from Gemini Live API (direct connection) to browser AudioWorklet
</done>
</task>

</tasks>

<verification>
Hook created and ready to integrate with Roast.exe
</verification>

<success_criteria>
1. useLiveAudio hook handles streaming audio playback
2. Chunked audio plays as it arrives (not waiting for full response)
</success_criteria>

<output>
After completion, create `.planning/phases/02-live-api/02-02-SUMMARY.md`
</output>
