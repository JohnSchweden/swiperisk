---
phase: 01-voice-files
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: []
autonomous: true
user_setup:
  - service: google-gemini
    why: "Voice generation via Gemini TTS API"
    env_vars:
      - name: GEMINI_API_KEY
        source: "Google AI Studio -> API Key"

must_haves:
  truths:
    - "Roaster victory and failure voice files can be generated"
    - "All 3 core Roaster files (onboarding, victory, failure) work together"
  artifacts:
    - path: "public/audio/voices/roaster/victory.wav"
      provides: "Victory voice message"
    - path: "public/audio/voices/roaster/failure.wav"
      provides: "Failure voice message"
  key_links: []
---

<objective>
Generate Roaster victory and failure voice files, completing the core 3-file set (onboarding already exists from Plan 01).

Purpose: Test that multiple voice files can be generated consistently. These 3 files (onboarding, victory, failure) are the primary triggers for all personalities.

Output: 2 new WAV files at `public/audio/voices/roaster/victory.wav` and `public/audio/voices/roaster/failure.wav`
</objective>

<execution_context>
@/Users/yevgenschweden/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/yevgenschweden/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-voice-files/01-CONTEXT.md
@.planning/ROADMAP.md

# Reference the onboarding file from Plan 01
@public/audio/voices/roaster/onboarding.wav
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate Roaster victory voice file</name>
  <files>public/audio/voices/roaster/victory.wav</files>
  <action>
1. Use Gemini TTS API to generate audio for:
   "I... don't hate it. Adequate performance. Here's a badge. Now leave."
2. Output format: WAV (PCM 16-bit, 24kHz, mono)
3. Save to: `public/audio/voices/roaster/victory.wav`
4. Use same generation approach as Plan 01
  </action>
  <verify>
- File exists: `ls -la public/audio/voices/roaster/victory.wav`
- File is valid WAV with audio content
  </verify>
  <done>
`public/audio/voices/roaster/victory.wav` exists and is a valid WAV file.
</done>
</task>

<task type="auto">
  <name>Task 2: Generate Roaster failure voice file</name>
  <files>public/audio/voices/roaster/failure.wav</files>
  <action>
1. Use Gemini TTS API to generate audio for:
   "Well, you managed to violate basic common sense. The legal team is crying. Pathetic."
2. Output format: WAV (PCM 16-bit, 24kHz, mono)
3. Save to: `public/audio/voices/roaster/failure.wav`
4. Use same generation approach as Plan 01
  </action>
  <verify>
- File exists: `ls -la public/audio/voices/roaster/failure.wav`
- File is valid WAV with audio content
  </verify>
  <done>
`public/audio/voices/roaster/failure.wav` exists and is a valid WAV file.
</done>
</task>

<task type="auto">
  <name>Task 3: Verify all 3 core Roaster files exist</name>
  <files>public/audio/voices/roaster/</files>
  <action>
1. List all files in `public/audio/voices/roaster/` directory
2. Verify these 3 files exist:
   - onboarding.wav
   - victory.wav  
   - failure.wav
3. Verify each file is valid WAV format
  </action>
  <verify>
- All 3 files exist with size > 10KB each
- All 3 are valid WAV format
  </verify>
  <done>
All 3 core Roaster voice files (onboarding, victory, failure) are generated and valid.
</done>
</task>

</tasks>

<verification>
3 core Roaster files ready for playback integration testing.
</verification>

<success_criteria>
- [x] `public/audio/voices/roaster/victory.wav` exists and valid
- [x] `public/audio/voices/roaster/failure.wav` exists and valid
- [x] All 3 core files (onboarding, victory, failure) verified
</success_criteria>

<output>
After completion, create `.planning/phases/01-voice-files/01-SUMMARY.md`
</output>
