---
phase: 21-refactor-the-glassmorphism-design
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
  - components/game/selectionStageStyles.ts
  - components/game/CardStack.tsx
  - components/game/FeedbackOverlay.tsx
  - components/game/RoastTerminal.tsx
  - components/game/InitializingScreen.tsx
  - components/game/Taskbar.tsx
  - components/game/StarfieldBackground.tsx
autonomous: true

must_haves:
  truths:
    - "Starfield visible through glass panels on desktop and mobile Safari"
    - "All glass panels use consistent low-opacity tint + strong blur + saturation"
    - "Components using shared constants auto-upgrade without additional changes"
  artifacts:
    - path: "index.html"
      provides: "Global glass CSS classes"
      contains: ".glass-card, .glass-strong, .glass-header"
    - path: "components/game/selectionStageStyles.ts"
      provides: "Shared glass constants for selection screens"
      exports: ["GLASS_FILL_STRONG", "GLASS_PANEL_DEFAULT"]
    - path: "components/game/CardStack.tsx"
      provides: "Incident card glass"
      contains: "incidentCardGlass uses glass-card"
  key_links:
    - from: "selectionStageStyles.ts"
      to: "index.html CSS classes"
      via: "references class names"
    - from: "CardStack.tsx, Taskbar.tsx, StarfieldBackground.tsx"
      to: "selectionStageStyles.ts constants"
      via: "import and use GLASS_* constants"
---

<objective>
Replace fake glass (heavy `bg-black/65` + weak `backdrop-blur-sm`) with real glassmorphism (low-opacity tint + strong blur + saturation boost) across all game components.

Purpose: Create consistent, visually appealing glass effect that allows background (starfield, etc.) to show through properly, matching reference design from sololevel-marketing-site.

Output: 9 components + CSS + shared constants all using real glassmorphism recipe.
</objective>

<context>
@.planning/phases/21-refactor-the-glassmorphism-design/21-CONTEXT.md
@components/game/selectionStageStyles.ts
@index.html (lines 16-150 for existing CSS patterns)

Reference: sololevel-marketing-site glass recipe
</context>

<tasks>

<task type="auto">
  <name>Add 3 glass CSS classes to index.html</name>
  <files>index.html</files>
  <action>
Add the following glass classes to index.html after the existing `.swipe-exit-left` animation block (around line 150):

```css
/* Glassmorphism - real glass effect with low opacity + strong blur + saturation */
.glass-card {
  background: rgba(71, 71, 71, 0.22);
  border: rgba(255, 255, 255, 0.08);
  backdrop-filter: blur(8px) saturate(150%);
  -webkit-backdrop-filter: blur(8px) saturate(150%);
}

.glass-strong {
  background: rgba(20, 20, 20, 0.45);
  backdrop-filter: blur(12px) saturate(160%);
  -webkit-backdrop-filter: blur(12px) saturate(160%);
}

.glass-header {
  background: rgba(14, 14, 14, 0.25);
  backdrop-filter: blur(20px) saturate(180%);
  -webkit-backdrop-filter: blur(20px) saturate(180%);
}
```

Keep custom shadows where they exist (e.g., Taskbar has shadow-lg). Include WebKit prefix for Safari support.
  </action>
  <verify>Run `bun run typecheck` to verify no errors</verify>
  <done>3 glass classes exist in index.html with proper blur/saturation values</done>
</task>

<task type="auto">
  <name>Update shared constants in selectionStageStyles.ts</name>
  <files>components/game/selectionStageStyles.ts</files>
  <action>
Update the glass constants to reference the new CSS classes:

1. Change `GLASS_FILL_STRONG` from:
   `"bg-black/65 shadow-lg ${GLASS_BACKDROP}"` 
   to:
   `"glass-strong shadow-lg"`

2. Change `GLASS_PANEL_DEFAULT` from:
   `"border border-white/10 ${GLASS_FILL_STRONG}"`
   to:
   `"glass-card"`

3. Keep `GLASS_BACKDROP` as is (may be used elsewhere), or comment it out if not needed.

This makes PersonalitySelect, RoleSelect, and all debrief pages automatically use the new glass.
  </action>
  <verify>Run `bun run typecheck` — verify constants compile and are exported</verify>
  <done>GLASS_FILL_STRONG and GLASS_PANEL_DEFAULT use new CSS classes</done>
</task>

<task type="auto">
  <name>Update 7 components with inline glass definitions</name>
  <files>
    - components/game/CardStack.tsx
    - components/game/FeedbackOverlay.tsx
    - components/game/RoastTerminal.tsx
    - components/game/InitializingScreen.tsx
    - components/game/Taskbar.tsx
    - components/game/BossFight.tsx
    - components/game/StarfieldBackground.tsx
  </files>
  <action>
For each component, replace fake glass with real glassmorphism:

**CardStack.tsx** (line 10-11):
- Replace `incidentCardGlass = "bg-black/65 border border-white/10 shadow-lg backdrop-blur-sm backdrop-saturate-100"`
- With `incidentCardGlass = "glass-card"`

**FeedbackOverlay.tsx** (line 126, 136, 465):
- Replace modal overlay class `bg-black/50` → use appropriate glass class
- Replace escalation badges `bg-black/30` → `glass-card`
- Replace learning moment box `bg-black/50 border border-white/5` → `glass-card`

**RoastTerminal.tsx** (line 78):
- Replace wrapper `bg-black/80 border border-slate-800` → `glass-card`
- Keep existing shadow-2xl

**InitializingScreen.tsx** (line 23):
- Replace card `bg-black/80 border border-slate-800` → `glass-card`

**Taskbar.tsx** (line 79):
- Replace main bar `bg-slate-950/55 backdrop-blur-lg backdrop-saturate-100` → `glass-header`
- Keep existing shadow-[0_-8px_32px_rgba(0,0,0,0.35)]

**BossFight.tsx** (line 52):
- Replace quiz panel `bg-slate-900 border border-slate-700` → `glass-card`

**StarfieldBackground.tsx** (line 207):
- Replace speed panel `bg-black/65 border border-white/10 shadow-lg backdrop-blur-sm backdrop-saturate-100` → `glass-card`

Preserve any existing custom shadows (shadow-lg, shadow-2xl, etc.) as noted in CONTEXT.
  </action>
  <verify>Run `bun run typecheck && bun run test:smoke`</verify>
  <done>All 7 components use real glassmorphism classes</done>
</task>

</tasks>

<verification>
- [ ] Run `bun run typecheck` — no TypeScript errors
- [ ] Run `bun run test:smoke` — all tests pass
- [ ] Visual spot-check: starfield visible through glass on desktop and mobile Safari
</verification>

<success_criteria>
- CSS classes `.glass-card`, `.glass-strong`, `.glass-header` exist in index.html
- selectionStageStyles.ts exports updated GLASS_FILL_STRONG and GLASS_PANEL_DEFAULT
- 7 components (CardStack, FeedbackOverlay, RoastTerminal, InitializingScreen, Taskbar, BossFight, StarfieldBackground) use glass CSS classes
- Components using shared constants (PersonalitySelect, RoleSelect, Debrief pages) auto-upgrade
- Starfield visible through glass panels (low opacity enables visibility)
</success_criteria>

<output>
After completion, create `.planning/phases/21-refactor-the-glassmorphism-design/21-01-SUMMARY.md`
</output>