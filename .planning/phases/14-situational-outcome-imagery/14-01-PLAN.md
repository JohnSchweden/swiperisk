---
phase: 14-situational-outcome-imagery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/ImageWithFallback.tsx
  - utils/imagePaths.ts
  - types.ts
  - tests/image-incident-card.spec.ts
  - tests/image-fallback.spec.ts
autonomous: true
requirements:
  - IMAGE-01
  - IMAGE-05
must_haves:
  truths:
    - ImageWithFallback component exists with glitch placeholder fallback
    - Image path utilities generate correct paths following Phase 13 naming
    - Types extended with optional image fields for cards, outcomes, endings, archetypes
    - Test stubs created for incident card images and fallback behavior
  artifacts:
    - path: components/ImageWithFallback.tsx
      provides: Reusable image component with loading state and glitch placeholder
      exports: [ImageWithFallback]
    - path: utils/imagePaths.ts
      provides: Image path resolution utilities
      exports: [getIncidentImagePath, getOutcomeImagePath, getCollapseImagePath, getArchetypeImagePath]
  key_links:
    - from: ImageWithFallback
      to: components using images
      via: props (src, alt, aspectRatio)
    - from: imagePaths
      to: ImageWithFallback
      via: src prop
---

<objective>
Create the foundational image infrastructure: reusable ImageWithFallback component with glitch placeholder, image path utilities, type extensions, and test scaffolding. This is Wave 0/1 foundation that all other plans depend on.

Purpose: Establish the core image rendering pattern before integrating into UI surfaces.
Output: ImageWithFallback component, imagePaths utilities, extended types, failing test stubs.
</objective>

<execution_context>
@/Users/yevgenschweden/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/yevgenschweden/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-situational-outcome-imagery/14-CONTEXT.md
@.planning/phases/14-situational-outcome-imagery/14-RESEARCH.md

# Phase 13 establishes image naming convention:
# - /images/incident-{cardId}.webp — incident card images
# - /images/outcome-{cardId}-{direction}.webp — outcome images (left/right variants)
# - /images/collapse-{deathType}.webp — death ending images
# - /images/archetype-{archetypeId}.webp — archetype badge images
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ImageWithFallback component with glitch placeholder</name>
  <files>components/ImageWithFallback.tsx</files>
  <action>
Create a reusable ImageWithFallback component at components/ImageWithFallback.tsx:

1. Component accepts props:
   - src: string (image path)
   - alt: string (alt text)
   - aspectRatio?: 'video' | 'square' | 'auto' (default: 'video')
   - className?: string (additional classes)
   - containerClassName?: string (container classes)

2. State management:
   - isLoaded: boolean (tracks if image loaded successfully)
   - hasError: boolean (tracks if image failed to load)

3. Container styling:
   - relative positioning
   - overflow-hidden
   - rounded-lg border
   - aspect ratio via aspect-video (16:9) or aspect-square (1:1)
   - Use Tailwind classes: aspect-video, aspect-square, overflow-hidden, rounded-lg

4. Glitch placeholder (shown while loading or on error):
   - Absolute inset-0 positioning
   - Dark background (slate-800/900 gradient)
   - Cyan icon (fa-image) with animate-pulse
   - Scanline effect via CSS (add to index.html or component):
     - Repeating linear gradient lines
     - Subtle animation (glitch-scan keyframes)
   - opacity-100 when placeholder visible, opacity-0 when image loaded

5. Image element:
   - loading="lazy" for lazy loading
   - object-cover for cropping
   - opacity-0 initially, opacity-100 when loaded
   - transition-opacity duration-300 ease-out
   - onLoad: setIsLoaded(true)
   - onError: setHasError(true)

6. Export as named export: export function ImageWithFallback(...)

Reference patterns from 14-RESEARCH.md Architecture section.
</action>
  <verify>bun run typecheck && bunx playwright test tests/image-fallback.spec.ts --reporter=list 2>&1 | head -30</verify>
  <done>ImageWithFallback component exists at components/ImageWithFallback.tsx with glitch placeholder, proper TypeScript types, and exports correctly. Component has loading state management and fallback behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Create image path utility functions</name>
  <files>utils/imagePaths.ts</files>
  <action>
Create image path utility functions at utils/imagePaths.ts:

1. getIncidentImagePath(cardId: string): string
   - Returns: `/images/incident-${cardId}.webp`
   - Handles card ID normalization if needed

2. getOutcomeImagePath(cardId: string, direction: 'LEFT' | 'RIGHT'): string
   - Returns: `/images/outcome-${cardId}-${direction.toLowerCase()}.webp`
   - Direction should be lowercase in filename

3. getCollapseImagePath(deathType: DeathType): string
   - Returns: `/images/collapse-${deathType.toLowerCase()}.webp`
   - Import DeathType from types.ts

4. getArchetypeImagePath(archetypeId: ArchetypeId): string
   - Returns: `/images/archetype-${archetypeId.toLowerCase()}.webp`
   - Import ArchetypeId from types.ts

5. All functions should:
   - Be pure functions (no side effects)
   - Handle string normalization (lowercase)
   - Return paths starting with / (root-relative)
   - Include JSDoc comments

6. Export all functions as named exports.

Create utils/ directory if it doesn't exist.
</action>
  <verify>bun run typecheck && grep -E "export (function|const)" utils/imagePaths.ts | wc -l</verify>
  <done>All 4 image path utility functions exist in utils/imagePaths.ts, properly typed, and export correctly. Functions generate paths following Phase 13 naming convention.</done>
</task>

<task type="auto">
  <name>Task 3: Extend types with optional image fields</name>
  <files>types.ts</files>
  <action>
Extend types.ts with optional image fields for Phase 14:

1. Update Card interface:
   - Add optional `image?: string` field (incident card image path)
   - Place after realWorldReference field

2. Update onRight and onLeft (ChoiceOutcome) in Card:
   - Both already have the structure defined inline
   - Add optional `image?: string` to each outcome object
   - This allows per-outcome images

3. Update DEATH_ENDINGS type in data/deathEndings.ts (via type extension or inline):
   - The existing type doesn't need to change structurally
   - Add optional `image?: string` to the value type
   - This is in data/deathEndings.ts, will be updated in Plan 03

4. Update Archetype interface:
   - Add optional `image?: string` field
   - This is in types.ts Archetype interface

Note: Type changes should be backward compatible (all fields optional).
Do not break existing card data that lacks images.
</action>
  <verify>bun run typecheck && grep -n "image?:" types.ts</verify>
  <done>Types.ts extended with optional image fields on Card (incident), ChoiceOutcome (outcome), and Archetype (badge). All fields are optional for backward compatibility.</done>
</task>

<task type="auto">
  <name>Task 4: Create test stubs for incident images and fallback</name>
  <files>
    tests/image-incident-card.spec.ts
    tests/image-fallback.spec.ts
  </files>
  <action>
Create test stub files (Wave 0 tests that will initially fail, then pass):

1. tests/image-incident-card.spec.ts:
   - Import { test, expect } from '@playwright/test'
   - Test: "Incident card displays image with 16:9 aspect ratio"
   - Test: "Image moves with card during swipe gesture"
   - Test: "Image lazy loads with loading='lazy' attribute"
   - Tests should expect elements that don't exist yet (RED phase)

2. tests/image-fallback.spec.ts:
   - Test: "Glitch placeholder shown while image loads"
   - Test: "Glitch placeholder shown when image fails to load"
   - Test: "Image fades in over 300ms when loaded"
   - Test: "Placeholder has glitch aesthetic (scanline animation)"

Use existing test patterns from other spec files:
- test.use({ baseURL: "https://localhost:3000" })
- test.describe with @area:layout or @area:gameplay tags
- page.goto("/") then setup game state via localStorage if needed
- Assertions using expect(locator).toBeVisible(), .toHaveAttribute(), etc.

Tests should initially fail (components don't exist yet) - this is intentional Wave 0 RED phase.
</action>
  <verify>bun run typecheck && ls -la tests/image-*.spec.ts | wc -l</verify>
  <done>Two test files created with comprehensive test stubs covering IMAGE-01 and IMAGE-05 requirements. Tests follow project conventions and will initially fail (Wave 0 RED).</done>
</task>

</tasks>

<verification>
- [ ] ImageWithFallback component renders without errors
- [ ] TypeScript compiles without type errors (bun run typecheck)
- [ ] Image path utilities return correct paths for all 4 image types
- [ ] Types extended with optional image fields
- [ ] Test stubs exist and are structured correctly
</verification>

<success_criteria>
- ImageWithFallback component exists with glitch placeholder and lazy loading
- utils/imagePaths.ts exports all 4 path resolution functions
- types.ts has optional image fields on Card, ChoiceOutcome, Archetype
- tests/image-incident-card.spec.ts and tests/image-fallback.spec.ts exist
- All tests can be run (even if failing)
- TypeScript compilation passes
</success_criteria>

<output>
After completion, create `.planning/phases/14-situational-outcome-imagery/14-01-SUMMARY.md`
</output>
