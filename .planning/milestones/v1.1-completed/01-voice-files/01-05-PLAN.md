---
phase: 01-voice-files
plan: 05
type: TDD
wave: 5
depends_on: ["01-04"]
files_modified: []
autonomous: true

must_haves:
  truths:
    - "Voice plays on personality selection (onboarding)"
    - "Voice plays on victory"
    - "Voice plays on failure"
    - "Roaster feedback voices play after specific actions"
  artifacts:
    - path: "App.tsx"
      provides: "Integration of voice playback with game flow"
      modifies: "Add voice.playVoice() calls at correct trigger points"
---

<objective>
Integrate voice playback with game triggers.

Purpose: Wire up the voice playback system to play at the correct moments in the game flow.
Output: Voice files play automatically based on game state
</objective>

<execution_context>
@/Users/yevgenschweden/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/yevgenschweden/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-voice-files/01-04-SUMMARY.md
@services/voicePlayback.ts (newly created)
@App.tsx (existing game flow)
@constants.ts (PERSONALITIES)
</context>

<tasks>

<task type="auto">
  <name>Integrate voice playback with game triggers</name>
  <files>App.tsx</files>
  <action>
Add voice playback integration to App.tsx:

1. Import voice playback functions:
   ```typescript
   import { loadVoice, playVoice, stopVoice } from './services/voicePlayback';
   ```

2. Add voice loading on personality selection:
   - When user selects a personality (in onboarding flow), call loadVoice with the selected personality and 'onboarding' trigger
   - After loading, automatically play with playVoice()

3. Add voice triggers at game outcomes:
   - When stage is completed (victory): call loadVoice(selectedPersonality, 'victory') then playVoice()
   - When stage is failed: call loadVoice(selectedPersonality, 'failure') then playVoice()

4. Add Roaster-specific feedback triggers:
   - After 'paste' action: loadVoice('roaster', 'feedback_paste') then playVoice()
   - After 'debug' action: loadVoice('roaster', 'feedback_debug') then playVoice()
   - After 'install' action: loadVoice('roaster', 'feedback_install') then playVoice()
   - After 'ignore' action: loadVoice('roaster', 'feedback_ignore') then playVoice()

5. Add stopVoice() calls:
   - When leaving a stage or resetting, call stopVoice() to stop any playing audio

6. Error handling:
   - Wrap voice calls in try/catch
   - On error, log the narrative error message but don't block game flow
   - Game continues silently if voice fails
  </action>
  <verify>
1. Check App.tsx has voice import
2. Check for loadVoice/playVoice calls at:
   - Personality selection (onboarding)
   - Victory condition
   - Failure condition
   - Roaster feedback actions
3. Run `npm run build` to verify no TypeScript errors
  </verify>
  <done>
- App.tsx imports voicePlayback functions
- Voice plays on personality selection (onboarding)
- Voice plays on victory
- Voice plays on failure
- Roaster feedback voices trigger after actions
- Error handling preserves game flow on voice failure
  </done>
</task>

</tasks>

<verification>
- [ ] App.tsx has voice import
- [ ] Voice plays at onboarding (personality selection)
- [ ] Voice plays on victory
- [ ] Voice plays on failure
- [ ] Roaster feedback voices play after actions
- [ ] Build passes without errors
</verification>

<success_criteria>
Voice playback integrated with all game triggers
</success_criteria>

<output>
After completion, create `.planning/phases/01-voice-files/01-05-SUMMARY.md`
</output>
