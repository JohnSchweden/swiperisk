---
phase: 04-immersive-pressure-effects
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - hooks/useIncidentPressure.ts
  - services/pressureAudio.ts
autonomous: true
gap_closure: true
requirements:
  - IMMERSE-02
  - IMMERSE-03

must_haves:
  truths:
    - "Stress effects (shake, flicker, pulse) activate when heat is critical, not only countdown"
    - "Heartbeat plays when heat is high; volume 10% louder"
  artifacts:
    - path: "hooks/useIncidentPressure.ts"
      provides: "isCritical = criticalFromScenario || heatHigh"
    - path: "services/pressureAudio.ts"
      provides: "GAIN_HEARTBEAT 0.132"
  key_links:
    - from: "useIncidentPressure.isCritical"
      to: "CardStack stress visuals, usePressureAudio"
      via: "Consumers already use isCritical for shake/flicker/pulse and audio"
---

<objective>
Fix card stress and heartbeat for high heat: isCritical currently gates heat behind scenario (heatHigh only counts when scenario != null). Change to isCritical = criticalFromScenario || heatHigh. Also increase heartbeat volume 10% (0.12 → 0.132).
</objective>

<context>
@.planning/phases/04-immersive-pressure-effects/04-UAT.md

Round 2 Gaps (tests 4, 5):
- Card stress: Works for countdown, but when heat critical the card doesn't show stress effects
- Heartbeat: Only plays when countdown/pressure on card; when heat high, no heartbeat. Also make 10% louder.
Root cause: useIncidentPressure line 41: isCritical = criticalFromScenario || (scenario != null && heatHigh) — heat only counts when card has pressure metadata.
</context>

<interfaces>
From hooks/useIncidentPressure.ts:
- isCritical = criticalFromScenario || (scenario != null && heatHigh)  — change second term to heatHigh only
- heatHigh = state.heat >= 70

From services/pressureAudio.ts:
- GAIN_HEARTBEAT = 0.12 — change to 0.132 (10% increase)
- Used in playPulse: gain.gain.linearRampToValueAtTime(GAIN_HEARTBEAT, ...)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: isCritical includes heat without scenario gate</name>
  <files>hooks/useIncidentPressure.ts</files>
  <action>
Change line 41:
From: isCritical = criticalFromScenario || (scenario != null && heatHigh)
To:   isCritical = criticalFromScenario || heatHigh

This allows card stress visuals and heartbeat to trigger when heat >= 70, regardless of whether the current card has pressure metadata.
</action>
  <verify>
    <automated>bunx playwright test tests/immersive-pressure-cues.spec.ts -x</automated>
  </verify>
  <done>When heat >= 70, isCritical is true; card shows stress effects and heartbeat plays.</done>
</task>

<task type="auto">
  <name>Task 2: Heartbeat volume 10% louder</name>
  <files>services/pressureAudio.ts</files>
  <action>
Change GAIN_HEARTBEAT from 0.12 to 0.132 (10% increase: 0.12 * 1.1 ≈ 0.132).
</action>
  <verify>
    <automated>bun run typecheck</automated>
  </verify>
  <done>Heartbeat plays at 0.132 gain when critical.</done>
</task>

</tasks>

<verification>
Dev role, make choices that raise heat to >= 70 without urgent countdown on card. Card shows shake/flicker/pulse. Heartbeat plays. Volume noticeably louder than before.
</verification>

<success_criteria>
- isCritical true when heat >= 70 (heatHigh) or criticalFromScenario
- GAIN_HEARTBEAT = 0.132
</success_criteria>

<output>
After completion, create .planning/phases/04-immersive-pressure-effects/04-07-SUMMARY.md
</output>
