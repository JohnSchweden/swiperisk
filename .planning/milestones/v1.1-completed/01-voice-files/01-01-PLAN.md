---
phase: 01-voice-files
plan: 01
type: TDD
wave: 1
depends_on: []
files_modified: []
autonomous: true

must_haves:
  truths:
    - "1 voice file generated as valid WAV"
    - "WAV file is valid PCM 16-bit, 24kHz, mono format"
    - "WAV file can be played (playback test passes)"
  artifacts:
    - path: "public/audio/voices/roaster/onboarding.wav"
      provides: "First voice file for testing pipeline"
      format: "WAV PCM 16-bit 24kHz mono"
  key_links:
    - from: "geminiService.ts"
      to: "public/audio/voices/roaster/onboarding.wav"
      via: "TTS API call saves to file"
---

<objective>
Generate the first voice file to test the TTS pipeline end-to-end.

Purpose: Verify Gemini TTS can generate valid WAV files before generating all 13 files. This is a pipeline validation step.
Output: One working WAV file at `public/audio/voices/roaster/onboarding.wav`
</objective>

<execution_context>
@/Users/yevgenschweden/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/yevgenschweden/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-voice-files/01-CONTEXT.md

Locked decisions (do not revisit):
- Gemini TTS model: gemini-2.5-flash-preview-tts
- Audio format: WAV (PCM 16-bit, 24kHz, mono)
- File location: public/audio/voices/{personality}/{trigger}.wav
- Error handling: In-game narrative framing
</context>

<tasks>

<task type="auto">
  <name>Generate first voice file using Gemini TTS</name>
  <files>public/audio/voices/roaster/onboarding.wav</files>
  <action>
1. Create directory structure: `public/audio/voices/roaster/`
2. Call Gemini TTS API with gemini-2.5-flash-preview-tts model
3. Use text: "Oh, look. Another 'Visionary' hired to save the company. Try not to destroy us in the first 5 minutes, yeah?"
4. Request output as WAV (PCM 16-bit, 24kHz, mono)
5. Save response to `public/audio/voices/roaster/onboarding.wav`

Use the geminiService.ts as reference for TTS API call patterns. The API call should:
- Use GoogleGenAI client
- Call models.generateContent with model: "gemini-2.5-flash-preview-tts"
- Set responseModalities: [Modality.AUDIO]
- Extract base64 audio from response.candidates[0].content.parts[0].inlineData.data
- Decode and save as WAV file
  </action>
  <verify>
1. Run `file public/audio/voices/roaster/onboarding.wav` - should show "RIFF WAVE audio"
2. Run `ffplay -autoexit -nodisp public/audio/voices/roaster/onboarding.wav 2>&1` - should play without errors, or use Node script to verify file can be decoded
3. Verify file size > 1KB (non-empty)
  </verify>
  <done>
- public/audio/voices/roaster/onboarding.wav exists
- File command shows "RIFF WAVE audio"
- Playback test passes (file decodes without errors)
  </done>
</task>

</tasks>

<verification>
- [ ] `file public/audio/voices/roaster/onboarding.wav` returns "RIFF WAVE"
- [ ] File size > 1KB
- [ ] Playback test passes
</verification>

<success_criteria>
1 voice file generated and verified as valid WAV format
</success_criteria>

<output>
After completion, create `.planning/phases/01-voice-files/01-01-SUMMARY.md`
</output>
