---
phase: 14-situational-outcome-imagery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/ImageWithFallback.tsx
  - types.ts
  - tests/image-incident-card.spec.ts
  - tests/image-fallback.spec.ts
autonomous: true
requirements:
  - IMAGE-01
  - IMAGE-05
must_haves:
  truths:
    - ImageWithFallback component exists with glitch placeholder fallback
    - Phase 13 imageMap.ts helpers used for all image path resolution (no separate imagePaths.ts)
    - Only Archetype interface gets optional image field (Card and ChoiceOutcome unchanged)
    - Test stubs scoped to HOS pilot role (or placeholder path for non-pilot)
  artifacts:
    - path: components/ImageWithFallback.tsx
      provides: Reusable image component with loading state and glitch placeholder
      exports: [ImageWithFallback]
  key_links:
    - from: ImageWithFallback
      to: components using images
      via: props (src, alt, aspectRatio)
    - from: data/imageMap.ts
      to: ImageWithFallback
      via: src prop (helpers from Phase 13)
---

<objective>
Create the foundational image infrastructure: reusable ImageWithFallback component with glitch placeholder, extend Archetype type with image field, and create test scaffolding. Phase 13's `data/imageMap.ts` provides all path resolution — no separate `utils/imagePaths.ts` needed.

Purpose: Establish the core image rendering pattern before integrating into UI surfaces.
Output: ImageWithFallback component, extended Archetype type, failing test stubs scoped to HOS pilot.
</objective>

<execution_context>
@/Users/yevgenschweden/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yevgenschweden/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-situational-outcome-imagery/14-CONTEXT.md
@.planning/phases/14-situational-outcome-imagery/14-RESEARCH.md
@.planning/phases/13-image-asset-pipeline/13-CONTRACT.md
@data/imageMap.ts

# Phase 13 imageMap.ts (see 13-CONTRACT.md):
# - INCIDENT_IMAGES: slugified incident → path
# - OUTCOME_IMAGES: keys `${incidentSlug}-left` / `-right`
# - ARCHETYPE_IMAGES, DEATH_IMAGES: enum-keyed
# - getOutcomeImagePath(incidentSlug, "LEFT" | "RIGHT") → string | undefined
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ImageWithFallback component with glitch placeholder</name>
  <files>components/ImageWithFallback.tsx</files>
  <action>
Create a reusable ImageWithFallback component at components/ImageWithFallback.tsx:

1. Component accepts props:
   - src: string (image path)
   - alt: string (alt text)
   - aspectRatio?: 'video' | 'square' | 'auto' (default: 'video')
   - className?: string (additional classes)
   - containerClassName?: string (container classes)

2. State management:
   - isLoaded: boolean (tracks if image loaded successfully)
   - hasError: boolean (tracks if image failed to load)

3. Container styling:
   - relative positioning
   - overflow-hidden
   - rounded-lg border
   - aspect ratio via aspect-video (16:9) or aspect-square (1:1)
   - Use Tailwind classes: aspect-video, aspect-square, overflow-hidden, rounded-lg

4. Glitch placeholder (shown while loading or on error):
   - Absolute inset-0 positioning
   - Dark background (slate-800/900 gradient)
   - Cyan icon (fa-image) with animate-pulse
   - Scanline effect via CSS (add to index.html or component):
     - Repeating linear gradient lines
     - Subtle animation (glitch-scan keyframes)
   - opacity-100 when placeholder visible, opacity-0 when image loaded

5. Image element:
   - loading="lazy" for native lazy loading (no IntersectionObserver — native only)
   - object-cover for cropping
   - opacity-0 initially, opacity-100 when loaded
   - transition-opacity duration-300 ease-out
   - onLoad: setIsLoaded(true)
   - onError: setHasError(true)

6. Export as named export: export function ImageWithFallback(...)

Reference patterns from 14-RESEARCH.md Architecture section.
</action>
  <verify>bun run typecheck && bun run test -- tests/image-fallback.spec.ts --reporter=list 2>&1 | head -30</verify>
  <done>ImageWithFallback component exists at components/ImageWithFallback.tsx with glitch placeholder, proper TypeScript types, and exports correctly. Component has loading state management and fallback behavior. Uses native loading="lazy" only (no IntersectionObserver).</done>
</task>

<task type="auto">
  <name>Task 2: Extend Archetype interface with optional image field</name>
  <files>types.ts</files>
  <action>
Extend ONLY the Archetype interface in types.ts with an optional image field:

1. Update Archetype interface (around line 194-201):
   - Add optional `image?: string` field after existing fields
   - This is used for the verdict page badge image

2. DO NOT add image fields to:
   - Card interface — images resolved via imageMap.ts lookup using realWorldReference.incident slug
   - ChoiceOutcome — no image field; outcome overlay uses incident slug + swipe direction via imageMap
   - Death endings type — death images resolved via imageMap.ts using DeathType

3. The Archetype interface is a small fixed set (7 entries including KIRK), so an inline `image?` field is appropriate. Cards are dynamic and use the imageMap lookup pattern instead.

Note: Type change is backward compatible (field is optional).
Do not break existing archetype data that lacks images.
</action>
  <verify>bun run typecheck && grep -n "image?:" types.ts</verify>
  <done>types.ts Archetype interface extended with optional image field. Card and ChoiceOutcome interfaces left unchanged — they resolve images via imageMap.ts lookup.</done>
</task>

<task type="auto">
  <name>Task 3: Create test stubs for incident images and fallback (HOS pilot scope)</name>
  <files>
    tests/image-incident-card.spec.ts
    tests/image-fallback.spec.ts
  </files>
  <action>
Create test stub files scoped to **Head of Something (HOS)** pilot role:

1. tests/image-incident-card.spec.ts:
   - Import { test, expect } from '@playwright/test'
   - Test: "HOS incident card displays image with 16:9 aspect ratio"
   - Test: "HOS card image resolves via realWorldReference.incident slug"
   - Test: "Image moves with card during swipe gesture"
   - Test: "Image lazy loads with loading='lazy' attribute"
   - Test: "Non-HOS role shows placeholder when image missing"
   - Scope assertions to HOS pilot cards; other roles verify placeholder behavior

2. tests/image-fallback.spec.ts:
   - Test: "Glitch placeholder shown while image loads"
   - Test: "Glitch placeholder shown when image fails to load"
   - Test: "Image fades in over 300ms when loaded"
   - Test: "Placeholder has glitch aesthetic (scanline animation)"

Use existing test patterns from other spec files:
- test.use({ baseURL: "https://localhost:3000" })
- test.describe with @area:layout or @area:gameplay tags
- page.goto("/") then setup game state via localStorage if needed
- Assertions using expect(locator).toBeVisible(), .toHaveAttribute(), etc.

Key: HOS cards have `realWorldReference.incident` populated for pilot assets (see **13-CONTRACT.md**).
Other roles test the "placeholder shown when missing" path.

Tests should initially fail (components don't exist yet) - this is intentional Wave 0 RED phase.
</action>
  <verify>bun run typecheck && ls -la tests/image-*.spec.ts | wc -l</verify>
  <done>Two test files created with comprehensive test stubs covering IMAGE-01 and IMAGE-05 requirements. Tests scoped to HOS pilot with placeholder assertions for other roles. Tests follow project conventions and will initially fail (Wave 0 RED).</done>
</task>

</tasks>

<verification>
- [ ] ImageWithFallback component renders without errors
- [ ] TypeScript compiles without type errors (bun run typecheck)
- [ ] Image path resolution uses data/imageMap.ts helpers (no utils/imagePaths.ts created)
- [ ] Only Archetype interface extended with image field (Card/ChoiceOutcome unchanged)
- [ ] Test stubs exist, scoped to HOS pilot, and are structured correctly
</verification>

<success_criteria>
- ImageWithFallback component exists with glitch placeholder and native lazy loading (no IntersectionObserver)
- No utils/imagePaths.ts file created — all path resolution via data/imageMap.ts from Phase 13
- types.ts has optional image field on Archetype only (not Card or ChoiceOutcome)
- tests/image-incident-card.spec.ts scoped to HOS pilot role
- tests/image-fallback.spec.ts covers placeholder behavior
- All tests can be run (even if failing)
- TypeScript compilation passes
</success_criteria>

<output>
After completion, create `.planning/phases/14-situational-outcome-imagery/14-01-SUMMARY.md`
</output>
