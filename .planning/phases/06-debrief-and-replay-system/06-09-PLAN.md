---
phase: 06-debrief-and-replay-system
plan: 09
type: auto
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - components/game/debrief/DebriefPage3Verdict.tsx
autonomous: true

must_haves:
  truths:
    - "Email capture form is always visible on Page 3"
    - "Form shows input field and submit button (not just envelope icon)"
  artifacts:
    - path: "components/game/debrief/DebriefPage3Verdict.tsx"
      provides: "Unconditional EmailCaptureForm rendering"
  key_links:
    - from: "DebriefPage3Verdict"
      to: "EmailCaptureForm"
      via: "Always render, pass role/archetype (or defaults)"

gap_reference:
  test: 5
  gap_id: 4
  truth: "Email capture form with input field and submit button is visible and functional on Page 3"
  severity: major
---

<objective>
Fix email capture form visibility: Remove conditional rendering so form is always visible.

Current issue: Form is conditionally rendered only when role && archetype are truthy. If either is null, users only see the envelope icon and description text, not the actual form.

Fix: Always render EmailCaptureForm. Pass role/archetype if available, or use fallback values if not.
</objective>

<files>
- components/game/debrief/DebriefPage3Verdict.tsx
</files>

<implementation_notes>
Current code (lines 121-127):
  {role && archetype && (
    <EmailCaptureForm ... />
  )}

Change to:
  <EmailCaptureForm 
    role={role || "unknown"}
    archetype={archetype || { name: "Unknown", id: "UNKNOWN" }}
    ...
  />

The form component should handle null/undefined gracefully and show disabled state if needed.
</implementation_notes>

<verify>
1. Play game to completion
2. Navigate to Page 3 (Verdict) of debrief
3. Scroll down to email capture section
4. Verify input field and submit button are visible (not just envelope icon)
5. Verify can type email and submit
</verify>
