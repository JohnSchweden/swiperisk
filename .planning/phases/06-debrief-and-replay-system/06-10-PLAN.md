---
phase: 06-debrief-and-replay-system
plan: 10
type: auto
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - components/game/SummaryScreen.tsx
  - App.tsx
autonomous: true

must_haves:
  truths:
    - "Success screen (SUMMARY stage) shows same metrics as failure screen (GAME_OVER)"
    - "Success screen has [Debrief Me] button instead of [Log off]"
    - "Can navigate to debrief flow from success screen"
  artifacts:
    - path: "components/game/SummaryScreen.tsx"
      provides: "Budget/Heat/Hype metrics grid, Debrief button"
    - path: "App.tsx"
      provides: "Wired onDebrief callback"
  key_links:
    - from: "SummaryScreen"
      to: "DebriefContainer"
      via: "onDebrief callback"

gap_reference:
  test: 6
  gap_id: 5
  truth: "Success game over screen consistently shows Debrief button and all metrics"
  severity: major
---

<objective>
Fix success screen UI: Make SUMMARY stage consistent with GAME_OVER stage.

Current issue: Success screen (quarters survived) shows different UI than failure screen:
- Has [Log off] button instead of [Debrief Me]
- Only shows budget, NOT heat/hype metrics
- Cannot navigate to debrief flow

Fix: Add same metrics grid and [Debrief Me] button to SummaryScreen. Wire onDebrief callback in App.tsx.
</objective>

<files>
- components/game/SummaryScreen.tsx
- App.tsx
</files>

<implementation_notes>
SummaryScreen.tsx needs:
1. Add Budget/Heat/Hype metrics grid (like GameOver.tsx lines 62-91)
2. Change "Log off" button to "Debrief Me" button
3. Add onDebrief prop to interface
4. Call onDebrief when button clicked

App.tsx needs:
1. Pass onDebrief callback to SummaryScreen (lines 478-479)
2. Callback should trigger debrief flow entry (similar to GameOver)
</implementation_notes>

<verify>
1. Play game and survive all quarters (success ending)
2. Verify success screen shows Budget, Heat, Hype metrics
3. Verify [Debrief Me] button is present (not [Log off])
4. Click [Debrief Me] and verify debrief flow starts
</verify>
