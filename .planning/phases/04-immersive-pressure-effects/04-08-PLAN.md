---
phase: 04-immersive-pressure-effects
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - hooks/useSwipeGestures.ts
  - App.tsx
autonomous: true
gap_closure: true
requirements:
  - IMMERSE-05

must_haves:
  truths:
    - "Touch-swipe on mobile triggers haptic when critical/urgent"
  artifacts:
    - path: "hooks/useSwipeGestures.ts"
      provides: "onBeforeSwipe called sync when direction known"
    - path: "App.tsx"
      provides: "onBeforeSwipe that vibrates when pressure.isCritical || pressure.isUrgent"
  key_links:
    - from: "useSwipeGestures.handleTouchEnd"
      to: "navigator.vibrate"
      via: "onBeforeSwipe(direction) called sync before setTimeout"
---

<objective>
Add haptic to touch-swipe path. Mobile uses touch-swipe (useSwipeGestures); vibrate is currently only in swipe buttons (onSwipeLeft/onSwipeRight). Touch path: handleTouchEnd → setTimeout(350ms) → onSwipe. Call vibrate synchronously when direction is known, before setTimeout, so it runs within user gesture.
</objective>

<context>
@.planning/phases/04-immersive-pressure-effects/04-UAT.md
@.planning/debug/haptic-mobile-round2.md

Round 2 Gap (test 7): User reported "no vibrations" on mobile. Root cause: vibrate only in swipe buttons; mobile uses touch-swipe. handleTouchEnd fires from touch event (user gesture) but onSwipe is invoked from setTimeout — outside gesture. Fix: call vibrate sync in handleTouchEnd when direction known, before scheduling setTimeout.
</context>

<interfaces>
From hooks/useSwipeGestures.ts:
- UseSwipeGesturesOptions: { enabled, onSwipe }
- handleTouchEnd: when Math.abs(finalOffset) > SWIPE_THRESHOLD, direction = finalOffset > 0 ? "RIGHT" : "LEFT", then setState, then setTimeout 350 → onSwipe(direction)

Add: onBeforeSwipe?: (direction: "LEFT" | "RIGHT") => void — optional, called sync when direction determined, before setTimeout.

From App.tsx:
- pressure.isCritical, pressure.isUrgent available
- swipe = useSwipeGestures({ enabled, onSwipe: handleChoice })
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add onBeforeSwipe to touch-swipe path</name>
  <files>hooks/useSwipeGestures.ts, App.tsx</files>
  <action>
1. useSwipeGestures: Extend UseSwipeGesturesOptions with optional onBeforeSwipe?: (direction: "LEFT" | "RIGHT") => void.

2. In handleTouchEnd, when Math.abs(finalOffset) &gt; SWIPE_THRESHOLD:
   - Compute direction = finalOffset &gt; 0 ? "RIGHT" : "LEFT"
   - Call onBeforeSwipe?.(direction) synchronously, BEFORE setState or setTimeout
   - Then proceed with setState, setTimeout(350) → onSwipe(direction)

3. App.tsx: Pass onBeforeSwipe to useSwipeGestures:
   onBeforeSwipe: (direction) => {
     if ((pressure.isCritical || pressure.isUrgent) && typeof navigator !== "undefined" && "vibrate" in navigator && typeof navigator.vibrate === "function") {
       navigator.vibrate([50, 30, 50]);
     }
   }

Use same pattern as onSwipeLeft/onSwipeRight: vibrate pattern [50, 30, 50]. The callback must be sync and run in the same call stack as the touch event. Do NOT call onBeforeSwipe from the setTimeout callback.
</action>
  <verify>
    <automated>bun run typecheck</automated>
  </verify>
  <done>Touch-swipe on mobile with critical/urgent card triggers vibrate before animation completes.</done>
</task>

</tasks>

<verification>
On mobile with vibration enabled: swipe card by touch when urgent or heat critical. Device vibrates. (Desktop/Playwright cannot verify; typecheck and code review confirm implementation.)
</verification>

<success_criteria>
- onBeforeSwipe called synchronously in handleTouchEnd when direction known
- App passes onBeforeSwipe that vibrates when pressure.isCritical || pressure.isUrgent
- Vibrate runs before any setTimeout (within user gesture)
</success_criteria>

<output>
After completion, create .planning/phases/04-immersive-pressure-effects/04-08-SUMMARY.md
</output>
