---
phase: 24-consensus-copy-and-funnel-fixes
plan: "05"
type: execute
wave: 5
depends_on:
  - "24-04"
files_modified: []
autonomous: true
requirements:
  - VERIFY-01

must_haves:
  truths:
    - "bun run typecheck exits 0"
    - "bun run lint exits 0"
    - "bun run test:unit exits 0"
    - "bun run test:smoke exits 0"
  artifacts: []
  key_links: []
---

<objective>
Final verification pass: typecheck, lint, unit tests, smoke tests. Confirms all four waves landed cleanly with no regressions.

Purpose: Gate before marking phase complete. All previous plans are autonomous edits — this plan proves they integrated without type errors, lint violations, or test failures.
Output: No file changes. Green CI signals across all four test tiers.
</objective>

<execution_context>
@/Users/yevgenschweden/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yevgenschweden/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/24-consensus-copy-and-funnel-fixes/24-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Typecheck and lint</name>
  <files></files>
  <action>
Run the two static analysis checks in sequence:

```bash
bun run typecheck
bun run lint
```

If typecheck fails: read the error output, identify which file and line, fix the type error (most likely a missing import or wrong type on getPublicGameUrl()), then re-run.

If lint fails: run `bun run lint:fix` to auto-fix Biome issues, then verify the fix didn't introduce logic errors.
  </action>
  <verify>
    <automated>cd /Users/yevgenschweden/swiperisk && bun run typecheck && bun run lint</automated>
  </verify>
  <done>Both commands exit 0 with no errors.</done>
</task>

<task type="auto">
  <name>Task 2: Unit tests and smoke tests</name>
  <files></files>
  <action>
Run unit tests then smoke tests:

```bash
bun run test:unit
bun run test:smoke
```

If `test:unit` fails on linkedin-share.test.ts: the test expectations from 24-04 may not fully cover edge cases — re-read the actual error output and adjust test assertions to match the actual output of `formatShareText` with the new template.

If `test:smoke` fails on PRISON heading: confirm `deathEndings.ts` was updated in 24-01 and `image-collapse-page.spec.ts` was updated in 24-04.

Smoke tests require the dev server running. If not already running, start it: `bun dev &` then wait 3 seconds before running tests.
  </action>
  <verify>
    <automated>cd /Users/yevgenschweden/swiperisk && bun run test:unit 2>&1 | tail -10</automated>
  </verify>
  <done>test:unit exits 0. test:smoke exits 0. Phase 24 verification complete.</done>
</task>

</tasks>

<verification>
All four verification checks green:
1. `bun run typecheck` — 0 errors
2. `bun run lint` — 0 warnings/errors
3. `bun run test:unit` — all tests pass
4. `bun run test:smoke` — all tests pass
</verification>

<success_criteria>
Phase 24 is complete when all four commands exit 0 with no failures. The copy and funnel fixes are live with full test coverage.
</success_criteria>

<output>
After completion, create `.planning/phases/24-consensus-copy-and-funnel-fixes/24-05-SUMMARY.md`
</output>
