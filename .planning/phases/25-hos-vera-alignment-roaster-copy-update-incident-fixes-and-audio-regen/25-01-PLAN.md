---
phase: 25-hos-vera-alignment-roaster-copy-update-incident-fixes-and-audio-regen
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/cards/head-of-something.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "hos_ai_management_elimination has a long setup paragraph as storyContext (5th arg) and a short binary prompt as text (6th arg)"
    - "hos_process_automation_takeover has a long setup paragraph as storyContext (5th arg) and a short binary prompt as text (6th arg)"
    - "No wording changes — only arg positions swapped"
  artifacts:
    - path: "src/data/cards/head-of-something.ts"
      provides: "makeCard arg-order fix for 2 cards"
      contains: "hos_ai_management_elimination"
  key_links:
    - from: "makeCard call site"
      to: "types.ts makeCard signature"
      via: "arg position 5=storyContext, 6=text"
      pattern: "makeCard\\(.*storyContext.*text"
---

<objective>
Swap the 5th and 6th `makeCard` string arguments for two cards so that the long setup paragraph is `storyContext` (5th) and the short binary prompt is `text` (6th). No content changes — position only.

Purpose: Both cards currently have their args reversed, causing the card title/prompt to display the long paragraph and the story context to show the short question.
Output: `head-of-something.ts` with corrected arg order for `hos_ai_management_elimination` and `hos_process_automation_takeover`.
</objective>

<execution_context>
@/Users/yevgenschweden/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yevgenschweden/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/data/cards/head-of-something.ts

<interfaces>
<!-- makeCard signature from src/types.ts — arg order matters -->
<!-- makeCard(id, realWorld, leftOutcome, rightOutcome, storyContext, text) -->
<!-- 5th arg = storyContext (long background paragraph shown below card) -->
<!-- 6th arg = text (short binary question shown as card prompt) -->

For hos_ai_management_elimination (lines ~814-858):
  CURRENT (wrong):
    5th arg (text): "Volunteer to pilot your own replacement or fight the restructuring?"
    6th arg (storyContext): "The CTO's 'AI-First Operations' report concluded that AI orchestration tools can replace 60% of middle management coordination tasks. Your entire Head of Something layer is being evaluated for elimination. You can volunteer to lead the pilot — proving AI can do your job — or fight the restructure through HR."
  TARGET (correct):
    5th arg (storyContext): "The CTO's 'AI-First Operations' report concluded..." (long paragraph)
    6th arg (text): "Volunteer to pilot your own replacement or fight the restructuring?" (short question)

For hos_process_automation_takeover (lines ~859-904):
  CURRENT (wrong):
    5th arg (text): "Honestly document your remaining value-add or inflate your contribution metrics?"
    6th arg (storyContext): "Your team's workflow automation initiative has been so successful that the AI tools now handle all the status updates, sprint planning coordination, and stakeholder reporting you used to manage. The quarterly org review asks: does your role still add value beyond what the tools already do?"
  TARGET (correct):
    5th arg (storyContext): "Your team's workflow automation initiative..." (long paragraph)
    6th arg (text): "Honestly document your remaining value-add or inflate your contribution metrics?" (short question)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap makeCard storyContext/text args for hos_ai_management_elimination</name>
  <files>src/data/cards/head-of-something.ts</files>
  <action>
    Locate the `hos_ai_management_elimination` makeCard call (around line 813). The call currently has:
    - 5th arg (wrong position): `"Volunteer to pilot your own replacement or fight the restructuring?"`
    - 6th arg (wrong position): `"The CTO's 'AI-First Operations' report concluded that AI orchestration tools can replace 60% of middle management coordination tasks. Your entire Head of Something layer is being evaluated for elimination. You can volunteer to lead the pilot — proving AI can do your job — or fight the restructure through HR."`

    Swap them so the long paragraph is 5th (storyContext) and the short question is 6th (text). Do NOT change any wording.
  </action>
  <verify>
    <automated>bun run typecheck</automated>
  </verify>
  <done>5th arg is the long paragraph starting "The CTO's..."; 6th arg is the short question "Volunteer to pilot..."</done>
</task>

<task type="auto">
  <name>Task 2: Swap makeCard storyContext/text args for hos_process_automation_takeover</name>
  <files>src/data/cards/head-of-something.ts</files>
  <action>
    Locate the `hos_process_automation_takeover` makeCard call (around line 859). The call currently has:
    - 5th arg (wrong position): `"Honestly document your remaining value-add or inflate your contribution metrics?"`
    - 6th arg (wrong position): `"Your team's workflow automation initiative has been so successful that the AI tools now handle all the status updates, sprint planning coordination, and stakeholder reporting you used to manage. The quarterly org review asks: does your role still add value beyond what the tools already do?"`

    Swap them so the long paragraph is 5th (storyContext) and the short question is 6th (text). Do NOT change any wording.

    After both swaps, run typecheck and the data tests to confirm no regressions.
  </action>
  <verify>
    <automated>bun run typecheck && bun run test:data</automated>
  </verify>
  <done>5th arg is the long paragraph starting "Your team's workflow..."; 6th arg is the short question "Honestly document...". Both typechecks and data tests pass.</done>
</task>

</tasks>

<verification>
- `bun run typecheck` passes with no errors
- `bun run test:data` passes
- Both cards have long paragraph as storyContext (5th) and short question as text (6th)
- No other lines in head-of-something.ts were modified
</verification>

<success_criteria>
Two makeCard calls corrected. Arg swap only — zero wording changes. Typecheck and data tests green.
</success_criteria>

<output>
After completion, create `.planning/phases/25-hos-vera-alignment-roaster-copy-update-incident-fixes-and-audio-regen/25-01-SUMMARY.md`
</output>
