---
phase: 24-consensus-copy-and-funnel-fixes
plan: "02"
type: execute
wave: 2
depends_on:
  - "24-01"
files_modified:
  - src/utils/linkedin-share.ts
  - src/components/game/debrief/DebriefPage3Verdict.tsx
autonomous: true
requirements:
  - BRAND-01
  - SHARE-01

must_haves:
  truths:
    - "formatShareText produces K-Maru-first body with concise resilience line"
    - "getShareUrl title is 'K-Maru — {archetype.name}' (em dash, no 'Archetype' suffix)"
    - "KIRK_SHARE_TEXT uses getPublicGameUrl() instead of hardcoded URL"
    - "updateMetaTags ogDesc uses concise brag copy (no 'Kobayashi Maru' in description)"
    - "updateMetaTags ogTitle uses em dash (not hyphen)"
    - "V2 DM button is demoted to secondary/ghost style"
    - "Helper hint paragraph inserted above LinkedIn button section"
  artifacts:
    - path: "src/utils/linkedin-share.ts"
      provides: "Updated formatShareText and getShareUrl"
      contains: "K-Maru"
    - path: "src/components/game/debrief/DebriefPage3Verdict.tsx"
      provides: "Updated KIRK_SHARE_TEXT, updateMetaTags, V2 button style, helper hint"
      contains: "getPublicGameUrl"
  key_links:
    - from: "src/components/game/debrief/DebriefPage3Verdict.tsx"
      to: "src/lib/publicGameUrl.ts"
      via: "import { getPublicGameUrl } from '../../../lib/publicGameUrl'"
      pattern: "getPublicGameUrl"
    - from: "KIRK_SHARE_TEXT"
      to: "getPublicGameUrl()"
      via: "template literal"
      pattern: "getPublicGameUrl\\(\\)"
---

<objective>
Brand spine and share text unification: update linkedin-share.ts to K-Maru-first copy, fix the getShareUrl title em dash, update DebriefPage3Verdict to use getPublicGameUrl() in KIRK_SHARE_TEXT, update updateMetaTags to concise copy, demote V2 button to secondary style, and add the LinkedIn helper hint.

Purpose: Unifies the brand voice — K-Maru leads everywhere, Kobayashi Maru is secondary. Fixes the hardcoded URL in KIRK path and the misleading primary-style V2 button.
Output: 2 files updated — linkedin-share.ts and DebriefPage3Verdict.tsx.
</objective>

<execution_context>
@/Users/yevgenschweden/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yevgenschweden/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/24-consensus-copy-and-funnel-fixes/24-CONTEXT.md
@src/utils/linkedin-share.ts
@src/components/game/debrief/DebriefPage3Verdict.tsx
</context>

<interfaces>
<!-- From src/lib/publicGameUrl.ts (created in 24-01) -->
```typescript
export function getPublicGameUrl(): string
// Returns VITE_PUBLIC_GAME_URL env or "https://k-maru-seven.vercel.app"
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Update linkedin-share.ts formatShareText and getShareUrl</name>
  <files>src/utils/linkedin-share.ts</files>
  <action>
Replace the `formatShareText` function body. Current return template literal:
```typescript
return `I just faced the AI Kobayashi Maru as a ${roleTitle}.

My Resilience Score: ${clampedResilience}% (${archetypeName}). Can you beat my score?

Try the No-Win Simulation and swipe your way through the AI Singularity.

It's not about passing; it's about discovering who you are when the system collapses.

[NOTICE: Made for people who hate f*cking boring training]

${url}`;
```
Replace with:
```typescript
return `I just finished K-Maru as a ${roleTitle} — the AI no-win swipe game (Kobayashi energy, corporate liability).
Resilience: ${clampedResilience}% (${archetypeName}). Beat my score?
${url}
[NOTICE: Made for people who hate f*cking boring training]`;
```

In `getShareUrl`, replace the title line:
Before:
```typescript
const title = `Kobayashi Maru - ${archetype.name} Archetype`;
```
After:
```typescript
const title = `K-Maru — ${archetype.name}`;
```
  </action>
  <verify>
    <automated>cd /Users/yevgenschweden/swiperisk && grep -n "K-Maru" src/utils/linkedin-share.ts && grep -n "Kobayashi energy" src/utils/linkedin-share.ts && grep -n "K-Maru —" src/utils/linkedin-share.ts</automated>
  </verify>
  <done>formatShareText body uses K-Maru-first template. getShareUrl title uses em dash without "Archetype" suffix.</done>
</task>

<task type="auto">
  <name>Task 2: Update DebriefPage3Verdict — KIRK text, meta tags, hint, V2 button</name>
  <files>src/components/game/debrief/DebriefPage3Verdict.tsx</files>
  <action>
**Step 1 — Add import for getPublicGameUrl** at the top of the file, after the existing utils imports:
```typescript
import { getPublicGameUrl } from "../../../lib/publicGameUrl";
```

**Step 2 — Update KIRK_SHARE_TEXT** (currently a `const` string literal):
Before:
```typescript
const KIRK_SHARE_TEXT =
	"I broke the Kobayashi Maru. There was always a third option. Kirk would be proud.\n\nhttps://k-maru-seven.vercel.app/";
```
After (note: KIRK_SHARE_TEXT must become a function or be evaluated at component render time since it calls getPublicGameUrl()):
```typescript
const KIRK_SHARE_TEXT = `I broke K-Maru's no-win test. Third option. Kirk-coded.\n\n${getPublicGameUrl()}/`;
```

**Step 3 — Update updateMetaTags ogTitle** — fix hyphen → em dash and lowercase "resilience":
Before:
```typescript
const ogTitle = `K-Maru - ${archetype.name}${roleLabel} • ${resilience}% Resilience`;
```
After:
```typescript
const ogTitle = `K-Maru — ${archetype.name}${roleLabel} • ${resilience}% resilience`;
```

**Step 4 — Update updateMetaTags ogDesc** — drop "Kobayashi Maru", use concise copy:
Before:
```typescript
const ogDesc = `I faced the Kobayashi Maru as a ${role || "leader"} and discovered my leadership archetype. Can you beat my score?`;
```
After:
```typescript
const ogDesc = `Finished K-Maru as a ${role || "leader"}. Beat my resilience score?`;
```

**Step 5 — Add helper hint paragraph** in the JSX, immediately before the `{/* Action Buttons */}` `<div>` block. The hint explains LinkedIn preview behavior. Insert after the resilience score block closing `</div>` and before `{/* Action Buttons */}`:
```tsx
{/* LinkedIn share hint */}
<p className="text-xs text-slate-500 mb-4 text-center px-2">
	Copy the post text first — LinkedIn usually shows the site&apos;s static
	preview, not text from this screen.
</p>
```

**Step 6 — Demote V2 DM button style** in the V2 Waitlist section. Find:
```typescript
className="inline-flex items-center justify-center gap-2 px-4 py-2 md:px-8 md:py-3 text-base font-bold bg-cyan-600 hover:bg-cyan-500 text-white transition-colors duration-200 min-h-[40px] md:min-h-[48px]"
```
Replace with:
```typescript
className="inline-flex items-center justify-center gap-2 px-4 py-2 md:px-8 md:py-3 text-base font-bold border border-cyan-500/40 bg-transparent text-cyan-300 hover:bg-cyan-500/10 transition-colors duration-200 min-h-[40px] md:min-h-[48px]"
```
  </action>
  <verify>
    <automated>cd /Users/yevgenschweden/swiperisk && grep -n "getPublicGameUrl" src/components/game/debrief/DebriefPage3Verdict.tsx && grep -n "Kirk-coded" src/components/game/debrief/DebriefPage3Verdict.tsx && grep -n "bg-transparent text-cyan-300" src/components/game/debrief/DebriefPage3Verdict.tsx && grep -n "Copy the post text first" src/components/game/debrief/DebriefPage3Verdict.tsx</automated>
  </verify>
  <done>DebriefPage3Verdict imports getPublicGameUrl, KIRK_SHARE_TEXT uses it, updateMetaTags uses concise copy, hint paragraph is present, V2 button has ghost style.</done>
</task>

</tasks>

<verification>
- `bun run typecheck` passes (no type errors from new import)
- KIRK_SHARE_TEXT contains "Kirk-coded" and calls getPublicGameUrl()
- V2 DM button has `bg-transparent text-cyan-300` classes
- Helper hint text present above action buttons
</verification>

<success_criteria>
- Share text no longer opens with "I just faced the AI Kobayashi Maru"
- getShareUrl title uses em dash
- V2 button is visually demoted (ghost/outline vs previously filled cyan)
- KIRK path uses env-driven URL constant
</success_criteria>

<output>
After completion, create `.planning/phases/24-consensus-copy-and-funnel-fixes/24-02-SUMMARY.md`
</output>
