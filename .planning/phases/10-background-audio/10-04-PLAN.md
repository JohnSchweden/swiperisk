---
phase: 10-background-audio
plan: "04"
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - services/gameAudio.ts
  - services/voicePlayback.ts
  - components/SettingsPanel.tsx
  - components/game/IntroScreen.tsx
  - hooks/useAudioSettings.ts
  - tests/volume-controls.spec.ts
autonomous: true
must_haves:
  truths:
    - "Users can independently adjust voice, music, and SFX volume levels."
    - "Volume settings persist across browser sessions."
    - "Volume hierarchy (Voice > Music > SFX) is enforced through the mix."
  artifacts:
    - path: "hooks/useAudioSettings.ts"
      provides: "React hook for volume state management and localStorage persistence."
      exports: ["useAudioSettings"]
    - path: "components/SettingsPanel.tsx"
      provides: "Three-channel volume mixer UI with voice, music, and SFX sliders."
      contains: "volume"
    - path: "services/gameAudio.ts"
      provides: "Volume control methods integrated with the audio engine."
      contains: "setVolume"
    - path: "tests/volume-controls.spec.ts"
      provides: "Playwright coverage for volume slider behavior and persistence."
key_links:
  - from: "hooks/useAudioSettings.ts"
    to: "services/gameAudio.ts"
    via: "hook applies volume changes to the shared audio engine"
    pattern: "setVolume|musicVolume|sfxVolume"
  - from: "components/SettingsPanel.tsx"
    to: "hooks/useAudioSettings.ts"
    via: "sliders bind to the settings hook for real-time volume adjustment"
    pattern: "useAudioSettings"
  - from: "services/voicePlayback.ts"
    to: "services/gameAudio.ts"
    via: "voice volume controlled through the shared volume system"
    pattern: "voiceVolume|setVolume"
---

<objective>
Add volume controls for the three-channel audio mix so users can adjust voice, music, and SFX levels independently. Settings persist across sessions.

Purpose: Deliver the volume mixing controls specified in CONTEXT.md (Voice > Music > SFX hierarchy) as part of Phase 10 rather than deferring to Phase 11.
Output: Volume settings hook, settings panel UI, engine integration, and browser automation coverage.
</objective>

<execution_context>
@/Users/yevgenschweden/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/yevgenschweden/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-background-audio/10-CONTEXT.md
@.planning/phases/10-background-audio/10-01-PLAN.md
@services/gameAudio.ts
@services/voicePlayback.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend the audio engine with volume control methods</name>
  <files>services/gameAudio.ts, services/voicePlayback.ts</files>
  <action>Add volume control methods to `services/gameAudio.ts` that allow setting separate volume levels for music and SFX channels. Integrate these with the existing gain nodes. Also update `services/voicePlayback.ts` to use the voice volume setting from the shared settings system. Ensure volume changes apply in real-time without restarting audio. Add a mute toggle that silences all non-voice audio when active.</action>
  <verify>`bun run typecheck && bun run build`</verify>
  <done>The audio engine exposes setVolume methods for music and SFX, voice playback respects voice volume, and mute toggle works.</done>
</task>

<task type="auto">
  <name>Task 2: Create useAudioSettings hook with localStorage persistence</name>
  <files>hooks/useAudioSettings.ts</files>
  <action>Create a React hook `useAudioSettings` that manages three volume values (voice, music, SFX) with defaults of 0.8, 0.5, 0.6 respectively. Persist settings to localStorage under a stable key. On mount, load saved values or fall back to defaults. Expose a resetToDefaults function. The hook should subscribe to changes and update the audio engine in real-time.</action>
  <verify>`bun run typecheck && bun run build`</verify>
  <done>Settings persist across page reloads, defaults are sensible, and the hook integrates with the audio engine.</done>
</task>

<task type="auto">
  <name>Task 3: Build SettingsPanel component with three-channel mixer UI</name>
  <files>components/SettingsPanel.tsx, components/game/IntroScreen.tsx</files>
  <action>Create a `SettingsPanel` component with three range sliders for voice, music, and SFX volume. Use labels and visual indicators to show current levels. Include a mute toggle. Style appropriately for the game's aesthetic. Add the settings panel to the intro screen or as an accessible modal/drawer from the main menu. Follow existing component patterns in the codebase.</action>
  <verify>`bun run typecheck && bun run build && bun run test -- tests/volume-controls.spec.ts`</verify>
  <done>Users can access volume controls from the main UI, adjust all three channels, and see their changes applied in real-time.</done>
</task>

<task type="auto">
  <name>Task 4: Add browser automation coverage for volume controls</name>
  <files>tests/volume-controls.spec.ts</files>
  <action>Create a Playwright test suite that verifies: volume sliders adjust values, settings persist after page reload, mute toggle works, and the settings panel opens/closes correctly. Use deterministic state verification rather than checking actual audio output.</action>
  <verify>`bun run test -- tests/volume-controls.spec.ts`</verify>
  <done>Volume control behavior is regression-tested through browser automation.</done>
</task>

</tasks>

<verification>
1. Run `bun run typecheck`.
2. Run `bun run build`.
3. Run `bun run test -- tests/volume-controls.spec.ts`.
4. Manually verify volume sliders adjust audio levels and settings persist across refresh.
</verification>

<success_criteria>
- Three-channel volume mixer (voice, music, SFX) is accessible from the UI.
- Volume settings persist in localStorage across browser sessions.
- The audio hierarchy (Voice > Music > SFX) is enforced through the mix levels.
- Volume controls are included in Phase 10 as specified in CONTEXT.md.
</success_criteria>

<output>
After completion, create `.planning/phases/10-background-audio/10-04-SUMMARY.md`
</output>
