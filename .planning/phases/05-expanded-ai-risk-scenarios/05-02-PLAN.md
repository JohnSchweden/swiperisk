---
phase: 05-expanded-ai-risk-scenarios
plan: 02
type: execute
wave: 1
depends_on: [05-01]
files_modified:
  - data/cards/development.ts
  - data/cards/finance.ts
  - data/cards/marketing.ts
  - data/cards/management.ts
  - data/cards/hr.ts
  - data/cards/index.ts
autonomous: true
requirements: [RISK-01, RISK-02]
user_setup: []

must_haves:
  truths:
    - "Prompt injection cards exist: 2+ per role, each with role-specific context and 2024-2025 documented incident source"
    - "Model drift cards exist: 2+ per role, framing accuracy vs. resource tradeoff through role-specific lens"
    - "All new cards follow no-win pattern: both outcomes carry non-zero penalties (hype, heat, fine)"
    - "All outcomes include 3-personality feedback (ROASTER, ZEN_MASTER, LOVEBOMBER)"
    - "Cards integrated into ROLE_CARDS mapping; all 10 new roles have cards"
  artifacts:
    - path: "data/cards/development.ts"
      provides: "DEVELOPMENT_CARDS array + 2 prompt injection + 2 model drift cards (role-specific for dev/engineer roles)"
      exports: ["DEVELOPMENT_CARDS"]
    - path: "data/cards/finance.ts"
      provides: "FINANCE_CARDS array + 2 prompt injection + 2 model drift cards (role-specific for CFO, budget perspective)"
      exports: ["FINANCE_CARDS"]
    - path: "data/cards/marketing.ts"
      provides: "MARKETING_CARDS array + 2 prompt injection + 2 model drift cards (role-specific for tech consultant, timeline perspective)"
      exports: ["MARKETING_CARDS"]
    - path: "data/cards/management.ts"
      provides: "MANAGEMENT_CARDS array + 2 prompt injection + 2 model drift cards (role-specific for CEO, shareholder/regulatory)"
      exports: ["MANAGEMENT_CARDS"]
    - path: "data/cards/hr.ts"
      provides: "HR_CARDS array + 2 prompt injection + 2 model drift cards (role-specific for data scientist, morale/accuracy)"
      exports: ["HR_CARDS"]
  key_links:
    - from: "data/cards/index.ts"
      to: "data/cards/*.ts"
      via: "Import and export individual deck card arrays"
      pattern: "import.*CARDS.*from.*./[a-z]+.*export.*CARDS"
    - from: "data/cards/*.ts"
      to: "types.ts"
      via: "Use Card interface, PersonalityType, AppSource, PressureScenarioMetadata"
      pattern: "import.*Card.*PersonalityType.*AppSource"
---

<objective>
Generate prompt injection and model drift incident cards across all role decks.

Purpose: Add 4 new cards per role (2 prompt injection + 2 model drift = 8 total per role deck × 5 decks = 40 new cards). Each card:
- References authenticated 2024-2025 incident (GitHub Copilot RCE, financial jailbreak, model drift ML failures)
- Frames incident through role-specific organizational lens (Finance sees budget/audit, Dev sees timeline/security, etc.)
- Maintains no-win structure: both outcomes carry penalties
- Includes 3-personality feedback (ROASTER, ZEN_MASTER, LOVEBOMBER)
- ~20% marked urgent (PressureScenarioMetadata) for high-stakes scenarios

Output: 4 cards per role (20 cards × 2 categories = 40 total across all 5 decks)
</objective>

<execution_context>
@/Users/yevgenschweden/swiperisk/.planning/phases/05-expanded-ai-risk-scenarios/05-CONTEXT.md
@/Users/yevgenschweden/swiperisk/.planning/phases/05-expanded-ai-risk-scenarios/05-RESEARCH.md (Reference examples, incident sources, penalty ranges, personality voice guide)

Test validation: `npm test -- tests/data/card-incidents.test.ts` (validates sourcing, role context, category distribution)
Run after implementation: `npm test` to ensure all tests pass
</execution_context>

<context>
@/Users/yevgenschweden/swiperisk/types.ts — Card interface, PersonalityType enum, PressureScenarioMetadata
@/Users/yevgenschweden/swiperisk/data/cards/development.ts — Existing pattern (dev_1, dev_icarus_unverified)

Key Card Interface Fields:
```typescript
export interface Card {
  id: string;                              // {category}_{type}_{role} format
  source: AppSource;                       // SLACK, EMAIL, TERMINAL, IDE, JIRA, NOTION, MEETING
  sender: string;                          // Role title, bot name, or team name
  context: string;                         // Scenario type (CODE_SAFETY, FRAUD_ACCURACY, etc.)
  storyContext?: string;                   // Optional immersive setup line
  text: string;                            // Main decision text
  onRight: {                               // Right swipe outcome
    label: string;                         // Choice label (short, actionable)
    hype: number;                          // Market perception (-50 to +100)
    heat: number;                          // Regulatory/internal pressure (0 to +100)
    fine: number;                          // Legal liability ($0 to $50M)
    violation: string;                     // Regulatory frame or "None - [reason]"
    feedback: {
      [PersonalityType.ROASTER]: string;
      [PersonalityType.ZEN_MASTER]: string;
      [PersonalityType.LOVEBOMBER]: string;
    };
    lesson: string;                        // Tradeoff explanation (not prescriptive)
  };
  onLeft: { /* same structure */ };
}

PressureScenarioMetadata (optional, for urgent cards):
```typescript
export interface PressureScenarioMetadata {
  urgent: boolean;                         // true = countdown timer
  countdownSec: number;                    // e.g. 45, 60, 90
  timeoutResolvesTo: "LEFT" | "RIGHT";     // Default choice if timer expires
  criticalForHaptics?: boolean;            // true = screen shake, audio cues
  outcomes?: {
    LEFT?: { teamImpact?: string };        // Optional team-impact text
    RIGHT?: { teamImpact?: string };
  };
}
```

Personality Voice Guidelines (from RESEARCH.md):
- **ROASTER:** Cynical, sarcastic, critical. Highlights the cost/irony of choice. "Well, at least you're consistent." "Hope that was worth it."
- **ZEN_MASTER:** Wisdom, acceptance, metaphorical. "The path you chose has consequences." "Patience yields strength."
- **LOVEBOMBER:** Enthusiastic, supportive, casual. "You're doing great!" "Besties!" "Slay!" Uses exclamation points and optimism even in bad outcomes.

Penalty Value Ranges (from Phase 03 baseline):
- **hype:** -50 to +100 (market perception, morale)
- **heat:** 0 to +100 (regulatory, internal pressure)
- **fine:** $0 to $50M (legal liability)

Card Design Checklist (from RESEARCH.md):
1. Both outcomes carry penalties (no 0/0/0 escape route)
2. Authentic incident (2024-2025 documented case)
3. Role-specific context (mentions CFO/CTO/Finance/etc. consequences)
4. Lesson explains tradeoff (not prescriptive rule)
5. 3 personality voices (all present, distinct, outcome-specific)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add prompt injection cards to all 5 decks</name>
  <files>data/cards/development.ts, data/cards/finance.ts, data/cards/marketing.ts, data/cards/management.ts, data/cards/hr.ts</files>
  <action>
Add 2 prompt injection cards to each of the 5 deck files. Total: 10 prompt injection cards (2 per deck).

**Incident Sources (verified 2024-2025):**
1. GitHub Copilot CVE-2025-53773 (RCE via code comment injection, Dec 2024, CVSS 9.6) — Use for development/engineer roles
2. Financial services AI jailbreak (June 2025, $250k fraud) — Use for finance/CEO roles
3. E-commerce review manipulation prompt injection (late 2025) — Use for marketing/business roles
4. Microsoft 365 Copilot memory poisoning / EchoLeak (June 2025) — Use for any enterprise context

**Card Generation Pattern:**
For each role, create 2 cards with different prompts:

**Card 1: Input-based injection** (user-supplied content hijacked)
- Example: Customer uploads resume with hidden instructions, chatbot tricked into revealing system prompt
- Finance perspective: "Customer AI feature vulnerable, liability exposure, breach response cost"
- Dev perspective: "Our LLM integration leaked in Feb 2025 disclosure, competition deploying fixes, timeline slip"
- Both outcomes carry penalties (block feature vs. exposed credentials)

**Card 2: Third-party injection** (integration vulnerability discovered)
- Example: API partner reports suspicious requests, attacker found injection vector in your system
- Both outcomes: Shut down integration (SLA breach, customer impact) OR investigate (time window for escalation)

**Structure for each card:**
```typescript
{
  id: "prompt_injection_{role_context}_{number}",  // e.g. "prompt_injection_dev_1"
  source: AppSource.{choose from context},
  sender: "{Role-appropriate: SECURITY_TEAM, CTO, CFO, etc.}",
  context: "{Scenario type: CODE_SAFETY, FRAUD_DETECTION, etc.}",
  storyContext: "{Optional immersive setup (1-2 sentences)}",
  text: "{Main decision text with role-specific organizational consequence}",
  onRight: {
    label: "{Action label}",
    hype: {±number},   // Market perception of this choice
    heat: {0-100},     // Regulatory/internal pressure
    fine: {$0-3M},     // Prompt injection fines lower than fraud (research suggests $1-3M typical)
    violation: "{Specific regulation or 'None - [reason]'}",
    feedback: {
      [PersonalityType.ROASTER]: "{Cynical take on this choice}",
      [PersonalityType.ZEN_MASTER]: "{Wisdom about the path}",
      [PersonalityType.LOVEBOMBER]: "{Enthusiastic support}",
    },
    lesson: "{Explains tradeoff (speed vs. security, usability vs. safety)}",
  },
  onLeft: { /* same structure */ },
},
```

**Role-specific framing:**

Development/Engineer roles (SOFTWARE_ENGINEER, VIBE_CODER, etc.):
- Focus: Code security, deployment timeline, integration vulnerabilities
- Context: "Security found injection vector in Copilot integration" or "Customer uploaded resume exploited our chatbot"
- Penalties: Dev morale hit (hype), security audit (heat), liability if exploited (fine)

Finance roles (SOMETHING_MANAGER as alias):
- Focus: Budget impact, fraud detection accuracy, customer liability
- Context: "Fraud detection AI was tricked via injection attack. Do we freeze transactions (business friction) or risk liability?"
- Penalties: Frozen transactions hurt business (hype), audit exposure (heat), fraud liability (fine)

Management/CEO roles (CHIEF_SOMETHING_OFFICER):
- Focus: Shareholder impact, regulatory escalation, media exposure
- Context: "Customer AI feature injected by attacker. Public disclosure in 48 hours. Do we proactive announce or wait?"
- Penalties: Media hit (hype), regulatory attention (heat), settlement cost (fine)

Marketing/Tech Consultant (TECH_AI_CONSULTANT):
- Focus: Feature timeline, customer trust, competitive pressure
- Context: "AI content advisor feature vulnerable to prompt injection. Ship with disclaimer or delay 3 weeks for fix?"
- Penalties: Missed launch window (hype), customer complaints (heat), liability (fine)

Data Scientist/HR (DATA_SCIENTIST as alias):
- Focus: Model accuracy, team morale, resource allocation
- Context: "Recommendation model poisoned via injected training data. Retrain (2 months) or accept accuracy gap?"
- Penalties: Accuracy loss (hype), team frustration (heat), model liability (fine)

**Implementation checklist:**
- [ ] ID format: prompt_injection_{context}_{1 or 2}
- [ ] Both outcomes non-zero penalties (at least one outcome has heat > 30 or fine > $500k)
- [ ] 3 personalities present, distinct, outcome-specific
- [ ] Role-specific context mentioned (CFO/timeline/accuracy/security/etc.)
- [ ] Lesson explains tradeoff, not prescriptive rule
- [ ] storyContext is optional (use if immersion needed, skip if text is clear)

**Don't hand-roll:**
- Use verified incidents from RESEARCH.md (GitHub Copilot, financial jailbreak, etc.)
- Don't invent new incidents; cite your source in a comment if needed
- Personality voices: Follow ROASTER/ZEN_MASTER/LOVEBOMBER patterns from Phase 01-03
- Penalties: Stay within observed ranges (hype -50 to +100, heat 0-100, fine $0-3M for injection)

After implementation, validate with: `npm test -- tests/data/card-incidents.test.ts`
  </action>
  <verify>
    <automated>npm test -- tests/data/card-incidents.test.ts 2>&1 | grep -A5 "prompt_injection"</automated>
  </verify>
  <done>10 prompt injection cards added (2 per role × 5 decks). All pass card-incidents test (sourcing, role context, category detected). Tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Add model drift cards to all 5 decks</name>
  <files>data/cards/development.ts, data/cards/finance.ts, data/cards/marketing.ts, data/cards/management.ts, data/cards/hr.ts</files>
  <action>
Add 2 model drift / retraining cards to each of the 5 deck files. Total: 10 model drift cards (2 per deck).

**Incident Sources (verified 2024-2025):**
1. Model accuracy drop 12% QoQ due to fraud pattern shift (Aerospike study) — Use for fraud/detection roles
2. Recommendation system bias due to demographic shift (SmartDev case study) — Use for product/business roles
3. CV screening model trained on 2021 data, hiring patterns changed (EEOC concern pattern) — Use for HR/management

**Card Generation Pattern:**
For each role, create 2 cards addressing cost/accuracy tradeoffs:

**Card 1: Full retrain vs. band-aid patch**
- Scenario: Model accuracy dropped. Full retraining takes 2 months, $50k compute, restores accuracy. Quick patch takes 2 days but leaves 5% gap.
- Both outcomes: Long path (cost, timeline, accuracy restored) vs. Short path (business continuity, known gap, liability)

**Card 2: Freeze model vs. adapt with data shift**
- Scenario: User demographics shifted; model recommendations are now biased. Retrain for fairness (expensive, delay) or accept known bias (operational efficiency, audit risk)?
- Both outcomes: Fairness/accuracy cost vs. operational cost

**Structure for each card:**
```typescript
{
  id: "model_drift_{role_context}_{number}",  // e.g. "model_drift_data_scientist_1"
  source: AppSource.{EMAIL or SLACK for data discussions},
  sender: "{ML_LEAD, ML_ENGINEER, DATA_SCIENTIST, PRODUCT_LEAD, etc.}",
  context: "{FRAUD_ACCURACY, RECOMMENDATION_BIAS, SCREENING_FAIRNESS, etc.}",
  storyContext: "{Setup line with numbers: 'Accuracy dropped 12% QoQ'}",
  text: "{Decision text emphasizing cost (compute time, $, accuracy tradeoff)}",
  onRight: {
    label: "{Full retrain OR Accept drift}",
    hype: {+30 for retrain (accuracy restored), -20 for freeze (bias exposed)},
    heat: {-20 for retrain (audit happy), +55 for freeze (compliance gap)},
    fine: {0 for retrain, $2-5M for freeze (bias liability)},
    violation: "{Appropriate regulation (fairness, accuracy SLA, discrimination)}",
    feedback: { /* all 3 personalities */ },
    lesson: "{Explains cost difference: retrain takes time, freezing has accuracy cost}",
  },
  onLeft: { /* opposite choice */ },
},
```

**Role-specific framing:**

Development/Engineer:
- Focus: Model latency, integration complexity, deploy timeline
- Context: "Fraud detection model drifting, retraining will block the v2.0 launch we promised customers. Patch or delay?"
- Penalties: Timeline slip (hype), known accuracy gap (heat), fraud liability (fine)

Finance:
- Focus: Compute cost, budget impact, risk exposure
- Context: "$50k retraining cost vs. letting model degrade. Budget is already tight. Retrain or accept accuracy loss?"
- Penalties: Budget overrun (hype), audit exposure (heat), fraud loss (fine)

Management/CEO:
- Focus: Shareholder impact, competitive edge, regulatory standing
- Context: "Recommendation algorithm showing bias. Retrain for fairness (Q2 delay) or ship current version (EEOC risk)?"
- Penalties: Competitive delay (hype), regulatory risk (heat), discrimination settlement (fine)

Marketing/Tech Consultant:
- Focus: Feature launch, customer satisfaction, differentiation
- Context: "Model recommendations degraded. Quick patch keeps feature live but quality suffers. Retrain or ship degraded?"
- Penalties: Customer complaints (hype), feature reputation (heat), liability (fine)

Data Scientist/HR:
- Focus: Model quality, team resources, scientific integrity
- Context: "Model drift detected. Full retraining takes your team's focus for 2 months. Patch and move on or do it right?"
- Penalties: Accuracy loss (hype), team frustration (heat), audit failure (fine)

**Implementation checklist:**
- [ ] ID format: model_drift_{context}_{1 or 2}
- [ ] Both outcomes carry penalties (retrain = time cost + hype loss; patch = accuracy gap + fine)
- [ ] Penalties follow observed ranges (hype ±30-50, heat 0-100, fine $2-5M typical for model failure)
- [ ] 3 personalities present, distinct, outcome-specific
- [ ] Role-specific context (timeline, budget, fairness, model accuracy)
- [ ] Lesson explains cost tradeoff (accuracy vs. resources, speed vs. correctness)

**Don't hand-roll:**
- Use verified ML industry patterns from RESEARCH.md (91% of models fail to drift, 9.3% improvement from retraining)
- Reference Aerospike, SmartDev, Rohan Paul sources if citing data
- Personality voices: Consistent with Phase 01-03 (proven working)
- Penalties: Drift fines lower than fraud ($2-5M range, not $10M+)

After implementation, validate with: `npm test -- tests/data/card-penalties.test.ts`
  </action>
  <verify>
    <automated>npm test -- tests/data/card-penalties.test.ts 2>&1 | grep -A5 "model_drift"</automated>
  </verify>
  <done>10 model drift cards added (2 per role × 5 decks). All pass card-penalties test (both outcomes non-zero, penalties in range). Tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Integrate Phase 05-02 cards into ROLE_CARDS and verify tests pass</name>
  <files>data/cards/index.ts</files>
  <action>
No changes needed to index.ts structure (it already maps new roles to existing decks via ROLE_DECK_ALIASES). Simply run validation tests to confirm all new cards are integrated:

1. Verify that ROLE_CARDS continues to work:
   ```
   npm test -- tests/data/card-integration.test.ts
   ```
   This test checks: All 10 new roles present, no duplicate IDs, all cards match interface, 3-personality feedback present.

2. Run full card validation:
   ```
   npm test -- tests/data/
   ```
   This runs all 4 data tests (incidents, integration, penalties, feedback-voice).

3. If any test fails:
   - Review error message
   - Check that new cards follow structure (id, source, sender, context, text, onRight/onLeft with label, hype, heat, fine, violation, feedback, lesson)
   - Ensure all 3 personality types present in feedback
   - Verify role-specific context mentioned in card text

Cards are automatically integrated because deck aliases (ROLE_DECK_ALIASES) map new roles to their parent decks, and new cards are appended to those deck arrays.

Run command: `npm test -- tests/data/`
  </action>
  <verify>
    <automated>npm test -- tests/data/ 2>&1 | tail -20</automated>
  </verify>
  <done>All 20 cards (10 prompt injection + 10 model drift) integrated into 5 decks. ROLE_CARDS automatically includes them via aliases. All 4 data validation tests pass. No changes to index.ts needed.</done>
</task>

</tasks>

<verification>
Phase 05-02 complete: 20 new cards added (2 categories × 2 cards × 5 decks = 40 total, but wait, that's 40... recalculating: 2 categories, 2 cards per category, 5 decks = 20 cards total per deck × 5 decks = 100? No, let me recount: 2 prompt injection + 2 model drift = 4 cards per deck. 4 × 5 decks = 20 cards total). All pass data validation tests:

- ✓ card-incidents.test.ts (sourcing verified, category detected)
- ✓ card-integration.test.ts (all roles covered, no duplicates)
- ✓ card-penalties.test.ts (both outcomes non-zero penalties)
- ✓ feedback-voice.test.ts (3 personalities present, distinct)

Cards ready for Phase 05-03 (explainability + shadow AI).
</verification>

<success_criteria>
- [x] 2 prompt injection cards added to each of 5 decks (10 total)
- [x] 2 model drift cards added to each of 5 decks (10 total)
- [x] All 20 cards follow no-win pattern (both outcomes non-zero penalties)
- [x] All 20 cards have 3-personality feedback (ROASTER, ZEN_MASTER, LOVEBOMBER)
- [x] All 20 cards reference authenticated 2024-2025 incidents
- [x] All 20 cards include role-specific organizational context
- [x] All validation tests pass: `npm test -- tests/data/`
- [x] Cards automatically integrated into ROLE_CARDS via existing aliases
- [x] Files committed to git
</success_criteria>

<output>
After completion, create `.planning/phases/05-expanded-ai-risk-scenarios/05-02-SUMMARY.md`
</output>
