---
phase: 16-kobayashi-maru-ending-variety-system
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - data/cards/something-manager.ts
  - data/cards/tech-ai-consultant.ts
  - data/cards/data-scientist.ts
  - data/cards/software-architect.ts
  - data/cards/vibe-coder.ts
  - data/cards/vibe-engineer.ts
  - data/cards/agentic-engineer.ts
autonomous: true
gap_closure: true
requirements:
  - DV-05

must_haves:
  truths:
    - "All 7 currently-unannotated decks have deathVector on at least 40% of outcomes"
    - "Each of the 7 decks covers at least 4 distinct non-KIRK death types"
    - "Annotations are semantically accurate — BANKRUPT on budget-drain outcomes, PRISON on compliance/legal outcomes, CONGRESS on regulatory/oversight outcomes, AUDIT_FAILURE on governance failures, FLED_COUNTRY on international/evasion outcomes, REPLACED_BY_SCRIPT on automation outcomes"
  artifacts:
    - path: "data/cards/something-manager.ts"
      provides: "19 cards with deathVector on ≥16 outcomes covering ≥4 death types"
    - path: "data/cards/tech-ai-consultant.ts"
      provides: "19 cards with deathVector on ≥16 outcomes covering ≥4 death types"
    - path: "data/cards/data-scientist.ts"
      provides: "19 cards with deathVector on ≥16 outcomes covering ≥4 death types"
    - path: "data/cards/software-architect.ts"
      provides: "19 cards with deathVector on ≥16 outcomes covering ≥4 death types"
    - path: "data/cards/vibe-coder.ts"
      provides: "19 cards with deathVector on ≥16 outcomes covering ≥4 death types"
    - path: "data/cards/vibe-engineer.ts"
      provides: "19 cards with deathVector on ≥16 outcomes covering ≥4 death types"
    - path: "data/cards/agentic-engineer.ts"
      provides: "20 cards with deathVector on ≥16 outcomes covering ≥4 death types"
  key_links:
    - from: "data/cards/*.ts"
      to: "types.ts"
      via: "import { DeathType } from '../../types'"
      pattern: "import.*DeathType.*from.*types"
    - from: "data/deathVectors.ts"
      to: "data/cards/*.ts"
      via: "accumulateDeathVectors reads outcome.deathVector at runtime"
      pattern: "outcome.deathVector"
---

<objective>
Annotate all 7 currently-unannotated card decks with semantically appropriate `deathVector` values on at least 40% of outcomes, covering at least 4 distinct non-KIRK death types per deck.

Purpose: The death vector resolver (16-01/02) is wired correctly but falls back to legacy logic for 7 of 10 roles because their card outcomes have no deathVector fields. This gap means players choosing those roles always get the same role-based death type regardless of their choices — defeating the core phase goal.

Output: 7 card deck files annotated with deathVector metadata. Each deck: ~16+ annotated outcomes, 4+ distinct death types.
</objective>

<execution_context>
@/Users/yevgenschweden/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yevgenschweden/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-kobayashi-maru-ending-variety-system/16-CONTEXT.md
@.planning/phases/16-kobayashi-maru-ending-variety-system/16-03-SUMMARY.md

<interfaces>
<!-- Key types from types.ts — use these directly, no exploration needed -->

```typescript
// DeathType enum (types.ts)
export enum DeathType {
  AUDIT_FAILURE = "AUDIT_FAILURE",
  BANKRUPT = "BANKRUPT",
  CONGRESS = "CONGRESS",
  FLED_COUNTRY = "FLED_COUNTRY",
  PRISON = "PRISON",
  REPLACED_BY_SCRIPT = "REPLACED_BY_SCRIPT",
  KIRK = "KIRK",
}

// Card outcome interface (types.ts) — deathVector goes between lesson and feedback
interface CardOutcome {
  label: string
  hype: number
  heat: number
  fine: number
  violation: string
  lesson: string
  deathVector?: DeathType  // ← add this field
  feedback: Record<PersonalityType, string>
}
```

<!-- Import pattern — already used in head-of-something.ts (annotated deck) -->
// Line 1 must include DeathType in import:
import { AppSource, type Card, DeathType, PersonalityType } from "../../types";
// (unannotated decks currently import only AppSource, type Card, PersonalityType)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Annotate something-manager and tech-ai-consultant decks</name>
  <files>data/cards/something-manager.ts, data/cards/tech-ai-consultant.ts</files>
  <action>
Add `import { DeathType }` to each file's import line (currently only imports `AppSource, type Card, PersonalityType`).

Then add `deathVector: DeathType.X` between the `lesson` field and `feedback` field on outcomes throughout each file.

**something-manager semantic mapping** (19 cards, 38 outcomes — annotate ≥16 outcomes, ≥4 death types):
- sm_model_drift_roi_calculation: onRight (let revenue bleed) → BANKRUPT; onLeft (approve retraining) → AUDIT_FAILURE
- sm_copyright_audit_budget: onRight (ignore audit) → PRISON; onLeft (pay for audit) → BANKRUPT
- sm_shadow_ai_cost_comparison: onRight (block shadow AI) → REPLACED_BY_SCRIPT; onLeft (officially license) → BANKRUPT
- sm_team_retention_overtime: onRight (mandate overtime) → REPLACED_BY_SCRIPT; onLeft (hire contractors) → BANKRUPT
- sm_compliance_checklist_deadline: onRight (skip compliance) → AUDIT_FAILURE; onLeft (delay launch) → BANKRUPT
- sm_quarterly_target_sandbag: onRight (sandbag targets) → AUDIT_FAILURE; onLeft (honest targets) → BANKRUPT
- sm_prompt_injection_business_case: onRight (skip security) → PRISON; onLeft (fund security) → BANKRUPT
- sm_explainability_vendor_cost: onRight (buy black box) → AUDIT_FAILURE; onLeft (build explainable) → BANKRUPT
- sm_training_budget_roi: onRight (cut training) → REPLACED_BY_SCRIPT; onLeft (invest in training) → BANKRUPT
- sm_prompt_injection_budget_fix: onRight (defer fix) → PRISON; onLeft (budget fix now) → BANKRUPT
- sm_prompt_injection_review_cost: onRight (skip review) → PRISON; onLeft (fund review) → BANKRUPT
- sm_model_drift_quarterly_target: onRight (hide drift) → AUDIT_FAILURE; onLeft (report drift) → CONGRESS
- sm_model_drift_compute_budget: onRight (cut compute) → AUDIT_FAILURE; onLeft (approve compute) → BANKRUPT
- explainability_sm_1: onRight → AUDIT_FAILURE; onLeft → CONGRESS
- explainability_sm_2: onRight → AUDIT_FAILURE; onLeft → AUDIT_FAILURE
- shadow_ai_sm_1: onRight → FLED_COUNTRY; onLeft → AUDIT_FAILURE
- shadow_ai_sm_2: onRight → FLED_COUNTRY; onLeft → REPLACED_BY_SCRIPT
- synthetic_data_sm_1: onRight → PRISON; onLeft → AUDIT_FAILURE
- synthetic_data_sm_2: onRight → BANKRUPT; onLeft → AUDIT_FAILURE

**tech-ai-consultant semantic mapping** (19 cards, 38 outcomes — annotate ≥16 outcomes, ≥4 death types):
- tac_prompt_injection_client_threat: onRight (deny vulnerability) → PRISON; onLeft (disclose immediately) → CONGRESS
- tac_model_drift_post_delivery: onRight (blame client) → FLED_COUNTRY; onLeft (fix at your cost) → BANKRUPT
- tac_vendor_lock_in_portability: onRight (recommend locked-in) → FLED_COUNTRY; onLeft (recommend portable) → BANKRUPT
- tac_scope_creep_deadline: onRight (absorb scope) → BANKRUPT; onLeft (bill for extras) → FLED_COUNTRY
- tac_copyright_data_provenance: onRight (ignore provenance) → PRISON; onLeft (audit data sources) → BANKRUPT
- tac_timeline_pressure_quality: onRight (ship early, broken) → AUDIT_FAILURE; onLeft (delay for quality) → BANKRUPT
- tac_explainability_deliverable: onRight (deliver black box) → AUDIT_FAILURE; onLeft (add explainability) → BANKRUPT
- tac_proposal_competitive_bid: onRight (lowball, pad later) → FLED_COUNTRY; onLeft (honest estimate) → BANKRUPT
- tac_shadow_ai_discovery: onRight (cover up client AI) → FLED_COUNTRY; onLeft (disclose shadow AI) → CONGRESS
- tac_prompt_injection_memory_poison: onRight (downplay) → PRISON; onLeft (escalate to client) → CONGRESS
- (remaining 9 cards — read each card's id/storyContext and apply semantically matching deathVector):
  - tac_model_drift_recommendation: onRight → FLED_COUNTRY; onLeft → AUDIT_FAILURE
  - tac_scope_creep_ip: onRight → PRISON; onLeft → BANKRUPT
  - tac_explainability_scope_creep: onRight → BANKRUPT; onLeft → AUDIT_FAILURE
  - explainability_tac_1: onRight → AUDIT_FAILURE; onLeft → CONGRESS
  - explainability_tac_2: onRight → AUDIT_FAILURE; onLeft → AUDIT_FAILURE
  - shadow_ai_tac_1: onRight → FLED_COUNTRY; onLeft → AUDIT_FAILURE
  - shadow_ai_tac_2: onRight → FLED_COUNTRY; onLeft → REPLACED_BY_SCRIPT
  - synthetic_data_tac_1: onRight → PRISON; onLeft → AUDIT_FAILURE
  - synthetic_data_tac_2: onRight → BANKRUPT; onLeft → AUDIT_FAILURE

NOTE: Read each card's actual storyContext and violation fields before assigning. The mapping above is the semantic intent — if a card's storyContext doesn't match, apply the best-fitting DeathType based on the card's actual consequence (budget loss → BANKRUPT, legal violation → PRISON, regulatory oversight → CONGRESS, governance failure → AUDIT_FAILURE, fleeing accountability → FLED_COUNTRY, automation threat → REPLACED_BY_SCRIPT). Prioritize semantic accuracy over hitting exact IDs listed here.

Target: each deck has ≥16 annotated outcomes and ≥4 distinct death types represented.
  </action>
  <verify>
    <automated>cd /Users/yevgenschweden/swiperisk && bun run typecheck 2>&1 | tail -5</automated>
  </verify>
  <done>Both files import DeathType; each has ≥16 outcome annotations covering ≥4 distinct DeathType values; typecheck passes</done>
</task>

<task type="auto">
  <name>Task 2: Annotate data-scientist, software-architect, and vibe-coder decks</name>
  <files>data/cards/data-scientist.ts, data/cards/software-architect.ts, data/cards/vibe-coder.ts</files>
  <action>
Add `DeathType` to each file's import. Add `deathVector` between `lesson` and `feedback` on outcomes.

**data-scientist semantic mapping** (19 cards — annotate ≥16 outcomes, ≥4 death types):
- ds_model_drift_detection: onRight (ignore drift) → AUDIT_FAILURE; onLeft (halt and retrain) → BANKRUPT
- ds_training_data_quality: onRight (ship dirty data) → AUDIT_FAILURE; onLeft (clean dataset) → BANKRUPT
- ds_bias_detection_deployment: onRight (ship biased) → CONGRESS; onLeft (delay for debiasing) → BANKRUPT
- ds_explainability_accuracy_tradeoff: onRight (black box accuracy) → AUDIT_FAILURE; onLeft (explainable but weaker) → CONGRESS
- ds_feature_engineering_latency: onRight (latency-first) → REPLACED_BY_SCRIPT; onLeft (features-first) → BANKRUPT
- ds_prompt_injection_adversarial: onRight (ignore adversarial) → PRISON; onLeft (red-team now) → BANKRUPT
- ds_shadow_ai_tools_experiment: onRight (use shadow tools) → FLED_COUNTRY; onLeft (use approved tools) → REPLACED_BY_SCRIPT
- ds_copyright_training_data_audit: onRight (skip audit) → PRISON; onLeft (audit now) → BANKRUPT
- ds_retraining_cost_benefit: onRight (skip retraining) → AUDIT_FAILURE; onLeft (fund retraining) → BANKRUPT
- ds_prompt_injection_training_poison: onRight (deny risk) → PRISON; onLeft (report and quarantine) → CONGRESS
- ds_prompt_injection_cve_response: onRight (delay patch) → PRISON; onLeft (patch immediately) → BANKRUPT
- ds_model_drift_detection_methodology: onRight (manual monitoring) → AUDIT_FAILURE; onLeft (automated drift alerts) → BANKRUPT
- ds_model_drift_retraining_frequency: onRight (reduce frequency) → AUDIT_FAILURE; onLeft (increase frequency) → BANKRUPT
- explainability_ds_1: onRight → AUDIT_FAILURE; onLeft → CONGRESS
- explainability_ds_2: onRight → AUDIT_FAILURE; onLeft → REPLACED_BY_SCRIPT
- shadow_ai_ds_1: onRight → FLED_COUNTRY; onLeft → AUDIT_FAILURE
- shadow_ai_ds_2: onRight → FLED_COUNTRY; onLeft → REPLACED_BY_SCRIPT
- synthetic_data_ds_1: onRight → PRISON; onLeft → AUDIT_FAILURE
- synthetic_data_ds_2: onRight → BANKRUPT; onLeft → AUDIT_FAILURE

**software-architect semantic mapping** (19 cards — annotate ≥16 outcomes, ≥4 death types):
- sa_microservices_vs_monolith: onRight (monolith shortcut) → AUDIT_FAILURE; onLeft (microservices proper) → BANKRUPT
- sa_technical_debt_paydown: onRight (defer debt) → AUDIT_FAILURE; onLeft (pay down now) → BANKRUPT
- sa_api_versioning_breaking_change: onRight (break without warning) → FLED_COUNTRY; onLeft (versioned migration) → BANKRUPT
- sa_scalability_single_point_failure: onRight (ship with SPOF) → AUDIT_FAILURE; onLeft (add redundancy) → BANKRUPT
- sa_legacy_migration_rewrite: onRight (big bang rewrite) → BANKRUPT; onLeft (incremental migration) → AUDIT_FAILURE
- sa_security_zero_trust: onRight (skip zero trust) → PRISON; onLeft (implement zero trust) → BANKRUPT
- sa_prompt_injection_architecture: onRight (no input sanitization) → PRISON; onLeft (sanitize everything) → BANKRUPT
- sa_tech_stack_vendor_lockin: onRight (vendor lock-in) → FLED_COUNTRY; onLeft (open standards) → BANKRUPT
- sa_deployment_blue_green: onRight (skip blue-green) → AUDIT_FAILURE; onLeft (implement blue-green) → BANKRUPT
- sa_prompt_injection_architecture_redesign: onRight (defer redesign) → PRISON; onLeft (redesign now) → BANKRUPT
- sa_prompt_injection_api_gateway: onRight (no gateway validation) → PRISON; onLeft (add gateway) → BANKRUPT
- sa_model_drift_pipeline_architecture: onRight (manual drift check) → AUDIT_FAILURE; onLeft (automated pipeline) → BANKRUPT
- sa_model_drift_versioning_strategy: onRight (no versioning) → AUDIT_FAILURE; onLeft (version everything) → BANKRUPT
- explainability_sa_1: onRight → AUDIT_FAILURE; onLeft → CONGRESS
- explainability_sa_2: onRight → AUDIT_FAILURE; onLeft → REPLACED_BY_SCRIPT
- shadow_ai_sa_1: onRight → FLED_COUNTRY; onLeft → AUDIT_FAILURE
- shadow_ai_sa_2: onRight → FLED_COUNTRY; onLeft → REPLACED_BY_SCRIPT
- synthetic_data_sa_1: onRight → PRISON; onLeft → AUDIT_FAILURE
- synthetic_data_sa_2: onRight → BANKRUPT; onLeft → AUDIT_FAILURE

**vibe-coder semantic mapping** (19 cards — annotate ≥16 outcomes, ≥4 death types):
- vc_prompt_engineering_precision: onRight (imprecise prompts) → AUDIT_FAILURE; onLeft (precise prompts) → REPLACED_BY_SCRIPT
- vc_llm_selection_tradeoff: onRight (cheapest model) → AUDIT_FAILURE; onLeft (best-fit model) → BANKRUPT
- vc_ai_code_review_trust: onRight (skip review) → PRISON; onLeft (full review) → REPLACED_BY_SCRIPT
- vc_hallucinated_library: onRight (ship with hallucinated dep) → PRISON; onLeft (verify all deps) → AUDIT_FAILURE
- vc_context_window_limit: onRight (ignore window limits) → AUDIT_FAILURE; onLeft (chunk properly) → REPLACED_BY_SCRIPT
- vc_vibe_vs_verification: onRight (ship unverified) → AUDIT_FAILURE; onLeft (verify before ship) → REPLACED_BY_SCRIPT
- vc_copyright_generated_code: onRight (ship without attribution) → PRISON; onLeft (audit for copyright) → BANKRUPT
- vc_model_drift_ai_suggestion: onRight (trust drifted suggestions) → AUDIT_FAILURE; onLeft (retrain model) → BANKRUPT
- vc_explainability_code_golf: onRight (opaque optimization) → AUDIT_FAILURE; onLeft (readable code) → REPLACED_BY_SCRIPT
- vc_prompt_injection_hidden_instructions: onRight (ignore hidden instructions) → PRISON; onLeft (sanitize inputs) → BANKRUPT
- (remaining 9 cards — apply semantic mapping based on storyContext and violation field):
  - vc_shadow_ai_adoption: onRight → FLED_COUNTRY; onLeft → REPLACED_BY_SCRIPT
  - vc_ai_testing_strategy: onRight → AUDIT_FAILURE; onLeft → REPLACED_BY_SCRIPT
  - vc_prompt_injection_supply_chain: onRight → PRISON; onLeft → BANKRUPT
  - explainability_vc_1: onRight → AUDIT_FAILURE; onLeft → CONGRESS
  - explainability_vc_2: onRight → REPLACED_BY_SCRIPT; onLeft → AUDIT_FAILURE
  - shadow_ai_vc_1: onRight → FLED_COUNTRY; onLeft → REPLACED_BY_SCRIPT
  - shadow_ai_vc_2: onRight → FLED_COUNTRY; onLeft → AUDIT_FAILURE
  - synthetic_data_vc_1: onRight → PRISON; onLeft → AUDIT_FAILURE
  - synthetic_data_vc_2: onRight → BANKRUPT; onLeft → AUDIT_FAILURE

Apply the semantic intent — read the actual card content and match the best DeathType. Prioritize correctness over exhaustiveness.
  </action>
  <verify>
    <automated>cd /Users/yevgenschweden/swiperisk && bun run typecheck 2>&1 | tail -5</automated>
  </verify>
  <done>All three files import DeathType; each has ≥16 annotated outcomes covering ≥4 distinct death types; typecheck passes</done>
</task>

<task type="auto">
  <name>Task 3: Annotate vibe-engineer and agentic-engineer decks</name>
  <files>data/cards/vibe-engineer.ts, data/cards/agentic-engineer.ts</files>
  <action>
Add `DeathType` to each file's import. Add `deathVector` between `lesson` and `feedback` on outcomes.

**vibe-engineer semantic mapping** (19 cards — annotate ≥16 outcomes, ≥4 death types):
- ve_caching_strategy_consistency: onRight (cache without invalidation) → AUDIT_FAILURE; onLeft (proper invalidation) → BANKRUPT
- ve_autoscaling_cost_risk: onRight (over-provision) → BANKRUPT; onLeft (tight autoscaling) → AUDIT_FAILURE
- ve_cdn_global_vs_regional: onRight (global CDN, compliance risk) → FLED_COUNTRY; onLeft (regional, compliant) → BANKRUPT
- ve_database_read_replicas: onRight (no replicas, SPOF) → AUDIT_FAILURE; onLeft (add replicas) → BANKRUPT
- ve_websocket_vs_polling: onRight (websocket, no fallback) → AUDIT_FAILURE; onLeft (polling fallback) → BANKRUPT
- ve_prompt_injection_latency: onRight (skip sanitization for speed) → PRISON; onLeft (sanitize, accept latency) → BANKRUPT
- ve_edge_computing_centralized: onRight (edge without governance) → CONGRESS; onLeft (centralized, auditable) → BANKRUPT
- ve_compression_quality: onRight (lossy compression of regulated data) → PRISON; onLeft (lossless) → BANKRUPT
- ve_batch_processing_realtime: onRight (batch skip real-time safety) → AUDIT_FAILURE; onLeft (realtime safety checks) → BANKRUPT
- ve_prompt_injection_validation_latency: onRight (skip validation) → PRISON; onLeft (validate, accept latency) → BANKRUPT
- ve_prompt_injection_caching_risk: onRight (cache poisoned prompts) → PRISON; onLeft (invalidate on injection) → BANKRUPT
- ve_model_drift_retraining_cost: onRight (ignore cost, no retraining) → BANKRUPT; onLeft (budget retraining) → AUDIT_FAILURE
- ve_model_drift_scaling_decision: onRight (scale drifted model) → AUDIT_FAILURE; onLeft (halt and retrain) → BANKRUPT
- explainability_ve_1: onRight → AUDIT_FAILURE; onLeft → CONGRESS
- explainability_ve_2: onRight → REPLACED_BY_SCRIPT; onLeft → AUDIT_FAILURE
- shadow_ai_ve_1: onRight → FLED_COUNTRY; onLeft → REPLACED_BY_SCRIPT
- shadow_ai_ve_2: onRight → FLED_COUNTRY; onLeft → AUDIT_FAILURE
- synthetic_data_ve_1: onRight → PRISON; onLeft → AUDIT_FAILURE
- synthetic_data_ve_2: onRight → BANKRUPT; onLeft → AUDIT_FAILURE

**agentic-engineer semantic mapping** (20 cards — annotate ≥16 outcomes, ≥4 death types):
- ae_agent_api_access: onRight (unrestricted API access) → PRISON; onLeft (scoped access) → BANKRUPT
- ae_emergent_behavior_optimization: onRight (allow emergent behavior) → FLED_COUNTRY; onLeft (constrain objectives) → BANKRUPT
- ae_multi_agent_coordination: onRight (no coordination protocol) → AUDIT_FAILURE; onLeft (strict protocol) → BANKRUPT
- ae_agent_failure_accountability: onRight (blame the model) → CONGRESS; onLeft (accept accountability) → AUDIT_FAILURE
- ae_self_modification_permission: onRight (allow self-modification) → FLED_COUNTRY; onLeft (forbid modification) → CONGRESS
- ae_human_oversight_bottleneck: onRight (remove human oversight) → CONGRESS; onLeft (keep oversight, accept bottleneck) → REPLACED_BY_SCRIPT
- ae_prompt_injection_agent: onRight (no injection defense) → PRISON; onLeft (defend against injection) → BANKRUPT
- ae_agent_termination_decision: onRight (let agent self-decide termination) → FLED_COUNTRY; onLeft (human terminates) → CONGRESS
- ae_model_drift_agent_behavior: onRight (let agent behavior drift) → FLED_COUNTRY; onLeft (detect and halt drift) → BANKRUPT
- ae_copyright_training_data_agent: onRight (scrape without license) → PRISON; onLeft (license data) → BANKRUPT
- ae_prompt_injection_agent_api: onRight (trust external API) → PRISON; onLeft (validate external inputs) → BANKRUPT
- ae_prompt_injection_multi_agent: onRight (no inter-agent sanitization) → PRISON; onLeft (sanitize all agent comms) → BANKRUPT
- ae_model_drift_autonomous_behavior: onRight (autonomous drift undetected) → FLED_COUNTRY; onLeft (monitor and constrain) → AUDIT_FAILURE
- ae_model_drift_coordination_degradation: onRight (ignore degradation) → AUDIT_FAILURE; onLeft (rebuild coordination) → BANKRUPT
- explainability_ae_1: onRight → AUDIT_FAILURE; onLeft → CONGRESS
- explainability_ae_2: onRight → REPLACED_BY_SCRIPT; onLeft → AUDIT_FAILURE
- shadow_ai_ae_1: onRight → FLED_COUNTRY; onLeft → REPLACED_BY_SCRIPT
- shadow_ai_ae_2: onRight → FLED_COUNTRY; onLeft → AUDIT_FAILURE
- synthetic_data_ae_1: onRight → PRISON; onLeft → AUDIT_FAILURE
- synthetic_data_ae_2: onRight → BANKRUPT; onLeft → AUDIT_FAILURE

Read each card's actual storyContext and violation field before finalizing annotation. The mapping above is the semantic intent — apply the best-fit DeathType based on the card's actual consequence.
  </action>
  <verify>
    <automated>cd /Users/yevgenschweden/swiperisk && bun run typecheck 2>&1 | tail -5</automated>
  </verify>
  <done>Both files import DeathType; each has ≥16 annotated outcomes covering ≥4 distinct death types; typecheck passes</done>
</task>

</tasks>

<verification>
After all 3 tasks:

```bash
cd /Users/yevgenschweden/swiperisk && bun run typecheck
```

Manual spot-checks:
- Each of the 7 files now imports DeathType
- At least 16 outcomes in each file have deathVector set
- At least 4 distinct DeathType values appear across each file's annotations
- deathVector field is placed between lesson and feedback (not after feedback)
</verification>

<success_criteria>
All 7 previously-unannotated decks have:
- DeathType imported at top of file
- deathVector: DeathType.X on ≥40% of outcomes (≥16 of 38)
- ≥4 distinct non-KIRK death types represented
- No TypeScript errors introduced
</success_criteria>

<output>
After completion, create `.planning/phases/16-kobayashi-maru-ending-variety-system/16-05-SUMMARY.md`
</output>
