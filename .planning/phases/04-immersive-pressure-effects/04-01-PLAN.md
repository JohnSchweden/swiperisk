---
phase: 04-immersive-pressure-effects
plan: 01
type: TDD
wave: 1
depends_on: []
files_modified:
  - types.ts
  - data/pressureScenarios.ts
  - data/index.ts
  - hooks/useIncidentPressure.ts
  - hooks/index.ts
  - components/game/PressureCueController.tsx
  - components/game/index.ts
  - App.tsx
autonomous: true

must_haves:
  truths:
    - "Only incidents explicitly marked urgent start a countdown during play."
    - "Pressure state is derived from live game metrics and the active card instead of hardcoded per-screen logic."
    - "Once a player commits to a choice or a timer expires, the flow advances without any undo path."
  artifacts:
    - path: "data/pressureScenarios.ts"
      provides: "Urgency, countdown, and team-impact metadata keyed by card and outcome"
      min_lines: 40
    - path: "hooks/useIncidentPressure.ts"
      provides: "Derived pressure state for timer, escalation, and critical moments"
      exports: ["useIncidentPressure"]
    - path: "App.tsx"
      provides: "Top-level orchestration for countdown expiry, final-choice lock, and pressure cue wiring"
      min_lines: 320
    - path: "components/game/PressureCueController.tsx"
      provides: "Mounted controller entrypoint for stress cues"
      exports: ["PressureCueController"]
  key_links:
    - from: "App.tsx"
      to: "hooks/useIncidentPressure.ts"
      via: "derived pressure hook invocation"
      pattern: "useIncidentPressure"
    - from: "App.tsx"
      to: "data/pressureScenarios.ts"
      via: "card metadata lookup before rendering overlay and timer"
      pattern: "pressureScenarios|PRESSURE_SCENARIOS"
    - from: "App.tsx"
      to: "components/game/PressureCueController.tsx"
      via: "mounted controller props"
      pattern: "PressureCueController"
---

<objective>
Create the pressure metadata and orchestration layer that later plans can render and sonify.

Purpose: Phase 04 needs a single source of truth for urgency, countdown behavior, team-impact text, and no-undo flow so the visual and cue plans do not re-implement game logic in parallel.
Output: Pressure metadata map, derived pressure hook, top-level timer/finality orchestration, and a mounted cue controller entrypoint.
</objective>

<execution_context>
@/Users/yevgenschweden/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/yevgenschweden/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@types.ts
@data/cards/index.ts
@hooks/useCountdown.ts
@hooks/index.ts
@components/game/index.ts
@App.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add explicit pressure metadata for urgent cards and team-impact outcomes</name>
  <files>types.ts, data/pressureScenarios.ts, data/index.ts</files>
  <action>Extend the shared types with phase-specific pressure metadata that can be referenced without mutating every card file. Add a new data module keyed by existing card IDs and outcome directions that declares: which incidents are urgent, countdown length in seconds, which branch should auto-resolve on timeout, whether the moment is critical enough for haptics/audio escalation, and optional team-impact copy for selected outcomes. Export the new metadata through the existing data barrel. Keep this additive and keyed to current card IDs only; do not rewrite the card decks or invent a persistence system. Include enough annotated incidents to cover both urgent countdown behavior and team-impact messaging across more than one role.</action>
  <verify>bun run typecheck</verify>
  <done>Shared types compile, pressure metadata is exported from the data layer, and there is a concrete mapping for urgent incidents plus team-impact outcome text on selected cards.</done>
</task>

<task type="auto">
  <name>Task 2: Wire top-level pressure state, timer expiry, and no-undo orchestration</name>
  <files>hooks/useIncidentPressure.ts, hooks/index.ts, components/game/PressureCueController.tsx, components/game/index.ts, App.tsx</files>
  <action>Create a dedicated pressure hook that derives the active incident metadata, countdown state, escalation flags, and critical-moment booleans from the current card plus live heat/budget values. In App, use that hook to: expose timer props to the play screen, auto-resolve urgent incidents when the countdown expires, prevent duplicate choice handling while a choice is resolving, and carry team-impact metadata into the feedback overlay payload. Mount a new PressureCueController component from App with the derived pressure props, even if later plans flesh out its internals. Treat timer expiry as a final decision path, not a reversible warning; the player should move forward without an undo affordance.</action>
  <verify>bun run typecheck && bunx playwright test tests/swipe-interactions.spec.ts</verify>
  <done>App owns one pressure/timer orchestration path, urgent cards can time out into a real outcome, the same choice cannot be processed twice, and the cue controller is mounted with live pressure props.</done>
</task>

</tasks>

<verification>
Run:
- `bun run typecheck`
- `bunx playwright test tests/swipe-interactions.spec.ts`

Then confirm in code review that:
- Only configured urgent incidents receive countdown behavior.
- Timer expiry resolves to a real left/right outcome without adding undo UI.
- Feedback overlay payload now has a path for optional team-impact consequences.
</verification>

<success_criteria>
- [ ] Pressure metadata exists outside the raw card decks and is exported for runtime use.
- [ ] Urgent incidents have explicit countdown length and timeout resolution data.
- [ ] App derives pressure state from live card + metrics rather than hardcoded stage checks.
- [ ] Timer expiry advances gameplay through the normal consequence flow.
- [ ] Choice handling is final for the incident once selected or timed out.
- [ ] `PressureCueController` is mounted and receives live pressure props for later plans.
</success_criteria>

<output>
After completion, create `.planning/phases/04-immersive-pressure-effects/04-01-SUMMARY.md`
</output>
