---
phase: 01-voice-files
plan: 04
type: execute
wave: 4
depends_on: ["01-03"]
files_modified: []
autonomous: true

must_haves:
  truths:
    - "Voice playback system can load and play WAV files"
    - "Playback system handles errors gracefully with in-game narrative"
  artifacts:
    - path: "src/lib/voicePlayback.ts"
      provides: "Voice playback utility using Web Audio API"
      exports: ["playVoice", "stopVoice", "isPlaying"]
    - path: "src/lib/voicePaths.ts"
      provides: "Voice file path mapping by personality and trigger"
      exports: ["VOICE_PATHS", "getVoicePath"]
  key_links:
    - from: "src/lib/voicePlayback.ts"
      to: "public/audio/voices/"
      via: "Web Audio API fetch + decodeAudioData"
      pattern: "fetch.*audio/voices"
---

<objective>
Create voice playback system for pre-recorded audio files using Web Audio API.

Purpose: Build the infrastructure to load and play WAV files based on personality and trigger. Must handle errors gracefully with in-game narrative framing (e.g., "V.E.R.A. voice module malfunctioned").

Output: Two files - voicePlayback.ts (playback utility) and voicePaths.ts (path mapping)
</objective>

<execution_context>
@/Users/yevgenschweden/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/yevgenschweden/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-voice-files/01-CONTEXT.md
@.planning/ROADMAP.md

# Check existing audio handling
@src/services/geminiService.ts
</context>

<tasks>

<task type="auto">
<name>Task 1: Create voice path mapping utility</name>
<files>src/lib/voicePaths.ts</files>
<action>
Create `src/lib/voicePaths.ts` with:

1. Define voice path mapping type:
```typescript
type VoiceTrigger = 'onboarding' | 'victory' | 'failure' | 'feedback_paste' | 'feedback_debug' | 'feedback_install' | 'feedback_ignore';
type Personality = 'roaster' | 'zen' | 'lovebomber';
```

2. Create VOICE_PATHS constant mapping all 13 files:
```typescript
export const VOICE_PATHS: Record<Personality, Record<VoiceTrigger, string>> = {
  roaster: {
    onboarding: '/audio/voices/roaster/onboarding.wav',
    victory: '/audio/voices/roaster/victory.wav',
    failure: '/audio/voices/roaster/failure.wav',
    feedback_paste: '/audio/voices/roaster/feedback_paste.wav',
    feedback_debug: '/audio/voices/roaster/feedback_debug.wav',
    feedback_install: '/audio/voices/roaster/feedback_install.wav',
    feedback_ignore: '/audio/voices/roaster/feedback_ignore.wav',
  },
  zen: {
    onboarding: '/audio/voices/zen/onboarding.wav',
    victory: '/audio/voices/zen/victory.wav',
    failure: '/audio/voices/zen/failure.wav',
  },
  lovebomber: {
    onboarding: '/audio/voices/lovebomber/onboarding.wav',
    victory: '/audio/voices/lovebomber/victory.wav',
    failure: '/audio/voices/lovebomber/failure.wav',
  },
};
```

3. Create helper function:
```typescript
export function getVoicePath(personality: Personality, trigger: VoiceTrigger): string {
  return VOICE_PATHS[personality]?.[trigger] ?? '';
}
```

4. Export type for use in other modules.
</action>
<verify>
- File created at `src/lib/voicePaths.ts`
- TypeScript compiles without errors
- All 13 voice paths correctly mapped
  </verify>
  <done>
`src/lib/voicePaths.ts` created with complete voice path mapping.
</done>
</task>

<task type="auto">
<name>Task 2: Create voice playback utility</name>
<files>src/lib/voicePlayback.ts</files>
<action>
Create `src/lib/voicePlayback.ts` using Web Audio API:

1. Create AudioContext (singleton pattern - reuse existing if available)

2. Export playVoice function:
```typescript
export async function playVoice(audioPath: string): Promise<void>
```
   - Fetch the WAV file
   - Decode using audioContext.decodeAudioData()
   - Create AudioBufferSourceNode
   - Connect to destination
   - Play

3. Export stopVoice function:
```typescript
export function stopVoice(): void
```
   - Stop any currently playing voice
   - Clear reference

4. Export isPlaying getter:
```typescript
export function isPlaying(): boolean
```
   - Return whether a voice is currently playing

5. **Error handling (per CONTEXT.md):**
   - If audio fails to load/play, log error to console with narrative framing:
     - Roaster: "V.E.R.A. voice module malfunctioned"
     - Zen Master: "The data stream is... disrupted"
     - Lovebomber: "Oopsie! Voice module took a nap"
   - Do NOT throw - game continues silently

6. Use existing pattern from geminiService.ts audio handling if applicable
</action>
<verify>
- File created at `src/lib/voicePlayback.ts`
- TypeScript compiles without errors
- Exports: playVoice, stopVoice, isPlaying functions
  </verify>
  <done>
`src/lib/voicePlayback.ts` created with Web Audio API playback and error handling.
</done>
</task>

<task type="auto">
<name>Task 3: Verify playback system compiles</name>
<files>src/lib/voicePlayback.ts, src/lib/voicePaths.ts</files>
<action>
1. Run TypeScript check on both files
2. Verify no import errors
3. Verify voicePaths is properly imported in voicePlayback
  </action>
<verify>
- TypeScript compiles successfully
- No type errors
  </verify>
  <done>
Voice playback system ready for integration.
</done>
</task>

</tasks>

<verification>
Voice playback utility created with path mapping and error handling.
</verification>

<success_criteria>
- [x] `src/lib/voicePaths.ts` created with all 13 voice paths
- [x] `src/lib/voicePlayback.ts` created with playVoice/stopVoice/isPlaying
- [x] Error handling uses in-game narrative framing
- [x] TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-voice-files/01-SUMMARY.md`
</output>
