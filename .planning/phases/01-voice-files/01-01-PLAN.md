---
phase: 01-voice-files
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
user_setup:
  - service: google-gemini
    why: "Voice generation via Gemini TTS API"
    env_vars:
      - name: GEMINI_API_KEY
        source: "Google AI Studio -> API Key"

must_haves:
  truths:
    - "Roaster onboarding voice file can be generated successfully"
    - "Voice generation pipeline works end-to-end"
  artifacts:
    - path: "public/audio/voices/roaster/onboarding.wav"
      provides: "First voice file for pipeline testing"
      format: "WAV PCM 16-bit 24kHz mono"
  key_links: []
---

<objective>
Generate first voice file to test the Gemini TTS pipeline works correctly.

Purpose: Validate that the voice generation pipeline produces valid WAV files before generating all 13 files. This catches any API issues or format problems early.

Output: One WAV file at `public/audio/voices/roaster/onboarding.wav`
</objective>

<execution_context>
@/Users/yevgenschweden/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/yevgenschweden/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01-voice-files/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate Roaster onboarding voice file</name>
  <files>public/audio/voices/roaster/onboarding.wav</files>
  <action>
1. Create directory: `public/audio/voices/roaster/`
2. Use Gemini TTS API (gemini-2.5-flash-preview-tts) to generate audio for:
   "Oh, look. Another 'Visionary' hired to save the company. Try not to destroy us in the first 5 minutes, yeah?"
3. Output format: WAV (PCM 16-bit, 24kHz, mono)
4. Save to: `public/audio/voices/roaster/onboarding.wav`

Use the Gemini TTS API directly via curl or a simple Node.js script. If there's an existing TTS script in the codebase, use it as a reference.
  </action>
  <verify>
- File exists: `ls -la public/audio/voices/roaster/onboarding.wav`
- File is valid WAV: `file public/audio/voices/roaster/onboarding.wav` shows WAV
- File size > 10KB (has actual audio content)
  </verify>
  <done>
`public/audio/voices/roaster/onboarding.wav` exists and is a valid WAV file with audio content.
</done>
</task>

<task type="auto">
  <name>Task 2: Verify WAV file plays correctly</name>
  <files>public/audio/voices/roaster/onboarding.wav</files>
  <action>
1. Verify the WAV file is valid and playable:
   - Check file header (should be RIFF WAVE format)
   - Confirm it's PCM 16-bit, 24kHz, mono as specified
2. Use a quick validation: `ffplay -autoexit public/audio/voices/roaster/onboarding.wav` if ffmpeg available, or just validate the file structure
  </action>
  <verify>
- File header validated as WAV format
- Audio parameters correct (PCM, 16-bit, 24kHz, mono)
  </verify>
  <done>
WAV file is valid and has correct audio format parameters.
</done>
</task>

</tasks>

<verification>
First voice file generated and validated. Pipeline is ready for bulk generation.
</verification>

<success_criteria>
- [x] One WAV file exists at `public/audio/voices/roaster/onboarding.wav`
- [x] File is valid WAV format (PCM 16-bit, 24kHz, mono)
- [x] File has audio content (> 10KB)
</success_criteria>

<output>
After completion, create `.planning/phases/01-voice-files/01-SUMMARY.md`
</output>
