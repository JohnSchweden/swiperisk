---
phase: 04-immersive-pressure-effects
plan: 03
type: TDD
wave: 2
depends_on:
  - "01"
files_modified:
  - components/game/PressureCueController.tsx
  - components/game/FeedbackOverlay.tsx
  - hooks/usePressureAudio.ts
  - services/pressureAudio.ts
  - tests/immersive-pressure-cues.spec.ts
autonomous: true

must_haves:
  truths:
    - "High-pressure states trigger audible stress cues without flooding the user with overlapping noise."
    - "Critical moments trigger haptic feedback on supported mobile devices."
    - "Outcome overlays can show team-impact consequences and reinforce that the decision is final."
  artifacts:
    - path: "components/game/PressureCueController.tsx"
      provides: "Live pressure cue orchestration for gameplay audio and haptics"
      min_lines: 40
    - path: "services/pressureAudio.ts"
      provides: "Stress audio engine using browser-safe synthesized cues"
      exports: ["createPressureAudioSession"]
    - path: "hooks/usePressureAudio.ts"
      provides: "React lifecycle wrapper around the pressure audio session"
      exports: ["usePressureAudio"]
    - path: "components/game/FeedbackOverlay.tsx"
      provides: "Team-impact and finality messaging in post-choice feedback"
      min_lines: 100
    - path: "tests/immersive-pressure-cues.spec.ts"
      provides: "Coverage for cue controller and finality/team-impact UX"
      min_lines: 35
  key_links:
    - from: "components/game/PressureCueController.tsx"
      to: "hooks/usePressureAudio.ts"
      via: "controller effect hook"
      pattern: "usePressureAudio"
    - from: "hooks/usePressureAudio.ts"
      to: "services/pressureAudio.ts"
      via: "session lifecycle"
      pattern: "createPressureAudioSession"
    - from: "components/game/FeedbackOverlay.tsx"
      to: "App.tsx"
      via: "team-impact and finality props"
      pattern: "teamImpact|final|locked"
---

<objective>
Add the sensory and social consequences layer for Phase 04: heartbeat/alert audio, haptics, and team-impact feedback.

Purpose: Visual stress alone is cosmetic; this plan makes pressure feel embodied and consequential while keeping the cue system deterministic and browser-safe.
Output: Pressure cue controller implementation, synthesized stress audio service, mobile vibration handling, and overlay updates for team impact plus no-undo finality messaging.
</objective>

<execution_context>
@/Users/yevgenschweden/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/yevgenschweden/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@App.tsx
@components/game/PressureCueController.tsx
@components/game/FeedbackOverlay.tsx
@hooks/useVoicePlayback.ts
@services/voicePlayback.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement non-overlapping audio and haptic pressure cues</name>
  <files>components/game/PressureCueController.tsx, hooks/usePressureAudio.ts, services/pressureAudio.ts</files>
  <action>Use the controller shell from plan 01 to drive stress cues from live pressure props. Build a browser-safe audio service with synthesized heartbeat/alert patterns using Web Audio primitives rather than new asset files, then wrap it in a React hook that starts, updates, throttles, and cleans up correctly. Trigger mobile vibration via `navigator.vibrate` only for supported devices and only on critical pressure moments or timeout resolution; treat haptics as enhancement, not required for play. Make sure repeated renders do not stack multiple heartbeat loops, and that cues stop when the user leaves PLAYING or pressure drops.</action>
  <verify>bun run typecheck && bunx playwright test tests/immersive-pressure-cues.spec.ts</verify>
  <done>Gameplay can emit heartbeat/alert cues under high heat, supported mobile browsers receive vibration on critical moments, and the cue system tears down cleanly instead of layering duplicate sessions.</done>
</task>

<task type="auto">
  <name>Task 2: Extend feedback overlay with team-impact consequences and finality messaging</name>
  <files>components/game/FeedbackOverlay.tsx, tests/immersive-pressure-cues.spec.ts</files>
  <action>Render optional team-impact copy from the pressure metadata when an outcome includes it, and add concise copy that reinforces the decision is logged/final before moving to the next incident. Keep the existing governance lesson and fine presentation intact; this is augmentation, not a rewrite. Update or add Playwright coverage so at least one feedback flow asserts the presence of team-impact messaging and confirms there is no undo control exposed in the overlay.</action>
  <verify>bun run typecheck && bunx playwright test tests/immersive-pressure-cues.spec.ts --grep "feedback|team impact|final"</verify>
  <done>Feedback overlays can show team-impact consequences for configured outcomes, the overlay copy reinforces finality, and automated coverage verifies the no-undo/impact behavior.</done>
</task>

</tasks>

<verification>
Run:
- `bun run typecheck`
- `bunx playwright test tests/immersive-pressure-cues.spec.ts`

Then confirm behavior with browser verification:
- High-pressure gameplay starts a heartbeat/alert cue and stops it when pressure resolves.
- Supported mobile emulation triggers vibration on critical moments.
- Feedback overlay shows team-impact text where configured and exposes no undo affordance.
</verification>

<success_criteria>
- [ ] High heat state can trigger heartbeat/alert audio cues.
- [ ] Pressure audio is throttled/managed so duplicate loops do not accumulate.
- [ ] Mobile haptic feedback fires for critical moments when the browser supports vibration.
- [ ] Feedback overlay can display optional team-impact consequences.
- [ ] Overlay copy reinforces finality and does not add undo behavior.
- [ ] Automated coverage exists for cue and overlay behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/04-immersive-pressure-effects/04-03-SUMMARY.md`
</output>
