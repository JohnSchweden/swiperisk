---
phase: 27-mobile-ux-fixes-audio-isolation-button-heights-linkedin-share-copy-link-debrief-text-uiux-sizing-audit
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/game/debrief/DebriefPage3Verdict.tsx
  - src/components/game/debrief/DebriefPage2AuditTrail.tsx
autonomous: true
requirements:
  - DEBRIEF-BTN-01
  - LINKEDIN-01
  - COPY-02
  - EMDASH-01

must_haves:
  truths:
    - "LinkedIn button, Reboot button, and Copy to clipboard button are same height on mobile"
    - "Tapping Share on LinkedIn opens the LinkedIn app (or web URL) in-app, not a new browser page"
    - "Debrief page 2 subtitle reads 'A complete record of your decisions' (no 'governance')"
    - "Debrief page 2 paper trail line reads 'Each one left a paper trail'"
    - "Em dashes replaced in debrief page copy"
  artifacts:
    - path: "src/components/game/debrief/DebriefPage3Verdict.tsx"
      provides: "Debrief page 3 with fixed button heights and LinkedIn in-app share"
    - path: "src/components/game/debrief/DebriefPage2AuditTrail.tsx"
      provides: "Debrief page 2 with updated copy and no em dashes"
  key_links:
    - from: "DebriefPage3Verdict LinkedIn anchor"
      to: "openLinkedInShare() utility"
      via: "onClick handler replacing <a href>"
      pattern: "openLinkedInShare"
    - from: "DebriefPage2AuditTrail subtitle"
      to: "updated string literals"
      via: "JSX text content"
      pattern: "complete record of your"
---

<objective>
Four targeted fixes to debrief page 2 and page 3:

1. **Button heights (mobile)**: LinkedIn anchor + Reboot button must match Copy to clipboard height.
2. **LinkedIn in-app**: Convert LinkedIn `<a target="_self">` to `<button onClick={openLinkedInShare}>` so mobile opens the LinkedIn app, not a new browser page.
3. **Debrief page 2 copy**: Remove "governance" from subtitle; change "Every decision" to "Each one".
4. **Em dashes**: Replace `—` in debrief page copy with cleaner alternatives.

Purpose: Polish the debrief flow so sharing works correctly on mobile and the copy reads cleanly.
Output: Updated DebriefPage3Verdict.tsx and DebriefPage2AuditTrail.tsx.
</objective>

<execution_context>
@/Users/yevgenschweden/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yevgenschweden/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-mobile-ux-fixes-audio-isolation-button-heights-linkedin-share-copy-link-debrief-text-uiux-sizing-audit/27-CONTEXT.md
@.planning/phases/27-mobile-ux-fixes-audio-isolation-button-heights-linkedin-share-copy-link-debrief-text-uiux-sizing-audit/27-RESEARCH.md

@src/components/game/debrief/DebriefPage3Verdict.tsx
@src/components/game/debrief/DebriefPage2AuditTrail.tsx
@src/utils/linkedin-share.ts
@src/lib/buttonStyles.ts

<research_insights>
**From RESEARCH.md — Web Share API Pattern:**
Research confirms Web Share API is SOTA for mobile sharing (opens native share sheet, works with LinkedIn app). However, user constraint requires LinkedIn-specific behavior. Current approach using `openLinkedInShare()` utility is validated — it handles mobile deep-link + web fallback.

**From RESEARCH.md — Mobile-First Touch Targets:**
- iOS Human Interface Guidelines: 44px minimum
- Android Material Design: 48dp minimum  
- WCAG 2.5.5: 44px minimum (Level AAA)
- Current buttons use 40px — needs bump to 44px

**From RESEARCH.md — Anti-Patterns to Avoid:**
- Fixed height buttons (`h-[]`): Use `min-h-[]` for content flexibility
- `target="_blank"` for mobile share: breaks in-app experience
- Em dashes in UI copy: Use commas, colons for cleaner typography

**Standards Applied:**
- Web Share API for mobile native sharing (native, no deps)
- Tailwind `min-h-[44px]` for touch target compliance
- Em dash replacement: `—` → `:` or `,` based on context
</research_insights>

<interfaces>
<!-- Key contracts the executor needs -->

From src/utils/linkedin-share.ts:
```typescript
// Already exists — handles mobile deep-link + web fallback
export function openLinkedInShare(webUrl: string): void;
// webUrl = the LinkedIn share-offsite URL (already constructed in DebriefPage3Verdict)
```

From src/lib/buttonStyles.ts:
```typescript
export const BTN_DEBRIEF_NAV =
  "px-4 py-2 md:px-8 md:py-3 text-base font-bold tracking-wide bg-white text-black hover:bg-cyan-400 hover:text-black transition-all duration-300 min-h-[40px] md:min-h-[48px]";
```

Current LinkedIn element in DebriefPage3Verdict.tsx (lines 253-263):
```tsx
<a
  href={linkedInShareUrl ?? undefined}
  target="_self"
  rel="noopener noreferrer"
  className={`${BTN_DEBRIEF_NAV} flex h-[40px] md:h-[48px] items-center justify-center gap-2 ...`}
  aria-label="Share on LinkedIn"
>
  <i className="fa-brands fa-linkedin text-lg leading-none"></i>
  2. Share on LinkedIn
</a>
```

Current Reboot button in DebriefPage3Verdict.tsx (lines 285-289):
```tsx
<button type="button" onClick={onRestart} className={BTN_DEBRIEF_NAV}>
  Reboot system
</button>
```

Current Copy button already has explicit height (lines 245-251):
```tsx
className={`${BTN_DEBRIEF_NAV} flex h-[40px] md:h-[48px] items-center justify-center gap-2 ...`}
```

**Research Update:** Change from `h-[40px]` to `h-[44px]` on mobile per RESEARCH.md (44px iOS HIG minimum). Desktop stays at 48px.

Current DebriefPage2AuditTrail.tsx copy to change (lines 217-227):
```tsx
<p className="px-1 text-base text-slate-400 md:text-lg">
  {isKirk ? "WARNING: Audit integrity compromised" : "A complete record of your governance decisions"}
</p>
{!isKirk && (
  <p className="mt-2 text-sm text-slate-500 max-w-lg mx-auto px-1">
    Every decision left a paper trail. This is it — no edits, no
    redactions, no plausible deniability.
  </p>
)}
```

LinkedIn DM anchor (reference — how the DM button works, in DebriefPage3Verdict.tsx line 277):
```tsx
<a
  href="https://www.linkedin.com/in/schwedeny/"
  className="inline-flex ... min-h-[40px] md:min-h-[48px]"
>
```
The DM anchor uses `target` omitted (defaults to `_self` effectively opens inline on mobile LinkedIn webview).
Per user decision: Share on LinkedIn should behave like the DM button — use `openLinkedInShare` which tries the linkedin:// deep-link on mobile and falls back to window.open on desktop.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix button heights, LinkedIn in-app share, and em dashes in DebriefPage3</name>
  <files>src/components/game/debrief/DebriefPage3Verdict.tsx</files>
  <action>
**Research Patterns Applied:**
- **Mobile-First Touch Targets** (RESEARCH.md Pattern 2): 44px minimum per iOS HIG/WCAG 2.5.5
- **Web Share API with Fallback** (RESEARCH.md Pattern 3): Use `openLinkedInShare()` for in-app behavior
- **Anti-Pattern Avoidance**: Fixed height buttons (`h-[]`) → use explicit heights for consistency

Three changes to DebriefPage3Verdict.tsx:

**A) Import openLinkedInShare**
Add to the existing linkedin-share import line:
```typescript
import {
  encodeLinkedInShareUrl,
  formatShareText,
  getShareUrl,
  openLinkedInShare,  // ADD THIS
} from "../../../utils/linkedin-share";
```

**B) Replace LinkedIn `<a>` with `<button>` using openLinkedInShare**
**Research Insight:** Using `<button onClick={openLinkedInShare}>` instead of `<a target="_self">` ensures mobile opens LinkedIn app (not new browser page). The `openLinkedInShare` utility handles mobile deep-link + web fallback.

Replace the LinkedIn anchor element with a button:
```tsx
<div className="flex w-full justify-center">
  <button
    type="button"
    onClick={() => {
      if (linkedInShareUrl) openLinkedInShare(linkedInShareUrl);
    }}
    disabled={!linkedInShareUrl}
    className={`${BTN_DEBRIEF_NAV} flex h-[44px] md:h-[48px] items-center justify-center gap-2 disabled:opacity-50 disabled:cursor-not-allowed`}
    aria-label="Share on LinkedIn"
  >
    <i className="fa-brands fa-linkedin text-lg leading-none"></i>
    2. Share on LinkedIn
  </button>
</div>
```

**C) Fix Reboot button height to match the other two buttons**
**Research Standard:** Per RESEARCH.md, use 44px on mobile (iOS HIG minimum) and 48px on desktop.

The Reboot button uses only `BTN_DEBRIEF_NAV` (which has `min-h-[40px]` but no explicit height). On mobile, min-h can vary based on content. Add explicit height to match research standards:
```tsx
<button
  type="button"
  onClick={onRestart}
  className={`${BTN_DEBRIEF_NAV} flex h-[44px] md:h-[48px] items-center justify-center`}
>
  Reboot system
</button>
```

**D) Check for em dashes in this file**
**Research Standard:** Per RESEARCH.md, replace `—` with cleaner punctuation.

Search DebriefPage3Verdict.tsx for `—` characters. Replace any found:
- `X — Y` patterns → `X: Y` or `X, Y` (use context to pick best alternative)
- Note: The `KIRK_SHARE_TEXT` constant uses em dash in share copy — per user decision, replace debrief PAGE em dashes, but share text strings are outbound content; leave those unless they appear directly in rendered JSX.
Scan JSX text nodes only (not string constants used in external URLs/share text).
  </action>
  <verify>
    <automated>cd /Users/yevgenschweden/swiperisk && rtk bun run typecheck 2>&1 | tail -5</automated>
  </verify>
  <done>Typecheck passes. LinkedIn button is a `<button>` calling `openLinkedInShare`. Reboot button has `h-[40px] md:h-[48px]` explicit height. No `<a target="_self">` remains for the share button. No em dashes in JSX text nodes of DebriefPage3Verdict.</done>
</task>

<task type="auto">
  <name>Task 2: Debrief page 2 copy changes and em dash cleanup</name>
  <files>src/components/game/debrief/DebriefPage2AuditTrail.tsx</files>
  <action>
**Research Pattern Applied:** Typography cleanup per RESEARCH.md — replace em dashes with cleaner punctuation.

Two copy changes in DebriefPage2AuditTrail.tsx:

**A) Remove "governance" from subtitle**
Change line:
```tsx
"A complete record of your governance decisions"
```
To:
```tsx
"A complete record of your decisions"
```

**B) Change "Every decision" to "Each one"**
Change:
```tsx
Every decision left a paper trail. This is it — no edits, no
redactions, no plausible deniability.
```
To:
```tsx
Each one left a paper trail. This is it: no edits, no redactions, no plausible deniability.
```
(Replace em dash `—` with colon `:` for cleaner punctuation as per RESEARCH.md recommendations.)

**C) Scan for all other em dashes in this file**
**Research Standard:** Per RESEARCH.md, em dashes in UI copy should use commas, colons, or periods for cleaner typography.

Search for `—` throughout DebriefPage2AuditTrail.tsx JSX text nodes and string literals rendered to the screen. Replace each:
- `—` between clauses → `:` or `,` based on context
- Check `formatConsequence`, `getPersonalityComment`, and all JSX text
- Note: `•` (bullet) in `formatConsequence` is NOT an em dash — leave it.
  </action>
  <verify>
    <automated>cd /Users/yevgenschweden/swiperisk && rtk bun run typecheck 2>&1 | tail -5</automated>
  </verify>
  <done>Typecheck passes. Subtitle reads "A complete record of your decisions". Paper trail line reads "Each one left a paper trail. This is it: no edits...". No em dashes (`—`) remain in JSX text nodes of DebriefPage2AuditTrail.</done>
</task>

</tasks>

<verification>
```bash
cd /Users/yevgenschweden/swiperisk && rtk bun run typecheck && rtk bun run lint && rtk bun run test:smoke 2>&1 | tail -10
```
All pass. Verify manually: debrief page 3 shows three buttons at same height on mobile (320-390px viewport). LinkedIn button opens in-app. Reboot, LinkedIn, Copy all visually same height.
</verification>

<success_criteria>
- **Research Pattern Applied:** Web Share API with Fallback (RESEARCH.md Pattern 3)
  - LinkedIn `<a target="_self">` replaced with `<button onClick={openLinkedInShare}>` in DebriefPage3Verdict
  - Opens LinkedIn in-app on mobile (not new browser page)
- **Research Standard Applied:** Mobile-First Touch Targets (RESEARCH.md Pattern 2)
  - Reboot button has `h-[44px] md:h-[48px]` (44px iOS HIG minimum, 48px desktop)
  - Copy and LinkedIn buttons updated to 44px mobile per RESEARCH.md
- **Research Pattern Applied:** Typography Cleanup
  - DebriefPage2AuditTrail subtitle: "A complete record of your decisions"
  - DebriefPage2AuditTrail paper trail: "Each one left a paper trail. This is it: no edits, no redactions, no plausible deniability."
  - No em dashes (`—`) in JSX text nodes of either debrief file (RESEARCH.md: use commas/colons)
- Typecheck, lint, smoke tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/27-mobile-ux-fixes-audio-isolation-button-heights-linkedin-share-copy-link-debrief-text-uiux-sizing-audit/27-02-SUMMARY.md`
</output>
