---
phase: 09-visual-effects
plan: "04"
type: TDD
wave: 3
depends_on: ["01", "02", "03"]
files_modified:
  - components/game/FeedbackOverlay.tsx
  - components/game/GameHUD.tsx
  - tests/stage-snapshots.spec.ts
  - tests/swipe-interactions.spec.ts
  - tests/exit-animation.spec.ts
  - tests/visual-effects-performance.spec.ts
autonomous: true
must_haves:
  truths:
    - "Choice outcomes feel more satisfying through distinct success and fail micro-effects, not just text and icons."
    - "The new visual effects phase can be regression-tested instead of depending on hand-wavy 'looks cool' claims."
    - "Visual polish stays performance-constrained through explicit guardrails and automated checks."
    - "prefers-reduced-motion respected; outcome micro-effects honor reduced-motion hooks from earlier plans."
  artifacts:
    - path: components/game/FeedbackOverlay.tsx
      provides: "Outcome micro-effects with success/failure visual differentiation and verification hooks."
      contains: "data-testid"
    - path: components/game/GameHUD.tsx
      provides: "Lightweight metric-response polish that complements the overlay without constant jank."
      contains: "progress-bar"
    - path: tests/visual-effects-performance.spec.ts
      provides: "Focused browser-level checks for visual-effect guardrails and bounded effect behavior."
    - path: tests/stage-snapshots.spec.ts
      provides: "Updated snapshot coverage for ambient screens, gameplay, and feedback visuals."
  key_links:
    - from: components/game/FeedbackOverlay.tsx
      to: components/game/GameHUD.tsx
      via: "outcome feedback and metric polish use a consistent success/failure vocabulary"
      pattern: "success|fail|warning|pulse"
    - from: tests/swipe-interactions.spec.ts
      to: components/game/CardStack.tsx
      via: "swipe-triggered assertions verify the bounded effect layer and stable DOM hooks added to the gameplay card path"
      pattern: "data-testid|effect|trail"
    - from: tests/visual-effects-performance.spec.ts
      to: App.tsx
      via: "performance-guardrail tests inspect stage-wrapper hooks and reduced-motion or fallback behavior exposed by the transition system"
      pattern: "data-stage|reduced-motion|fallback"
    - from: tests/visual-effects-performance.spec.ts
      to: components/game/FeedbackOverlay.tsx
      via: "guardrail tests assert bounded outcome-effect containers instead of unbounded visual spam"
      pattern: "data-testid|effect|burst|bounded"
---

<objective>
Finish Phase 09 with outcome micro-effects plus explicit regression and performance coverage so the phase ships as polished UI rather than a pile of unverified animation glitter.

Purpose: Deliver VFX-04 directly and close VFX-05 by turning performance constraints into executable checks.
Output: Enhanced feedback/HUD polish, focused visual-effects regression coverage, and a dedicated performance-guardrail test suite.
</objective>

<execution_context>
@/Users/yevgenschweden/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/yevgenschweden/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-visual-effects/09-01-PLAN.md
@.planning/phases/09-visual-effects/09-02-PLAN.md
@.planning/phases/09-visual-effects/09-03-PLAN.md
@components/game/FeedbackOverlay.tsx
@components/game/GameHUD.tsx
@tests/stage-snapshots.spec.ts
@tests/swipe-interactions.spec.ts
@tests/exit-animation.spec.ts
</context>

<tdd_config>
Use `type: execute`.

This plan closes the UI polish phase with targeted Playwright coverage and guardrail checks rather than a standalone red-green-refactor cycle.
</tdd_config>

<tasks>

<task type="auto">
  <name>Task 1: Add outcome micro-effects that differentiate success and failure states</name>
  <files>components/game/FeedbackOverlay.tsx, components/game/GameHUD.tsx</files>
  <action>Enhance `FeedbackOverlay.tsx` with distinct micro-effects for positive and negative outcomes (restrained bursts, glow pulses, ring sweeps, metric-response accents). Enforce explicit caps: bursts max 20 particles; keep trails at 15 segments if any. Animate only transform and opacity; avoid layout-heavy properties. Honor usePrefersReducedMotion — gate optional flourishes; core feedback (icon, text) stays when reduced. Extend `GameHUD.tsx` with short-lived metric-response polish; no constant churn. Expose stable `data-testid` or `data-*` hooks for effect containers.</action>
  <verify>`bun run typecheck && bun run build`</verify>
  <done>Outcome feedback feels visibly richer for both positive and negative results, HUD polish remains lightweight, and the implementation exposes stable hooks for automated verification.</done>
</task>

<task type="auto">
  <name>Task 2: Update visual regression coverage for the new effect surface</name>
  <files>tests/stage-snapshots.spec.ts, tests/swipe-interactions.spec.ts, tests/exit-animation.spec.ts</files>
  <action>Update existing Playwright coverage so the new Phase 09 visuals are exercised through stable selectors, snapshot masks, and deterministic assertions rather than fragile timing guesses. Extend swipe-focused tests to confirm the effect layer exists and stays bounded during drag/commit flows. Update snapshot coverage where needed for ambient screens, playing, and feedback-overlay states so the new visual language is captured without making the suite flaky from known animated regions.</action>
  <verify>`bun run test -- tests/stage-snapshots.spec.ts tests/swipe-interactions.spec.ts tests/exit-animation.spec.ts`</verify>
  <done>The main new visual surfaces are covered by deterministic Playwright assertions or snapshots, and the suite remains stable enough to catch regressions without constant false alarms.</done>
</task>

<task type="auto">
  <name>Task 3: Add a focused browser suite for Phase 09 performance guardrails</name>
  <files>tests/visual-effects-performance.spec.ts</files>
  <action>Create `tests/visual-effects-performance.spec.ts` to validate VFX-05 guardrails. Assert: bounded counts (15 trail segments, 20 burst particles), reduced-motion/fallback behavior, transform/opacity-only effect containers (no layout-heavy properties). Keep the suite browser-deterministic; do not benchmark FPS. Assert design choices that protect 60fps. Cover swipe effects, stage wrappers, outcome micro-effects. Confirm no framer-motion or react-spring.</action>
  <verify>`bun run test -- tests/visual-effects-performance.spec.ts`</verify>
  <done>VFX-05 is represented by explicit automated guardrail checks, including bounded effect behavior and implementation patterns chosen to avoid jank.</done>
</task>

</tasks>

<verification>
1. Run `bun run typecheck`.
2. Run `bun run build`.
3. Run `bun run test -- tests/stage-snapshots.spec.ts tests/swipe-interactions.spec.ts tests/exit-animation.spec.ts tests/visual-effects-performance.spec.ts`.
4. Run `bun run test` if this plan completes after significant Phase 09 implementation drift.
5. Confirm outcome effects use bounded caps (20 particles), transform/opacity only, and honor usePrefersReducedMotion.
</verification>

<success_criteria>
- VFX-04 is satisfied through distinct success/failure outcome micro-effects in the feedback path.
- VFX-05 is encoded as bounded implementation guardrails plus automated browser checks, not wishful thinking.
- Phase 09 finishes with regression coverage for the new swipe, stage, ambient, and outcome visuals.
</success_criteria>

<output>
After completion, create `.planning/phases/09-visual-effects/09-04-SUMMARY.md`
</output>
