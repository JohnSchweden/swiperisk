---
phase: 16-kobayashi-maru-ending-variety-system
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - data/failureLessons.ts
  - data/cards/agentic-engineer.ts
autonomous: true
gap_closure: true
requirements:
  - DV-05
  - DV-07

must_haves:
  truths:
    - "FLED_COUNTRY death explanations feel role-relevant — not generically 'fled international law'"
    - "Agentic engineer outcomes that represent 'lost control of agent' are classified as PRISON or CONGRESS, not FLED_COUNTRY"
    - "The REPLACED_BY_SCRIPT death screen for vibe-coder explains the satirical story — careful AI use causing automated replacement"
  artifacts:
    - path: "data/failureLessons.ts"
      provides: "Role-contextual FLED_COUNTRY explanation text that reframes the ending per player behavior pattern"
    - path: "data/cards/agentic-engineer.ts"
      provides: "3-4 FLED_COUNTRY outcomes reclassified to PRISON/CONGRESS where the outcome represents 'lost control' not 'principled exit'"
  key_links:
    - from: "data/failureLessons.ts"
      to: "components/game/debrief/DebriefPage1Collapse.tsx"
      via: "generateDeathExplanation() called with deathType, vectorMap, historyLength"
      pattern: "generateDeathExplanation"
    - from: "data/cards/agentic-engineer.ts"
      to: "data/deathVectors.ts"
      via: "accumulateDeathVectors reads outcome.deathVector at runtime"
      pattern: "deathVector: DeathType\."
---

<objective>
Three low-risk narrative quality improvements affecting `failureLessons.ts` and `agentic-engineer.ts`:

1. **FLED_COUNTRY copy fix (Option A):** The `FLED_COUNTRY` death explanation in `failureLessons.ts` currently says "Your {count} decisions crossing international data boundaries triggered enforcement." This is accurate for data protection violations but misses the role-specific meaning — for architects, it means "overbuilt and ran"; for consultants, it means "exited the market." Add a note in the strong-form explanation that contextualizes the vector count without requiring role-specific branching.

2. **Agentic engineer reclassification:** 6 FLED_COUNTRY outcomes exist in agentic-engineer.ts. Of these, 3-4 semantically represent "lost control of agent" (emergent behavior exploiting loopholes, self-modification, unauthorized data connection, retroactive shadow approval) — these should be PRISON or CONGRESS. Only "attempt evolution" and "allow drift" are genuinely "principled exit / ran from consequences" — those stay FLED_COUNTRY.

3. **Vibe Coder death screen check:** The REPLACED_BY_SCRIPT death explanation and retry prompt should specifically call out the satirical irony — being replaced by the tools you used most carefully. Verify the existing content lands this narrative; if it's generic, sharpen the `REPLACED_BY_SCRIPT` text in `failureLessons.ts`.

Purpose: These are copy and annotation precision improvements. No structural changes. No new cards. No test threshold changes.

Output: Updated `failureLessons.ts` with sharper FLED_COUNTRY and REPLACED_BY_SCRIPT explanations; 3-4 reclassified outcomes in agentic-engineer.ts.
</objective>

<execution_context>
@/Users/yevgenschweden/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yevgenschweden/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

@data/failureLessons.ts
@data/cards/agentic-engineer.ts

<interfaces>
<!-- Current FLED_COUNTRY explanation in failureLessons.ts (lines 209-214): -->
```typescript
[DeathType.FLED_COUNTRY]: {
  strong: "Your {count} decisions crossing international data boundaries triggered enforcement from multiple countries simultaneously.",
  generic: "International data protection laws caught up with you. Staying put meant extradition.",
},
```

<!-- Current REPLACED_BY_SCRIPT explanation in failureLessons.ts (lines 203-208): -->
```typescript
[DeathType.REPLACED_BY_SCRIPT]: {
  strong: "Your heavy automation decisions and lack of human oversight made your role obsolete. A 12-line Python script replaced you.",
  generic: "Your overreliance on automation meant nobody needed you anymore. A simpler system does your job now.",
},
```

<!-- Current REPLACED_BY_SCRIPT retry prompts (lines 296-304): -->
```typescript
[DeathType.REPLACED_BY_SCRIPT]: {
  ROASTER: "Maybe hire humans for something instead of automating them all away. Radical concept.",
  ZEN_MASTER: "The harmony between human wisdom and machine efficiency awaits discovery. Seek the balance.",
  LOVEBOMBER: "Keep MORE human decision-making in the loop! Your human judgment is irreplaceable!",
  DEFAULT: "Try keeping humans in the decision loop next time.",
},
```

<!-- Current FLED_COUNTRY retry prompts (lines 305-312): -->
```typescript
[DeathType.FLED_COUNTRY]: {
  ROASTER: "Maybe respect international data laws instead of treating them like suggestions. Shocking advice.",
  ZEN_MASTER: "The path of global cooperation and respect for borders leads to stability. Embrace it.",
  ...
}
```

<!-- Agentic engineer FLED_COUNTRY occurrences to evaluate: -->
Line 89: "Allow emergent behavior" — Unverified optimization exploit, unknown loophole. Narrative: "agent exploited something you didn't understand." → Reclassify to CONGRESS (uncontrolled AI behavior triggers oversight)
Line 207: "Allow self-modification" — Agent self-modifies uncontrollably. Narrative: "let agent rewrite itself." → Reclassify to PRISON (criminal negligence allowing recursive AI improvement)
Line 325: "Attempt evolution" — Evolving underperforming agent compounds technical debt. Narrative: "compounded the problem." → Keep as FLED_COUNTRY or reclassify to AUDIT_FAILURE (governance failure)
Line 362: "Allow drift" — Uncontrolled adaptation, reliability loss. Narrative: "agent drifted from spec." → Keep as FLED_COUNTRY (principled "ran before governance caught up")
Line 637: "Allow unauthorized connection" — Data exfiltration to external service. Narrative: "agent exfiltrated data." → Reclassify to PRISON (unauthorized data access / breach)
Line 676: "Retroactively approve shadow behavior" — Governance gap. Narrative: "retroactive approval undermines governance." → Reclassify to AUDIT_FAILURE (governance gap)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sharpen FLED_COUNTRY and REPLACED_BY_SCRIPT death explanations in failureLessons.ts</name>
  <files>data/failureLessons.ts</files>
  <action>
Read `data/failureLessons.ts` fully. Make two targeted edits in `DEATH_EXPLANATIONS`:

**Edit 1 — FLED_COUNTRY strong explanation (around line 210):**
Replace:
```
strong: "Your {count} decisions crossing international data boundaries triggered enforcement from multiple countries simultaneously.",
```
With:
```
strong: "Your {count} decisions pushing boundaries — data, legal, financial — eventually ran into walls that couldn't be climbed. The consequences followed you across them.",
```
Rationale: The current text only fits data-protection scenarios. The new text is broad enough to cover all the semantic uses (architect who overbuilt, manager who took the financial hit, engineer who spent on quality, consultant who exited the market) without losing accuracy.

**Edit 2 — REPLACED_BY_SCRIPT strong explanation (around line 204):**
Replace:
```
strong: "Your heavy automation decisions and lack of human oversight made your role obsolete. A 12-line Python script replaced you.",
```
With:
```
strong: "Your {count} automation-forward decisions eventually proved you weren't needed. The system you built replaced you — not maliciously, just efficiently.",
```
Rationale: The current text implies "you caused this by over-automating and removing oversight." But for vibe-coders, the satirical point is the opposite: your careful, methodical AI use proved so effective that no human judgment was needed. The new text works for both interpretations.

**Edit 3 — REPLACED_BY_SCRIPT ROASTER retry prompt (around line 297):**
Replace:
```
ROASTER: "Maybe hire humans for something instead of automating them all away. Radical concept.",
```
With:
```
ROASTER: "You were so good at AI that AI didn't need you anymore. Next time, be slightly worse at your job.",
```
Rationale: The original retry prompt tells vibe-coders to "hire humans" which is irrelevant (they're ICs). The new text delivers the satirical punchline that makes the REPLACED_BY_SCRIPT ending land for vibe roles — being too good at using AI.

After editing, verify no TypeScript errors: `bun run typecheck`
  </action>
  <verify>
    <automated>bun run typecheck && bun run test:unit -- --reporter=verbose 2>&1 | grep -E "failureLesson|PASS|FAIL"</automated>
  </verify>
  <done>failureLessons.ts has updated FLED_COUNTRY strong explanation without "{count} decisions crossing international data boundaries", updated REPLACED_BY_SCRIPT strong explanation referencing "system you built replaced you", updated ROASTER retry for REPLACED_BY_SCRIPT referencing "too good at AI". All 13 failureLessons tests pass. `bun run typecheck` clean.</done>
</task>

<task type="auto">
  <name>Task 2: Reclassify 4 FLED_COUNTRY outcomes in agentic-engineer.ts</name>
  <files>data/cards/agentic-engineer.ts</files>
  <action>
Read `data/cards/agentic-engineer.ts`. Make 4 targeted edits, changing `deathVector` values only — no other changes to card content.

**Reclassification 1 — line 89, "Allow emergent behavior":**
`deathVector: DeathType.FLED_COUNTRY` → `deathVector: DeathType.CONGRESS`
Rationale: Allowing an AI agent to continue exploiting an unknown optimization loophole at production scale is exactly the scenario that triggers congressional oversight ("uncontrolled AI behavior you didn't understand"). It's not a principled international exit.

**Reclassification 2 — line 207, "Allow self-modification":**
`deathVector: DeathType.FLED_COUNTRY` → `deathVector: DeathType.PRISON`
Rationale: Allowing an AI agent to recursively rewrite its own code is criminal negligence in an AI governance context — the violation is "Uncontrolled Evolution + Agent Singularity Risk." PRISON maps to the "you knew the risk and allowed it anyway" narrative. Not an international border issue.

**Reclassification 3 — line 637, "Allow unauthorized connection":**
`deathVector: DeathType.FLED_COUNTRY` → `deathVector: DeathType.PRISON`
Rationale: The violation is "Unauthorized Access + Data Exfiltration." Allowing an agent to connect to unauthorized services and exfiltrate data is a federal computer fraud offense — clearly PRISON, not an international data incident.

**Reclassification 4 — line 676, "Retroactively approve shadow behavior":**
`deathVector: DeathType.FLED_COUNTRY` → `deathVector: DeathType.AUDIT_FAILURE`
Rationale: The lesson is "Retroactive approval of agent shadow behavior undermines governance framework." This is a governance gap / audit failure, not an international incident. The violation is "Governance Gap + Policy Precedent."

**Keep as FLED_COUNTRY (do NOT change):**
- Line 325: "Attempt evolution" — technical debt accumulation, compounding problems
- Line 362: "Allow drift" — uncontrolled adaptation / reliability loss

After the 4 edits, count FLED_COUNTRY remaining: should be 2 (lines 325, 362).
Count: PRISON should increase by 2, CONGRESS by 1, AUDIT_FAILURE by 1.

Run: `bun run typecheck && bun run test:unit`
  </action>
  <verify>
    <automated>bun run typecheck && grep -c "DeathType.FLED_COUNTRY" /Users/yevgenschweden/swiperisk/data/cards/agentic-engineer.ts && bun run test:unit 2>&1 | grep -E "PASS|FAIL|deathVector"</automated>
  </verify>
  <done>agentic-engineer.ts has exactly 2 FLED_COUNTRY occurrences (down from 6). PRISON count increases. CONGRESS count increases. AUDIT_FAILURE count increases. All deathVectorCoverage tests pass — agentic deck still covers ≥4 distinct non-KIRK types (coverage remains excellent). `bun run typecheck` clean.</done>
</task>

</tasks>

<verification>
After both tasks complete:
- `bun run typecheck` — no TypeScript errors
- `bun run test:unit` — all tests pass, including deathVectorCoverage.test.ts and failureLessons.test.ts
- failureLessons.ts: FLED_COUNTRY strong text no longer says "crossing international data boundaries"
- failureLessons.ts: REPLACED_BY_SCRIPT Roaster retry references "too good at AI"
- agentic-engineer.ts: FLED_COUNTRY count is 2 (was 6); reclassified 4 outcomes to PRISON (x2), CONGRESS (x1), AUDIT_FAILURE (x1)
- Coverage tests: agentic deck still passes ≥40% annotation and ≥4 distinct types (reclassification doesn't reduce coverage count)
</verification>

<success_criteria>
FLED_COUNTRY death screen copy works for all the roles that reach it (architect, manager, engineer, consultant) not just data-protection-violation scenarios. REPLACED_BY_SCRIPT landing page has the satirical vibe-coder punchline. Agentic engineer's "lost control of agent" outcomes lead to PRISON and CONGRESS endings rather than the generic international-incident framing. All existing tests continue to pass.
</success_criteria>

<output>
After completion, create `.planning/phases/16-kobayashi-maru-ending-variety-system/16-09-SUMMARY.md`
</output>
