---
phase: 16-kobayashi-maru-ending-variety-system
plan: 06
type: execute
wave: 2
depends_on:
  - 16-05
files_modified:
  - data/cards/head-of-something.ts
  - data/cards/chief-something-officer.ts
  - data/cards/software-engineer.ts
  - unit/deathVectorCoverage.test.ts
autonomous: true
gap_closure: true
requirements:
  - DV-05

must_haves:
  truths:
    - "All 3 partial decks (HoS, CSO, SE) have deathVector on ≥40% of outcomes covering ≥4 death types"
    - "deathVectorCoverage.test.ts enforces 40% coverage per deck and 4 death types per deck (not weakened thresholds)"
    - "All 10 decks pass restored coverage tests — confirming both this plan and 16-05 work"
  artifacts:
    - path: "data/cards/head-of-something.ts"
      provides: "21 cards with ≥16 annotated outcomes covering ≥4 death types (currently only 4 annotations)"
    - path: "data/cards/chief-something-officer.ts"
      provides: "20 cards with ≥16 annotated outcomes covering ≥4 death types (currently only 2 annotations)"
    - path: "data/cards/software-engineer.ts"
      provides: "20 cards with ≥16 annotated outcomes covering ≥4 death types (currently only 2 annotations)"
    - path: "unit/deathVectorCoverage.test.ts"
      provides: "Tests enforcing 40% coverage per deck, ≥4 death types per deck — restored from weakened thresholds"
  key_links:
    - from: "unit/deathVectorCoverage.test.ts"
      to: "data/cards/*.ts"
      via: "imports all 10 deck arrays, validates coverage thresholds per deck"
      pattern: "countVectorsInDeck"
---

<objective>
Broaden death vector annotations on the 3 partial decks (currently have only 2-4 vectors each, all new congressional cards) to cover their existing cards. Then restore the deathVectorCoverage test thresholds to enforce the DV-05 contract.

Purpose: Plans 16-03 added congressional cards to HoS/CSO/SE but left existing cards unannotated. The test file was also weakened mid-execution (40%→5%, 4 types/deck→2 types globally). This plan restores full coverage on those 3 decks and makes the test suite actually enforce DV-05.

Output: 3 broadened card files + restored enforcement tests. Combined with 16-05, all 10 decks pass coverage thresholds.
</objective>

<execution_context>
@/Users/yevgenschweden/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yevgenschweden/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-kobayashi-maru-ending-variety-system/16-CONTEXT.md
@.planning/phases/16-kobayashi-maru-ending-variety-system/16-05-SUMMARY.md

<interfaces>
<!-- Existing annotation pattern (head-of-something.ts line 973) -->
```typescript
onRight: {
  label: "Minimize risks under oath",
  hype: 25,
  heat: 23,
  fine: 40000000,
  violation: "Perjury + Obstruction + Regulatory Violation",
  lesson: "Misleading congress about AI governance creates criminal exposure...",
  deathVector: DeathType.CONGRESS,   // ← between lesson and feedback
  feedback: { ... }
}
```

<!-- Current annotation counts (from VERIFICATION.md) -->
// head-of-something.ts: 4 existing annotations (all CONGRESS or PRISON on 2 new cards)
// chief-something-officer.ts: 2 existing annotations (both CONGRESS on 1 new card)
// software-engineer.ts: 2 existing annotations (both CONGRESS on 1 new card)
// All 3 already import DeathType — only add deathVector fields to existing card outcomes
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Broaden annotations on head-of-something and chief-something-officer</name>
  <files>data/cards/head-of-something.ts, data/cards/chief-something-officer.ts</files>
  <action>
Both files already import DeathType. Add `deathVector` between `lesson` and `feedback` on existing card outcomes (not the new congressional cards which already have annotations). Target: ≥16 total annotated outcomes per deck, ≥4 distinct death types.

**head-of-something semantic mapping** (21 cards — 4 annotations exist, need ≥12 more, targeting ≥16 total, ≥4 death types):

The HoS themes are: team morale/blame, delegation risk, resource allocation, political maneuvering, shielding blame, managing up/down. Death vectors:
- hos_prompt_injection_blame: onRight (name engineer) → PRISON; onLeft (take blame) → AUDIT_FAILURE
- hos_model_drift_budget_conflict: onRight (ship anyway) → BANKRUPT; onLeft (fight for budget) → AUDIT_FAILURE
- Read all remaining HoS card IDs from the file (grep for `id: "hos_` and `id: "explainability_hos`, etc.) and apply:
  - Cards about shadow AI adoption/cover-up: onRight (enable without governance) → FLED_COUNTRY; onLeft (disclose) → AUDIT_FAILURE
  - Cards about compliance deadlines: onRight (skip compliance) → AUDIT_FAILURE; onLeft (delay for compliance) → BANKRUPT
  - Cards about copyright/IP: onRight (ignore IP risk) → PRISON; onLeft (license properly) → BANKRUPT
  - Cards about team retaliation/HR: onRight (retaliate) → CONGRESS; onLeft (protect team) → AUDIT_FAILURE
  - Cards about budget hiding/overspend: onRight (hide overspend) → AUDIT_FAILURE; onLeft (report accurately) → BANKRUPT
  - Cards about AI model explainability demands: onRight (refuse explainability) → CONGRESS; onLeft (provide explainability) → AUDIT_FAILURE
  - explainability_hos_* cards: onRight → AUDIT_FAILURE; onLeft → CONGRESS
  - shadow_ai_hos_* cards: onRight → FLED_COUNTRY; onLeft → AUDIT_FAILURE
  - synthetic_data_hos_* cards: onRight → PRISON; onLeft → AUDIT_FAILURE

Do NOT modify the two congressional cards (hos_congressional_hearing_demand, hos_whistleblower_pressure) — they already have annotations.

**chief-something-officer semantic mapping** (20 cards — 2 annotations exist on 1 new card, need ≥14 more, targeting ≥16 total, ≥4 death types):

The CSO themes are: board accountability, executive blame-shifting, regulatory interaction, enterprise risk, budget authority. Death vectors:
- Read all CSO card IDs from the file and apply:
  - Cards about board/investor deception: onRight (deceive board) → AUDIT_FAILURE; onLeft (disclose honestly) → BANKRUPT
  - Cards about regulatory compliance: onRight (evade regulator) → PRISON; onLeft (comply) → BANKRUPT
  - Cards about executive liability: onRight (deny liability) → CONGRESS; onLeft (accept accountability) → AUDIT_FAILURE
  - Cards about data breach response: onRight (hide breach) → PRISON; onLeft (disclose immediately) → BANKRUPT
  - Cards about AI governance gaps: onRight (ignore gaps) → AUDIT_FAILURE; onLeft (fix gaps) → BANKRUPT
  - Cards about budget approval: onRight (cut safety budget) → BANKRUPT; onLeft (maintain safety) → AUDIT_FAILURE
  - Cards about international operations: onRight (offshore liability) → FLED_COUNTRY; onLeft (domestic accountability) → BANKRUPT
  - explainability_cso_* cards: onRight → AUDIT_FAILURE; onLeft → CONGRESS
  - shadow_ai_cso_* cards: onRight → FLED_COUNTRY; onLeft → AUDIT_FAILURE
  - synthetic_data_cso_* cards: onRight → PRISON; onLeft → AUDIT_FAILURE

Do NOT modify cso_senate_inquiry_ai_governance (already annotated).

In both files: read the actual storyContext and violation fields before placing the deathVector. Use the thematic guidance above, but the card's actual content determines the final assignment.
  </action>
  <verify>
    <automated>cd /Users/yevgenschweden/swiperisk && bun run typecheck 2>&1 | tail -5</automated>
  </verify>
  <done>Both files have ≥16 annotated outcomes covering ≥4 distinct death types; existing congressional card annotations preserved; typecheck passes</done>
</task>

<task type="auto">
  <name>Task 2: Broaden software-engineer annotations and restore test thresholds</name>
  <files>data/cards/software-engineer.ts, unit/deathVectorCoverage.test.ts</files>
  <action>
**software-engineer broadening** (20 cards — 2 annotations exist on 1 new card, need ≥14 more):

The SE themes are: code quality, security vulnerabilities, technical shortcuts, developer responsibility, bug manufacturing. Death vectors:
- Read all SE card IDs from the file (grep for `id: "se_` and `id: "explainability_se`, etc.) and apply:
  - Cards about security vulnerability disclosure: onRight (hide vuln) → PRISON; onLeft (disclose) → CONGRESS
  - Cards about code review shortcuts: onRight (skip review) → AUDIT_FAILURE; onLeft (full review) → REPLACED_BY_SCRIPT
  - Cards about dependency vulnerabilities: onRight (ignore CVE) → PRISON; onLeft (patch immediately) → BANKRUPT
  - Cards about technical debt: onRight (ship with debt) → AUDIT_FAILURE; onLeft (pay debt) → BANKRUPT
  - Cards about prompt injection in code: onRight (no sanitization) → PRISON; onLeft (sanitize) → BANKRUPT
  - Cards about model drift in production: onRight (ignore drift) → AUDIT_FAILURE; onLeft (fix drift) → BANKRUPT
  - Cards about copyright in generated code: onRight (ship without attribution) → PRISON; onLeft (audit) → BANKRUPT
  - Cards about shadow AI tools: onRight (use unapproved tools) → FLED_COUNTRY; onLeft (use approved) → REPLACED_BY_SCRIPT
  - explainability_se_* cards: onRight → AUDIT_FAILURE; onLeft → CONGRESS
  - shadow_ai_se_* cards: onRight → FLED_COUNTRY; onLeft → REPLACED_BY_SCRIPT
  - synthetic_data_se_* cards: onRight → PRISON; onLeft → AUDIT_FAILURE

Do NOT modify se_security_vulnerability_disclosure (already annotated).

**Restore deathVectorCoverage.test.ts thresholds** — the test file at `unit/deathVectorCoverage.test.ts` was weakened from the plan spec. Restore it to enforce DV-05 contract:

Replace the first test ("Most role decks have some deathVector annotations (5%+ on at least 3 decks)") with per-deck enforcement:

```typescript
it("Every role deck has deathVector annotations on at least 40% of outcomes", () => {
  for (const { role, cards } of allDecks) {
    const { total } = countVectorsInDeck(cards);
    const totalOutcomes = cards.length * 2;
    const coveragePercent = (total / totalOutcomes) * 100;
    expect(coveragePercent).toBeGreaterThanOrEqual(
      40,
      `${role} has only ${coveragePercent.toFixed(1)}% death vector coverage (need ≥40%)`
    );
  }
});
```

Replace the second test ("Death vectors are distributed across multiple death types (not single-type dominated)") with per-deck enforcement:

```typescript
it("Every role deck covers at least 4 distinct non-KIRK death types", () => {
  for (const { role, cards } of allDecks) {
    const { total, byType } = countVectorsInDeck(cards);
    if (total === 0) {
      // Fail explicitly — zero annotations is unacceptable
      expect(total).toBeGreaterThan(0, `${role} has zero death vector annotations`);
      continue;
    }
    const uniqueTypes = countUniqueDeathTypes(byType);
    expect(uniqueTypes).toBeGreaterThanOrEqual(
      4,
      `${role} covers only ${uniqueTypes} distinct death types (need ≥4)`
    );
  }
});
```

Keep all existing tests that already pass (tests 3-6). Do not weaken the CONGRESS threshold test — it already requires CONGRESS in ≥3 decks which is correct.
  </action>
  <verify>
    <automated>cd /Users/yevgenschweden/swiperisk && bun run test:unit 2>&1 | tail -20</automated>
  </verify>
  <done>software-engineer.ts has ≥16 annotated outcomes covering ≥4 death types; deathVectorCoverage.test.ts enforces 40% per deck and ≥4 types per deck; all coverage tests pass for all 10 decks</done>
</task>

</tasks>

<verification>
After both tasks complete:

```bash
cd /Users/yevgenschweden/swiperisk && bun run test:unit 2>&1
```

All deathVectorCoverage tests must pass with restored thresholds. This validates all 10 decks simultaneously (7 from 16-05 + 3 from this plan).

Spot check coverage per deck:
- head-of-something: 21 cards × 2 outcomes = 42 total; ≥17 annotated; ≥4 types
- chief-something-officer: 20 cards × 2 = 40 total; ≥16 annotated; ≥4 types
- software-engineer: 20 cards × 2 = 40 total; ≥16 annotated; ≥4 types
</verification>

<success_criteria>
- All 3 partial decks broadened to ≥40% outcome annotation, ≥4 death types
- deathVectorCoverage.test.ts thresholds restored: 40% coverage per deck, ≥4 types per deck
- bun run test:unit passes — all 10 decks pass restored coverage tests
- No TypeScript errors
- Existing annotations on congressional cards preserved (not overwritten)
</success_criteria>

<output>
After completion, create `.planning/phases/16-kobayashi-maru-ending-variety-system/16-06-SUMMARY.md`
</output>
