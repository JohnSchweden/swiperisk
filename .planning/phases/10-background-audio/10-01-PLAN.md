---
phase: 10-background-audio
plan: "01"
type: TDD
wave: 1
depends_on: []
files_modified:
  - services/gameAudio.ts
  - hooks/useGameAudio.ts
  - hooks/index.ts
  - services/voicePlayback.ts
  - unit/gameAudio.spec.ts
  - unit/voicePlayback.spec.ts
autonomous: true
must_haves:
  truths:
    - "Voice playback remains dominant when ambient music and non-voice cues are active."
    - "The app can start, stop, and swap non-voice audio layers without stacking broken loops across stage changes."
    - "Background audio works without requiring immediate external music or SFX sourcing."
  artifacts:
    - path: "services/gameAudio.ts"
      provides: "Shared procedural music and SFX engine with mix buses."
      exports: ["startStageLoop", "stopStageLoop", "playCue", "setMixState", "disposeGameAudio"]
    - path: "hooks/useGameAudio.ts"
      provides: "React lifecycle wrapper for stage audio orchestration and semantic cue triggering."
      exports: ["useGameAudio"]
    - path: "services/voicePlayback.ts"
      provides: "Voice playback integrated with music ducking and cue-safe restoration."
      contains: "setMixState"
  key_links:
    - from: "hooks/useGameAudio.ts"
      to: "services/gameAudio.ts"
      via: "hook delegates stage-loop and cue requests to the shared engine"
      pattern: "startStageLoop|playCue|stopStageLoop"
    - from: "services/voicePlayback.ts"
      to: "services/gameAudio.ts"
      via: "voice lifecycle ducks music and restores the mix after playback"
      pattern: "setMixState|duck|restore"
---

<objective>
Build the shared non-voice audio foundation for this phase so later plans can add stage music and interaction cues without rewriting playback logic or colliding with the existing voice system.

Purpose: Establish one reusable audio engine with a clear mix hierarchy before UI components start firing sounds.
Output: A procedural game-audio service, a React hook for orchestration, voice ducking support, and focused automated coverage for the mixer contract.
</objective>

<execution_context>
@/Users/yevgenschweden/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/yevgenschweden/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@App.tsx
@hooks/useVoicePlayback.ts
@services/voicePlayback.ts
@types.ts
@unit/voicePlayback.spec.ts
</context>

<tdd_config>
Use `type: execute`.

This phase is mostly runtime orchestration, browser-audio glue, and UI integration. Add automated coverage, but do not force a dedicated TDD plan split.
</tdd_config>

<tasks>

<task type="auto">
  <name>Task 1: Create a shared procedural game-audio engine with explicit mix buses</name>
  <files>services/gameAudio.ts, unit/gameAudio.spec.ts</files>
  <action>Create `services/gameAudio.ts` as the single non-voice audio engine for the app. Use Web Audio API primitives instead of external asset files so the phase can ship without immediate licensing or sourcing work. Implement separate gain stages for music and cues, semantic stage-loop presets for menu/game-flow moods, semantic cue presets for UI interactions, idempotent `startStageLoop`/`stopStageLoop` behavior, safe resume on first user gesture, and cleanup that prevents orphaned nodes or duplicate loops after repeated stage changes. Add focused unit coverage that validates stage-loop swapping, cue playback entry points, and cleanup/mix state behavior. Avoid coupling this service to specific React components or hardcoded DOM listeners.</action>
  <verify>`bun run typecheck && bun test unit/gameAudio.spec.ts`</verify>
  <done>`services/gameAudio.ts` exposes the shared audio API, stage loops can be swapped without overlap, cue playback uses semantic names instead of component-specific branches, and automated tests cover the engine contract.</done>
</task>

<task type="auto">
  <name>Task 2: Wire voice-priority ducking and React orchestration on top of the engine</name>
  <files>hooks/useGameAudio.ts, hooks/index.ts, services/voicePlayback.ts, unit/voicePlayback.spec.ts</files>
  <action>Create `useGameAudio` as the React-facing wrapper that owns stage-loop lifecycle and exposes stable semantic cue helpers for components. Update `services/voicePlayback.ts` so voice playback ducks music and, if needed, lightly attenuates cues while voice is active, then restores the mix on `ended`, stop, error, and unmount paths. Preserve existing voice-file behavior and radio effect handling; do not regress stage-triggered voice playback. Extend `unit/voicePlayback.spec.ts` to assert the new mix-priority behavior and restoration paths.</action>
  <verify>`bun run typecheck && bun test unit/gameAudio.spec.ts unit/voicePlayback.spec.ts`</verify>
  <done>The app has a reusable hook for non-voice audio, voice playback reliably takes priority over music, and mix restoration is covered for both normal completion and interrupted playback.</done>
</task>

</tasks>

<verification>
1. Run `bun run typecheck`.
2. Run `bun run build`.
3. Run `bun test unit/gameAudio.spec.ts unit/voicePlayback.spec.ts`.
4. Confirm the implementation does not require new external audio assets to satisfy baseline loop/cue behavior.
</verification>

<success_criteria>
- A shared non-voice audio engine exists and is not duplicated across components.
- Voice playback ducks background music so AUDIO-04 is enforceable before UI plans land.
- The implementation path for later plans uses semantic stage/cue APIs rather than ad hoc `new Audio()` calls in components.
</success_criteria>

<output>
After completion, create `.planning/phases/10-background-audio/10-01-SUMMARY.md`
</output>
