---
phase: 17-shuffle-aware-feedback-tts-fixes
plan: "04"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/feedbackAudioChoice.ts
  - App.tsx
  - hooks/useVoicePlayback.ts
  - unit/feedbackAudioChoice.test.ts
autonomous: true
gap_closure: true
requirements:
  - FA-01
  - FA-02

must_haves:
  truths:
    - "`authoringFeedbackStem` returns the slugified outcome label, not `'left'`/`'right'`"
    - "`feedbackAuthoringStem` type is `string | null` throughout the call chain (App → useVoicePlayback)"
    - "Unit tests pass with label-slug expected values"
    - "`feedbackVoiceTrigger` in `useVoicePlayback.ts` constructs the correct filename using the slug"
  artifacts:
    - path: lib/feedbackAudioChoice.ts
      provides: "authoringFeedbackStem returning string slug; slugify exported"
      exports: ["authoringFeedbackStem", "slugify", "PresentationChoiceSlot"]
    - path: unit/feedbackAudioChoice.test.ts
      provides: "Unit test coverage for slug return values"
      contains: "shield-the-team"
  key_links:
    - from: App.tsx
      to: useVoicePlayback
      via: "feedbackAuthoringStem: string | null"
      pattern: "feedbackAuthoringStem.*string"
    - from: useVoicePlayback.ts
      to: public/audio/voices/roaster/feedback/
      via: "feedbackVoiceTrigger(cardId, authoringStem)"
      pattern: "feedback_\\$\\{cardId\\}_\\$\\{authoringStem\\}"
---

<objective>
Migrate `authoringFeedbackStem` to return slugified outcome labels instead of `"left"|"right"`, and update all TypeScript callers to use `string` instead of the narrow `"left" | "right"` union.

Purpose: Audio filenames will be renamed from `_left`/`_right` to `_${labelSlug}` in plan 17-05. This plan updates the code to produce matching slugs before the assets are renamed.

Output: `lib/feedbackAudioChoice.ts` exports `slugify` and returns label slugs; `App.tsx` and `useVoicePlayback.ts` accept `string`; unit tests verify slug values.
</objective>

<execution_context>
@/Users/yevgenschweden/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yevgenschweden/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-shuffle-aware-feedback-tts-fixes/17-03-SUMMARY.md

<interfaces>
<!-- Current lib/feedbackAudioChoice.ts -->
```typescript
export type PresentationChoiceSlot = "LEFT" | "RIGHT";

export function authoringFeedbackStem(
  card: Pick<Card, "choiceSidesSwapped">,
  selectedPresentationSlot: PresentationChoiceSlot,
): "left" | "right"
```

<!-- Current App.tsx FeedbackOverlayState (line 105-115) -->
```typescript
type FeedbackOverlayState = {
  cardId: string;
  // ...
  feedbackAuthoringStem: "left" | "right";
};
```

<!-- Current useVoicePlayback.ts options interface (line 10-18) -->
```typescript
interface UseVoicePlaybackOptions {
  // ...
  feedbackAuthoringStem?: "left" | "right" | null;
}

function feedbackVoiceTrigger(
  cardId: string,
  authoringStem: "left" | "right",
): string {
  if (CRITICAL_HOS_CARDS.has(cardId)) {
    return `feedback_${cardId}_${authoringStem}`;
  }
  // ...
}
```

<!-- data/imageMap.ts slugify (canonical implementation) -->
```typescript
export function slugify(text: string): string {
  return text
    .toLowerCase()
    .replace(/[^a-z0-9]+/g, "-")
    .replace(/^-|-$/g, "");
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate authoringFeedbackStem to return label slugs</name>
  <files>lib/feedbackAudioChoice.ts, unit/feedbackAudioChoice.test.ts</files>
  <behavior>
    - Test: swapped=false, slot=LEFT, card.onLeft.label="Shield the team" → "shield-the-team"
    - Test: swapped=false, slot=RIGHT, card.onLeft.label="Shield the team", card.onRight.label="Give names to compliance" → "give-names-to-compliance"
    - Test: swapped=true, slot=LEFT (shows authoring-right), card.onRight.label="Give names to compliance" → "give-names-to-compliance"
    - Test: swapped=true, slot=RIGHT (shows authoring-left), card.onLeft.label="Shield the team" → "shield-the-team"
    - Test: swapped=undefined, slot=LEFT → authoring left label slug
    - Test: slugify("Take the blame") → "take-the-blame"
    - Test: slugify("Promise the impossible") → "promise-the-impossible"
    - Test: slugify("Side with auditors") → "side-with-auditors"
  </behavior>
  <action>
    In `lib/feedbackAudioChoice.ts`:
    1. Add `slugify` function — copy exactly from `data/imageMap.ts` (do NOT import from there; imageMap is data layer, feedbackAudioChoice is lib layer):
       ```typescript
       export function slugify(text: string): string {
         return text.toLowerCase().replace(/[^a-z0-9]+/g, "-").replace(/^-|-$/g, "");
       }
       ```
    2. Change `authoringFeedbackStem` signature:
       - Input: `Pick<Card, "choiceSidesSwapped" | "onLeft" | "onRight">` (needs labels)
       - Return type: `string` (not `"left" | "right"`)
    3. Logic stays IDENTICAL — swap detection is unchanged. Only the return value changes:
       - Was: `return selectedPresentationSlot === "LEFT" ? "left" : "right"` (and swapped inverse)
       - Now: when unswapped LEFT → `slugify(card.onLeft.label)`, unswapped RIGHT → `slugify(card.onRight.label)`
       - When swapped: LEFT slot shows authoring-right → `slugify(card.onRight.label)`, RIGHT slot shows authoring-left → `slugify(card.onLeft.label)`

    In `unit/feedbackAudioChoice.test.ts`:
    1. Update the `card()` helper to accept `onLeft` and `onRight` labels:
       ```typescript
       function card(swapped: boolean | undefined, leftLabel = "Left Option", rightLabel = "Right Option") {
         return {
           ...(swapped !== undefined ? { choiceSidesSwapped: swapped } : {}),
           onLeft: { label: leftLabel } as any,
           onRight: { label: rightLabel } as any,
         };
       }
       ```
    2. Replace all `expected: "left" | "right"` with actual slug strings.
    3. Add test cases for `slugify` with real card labels.
    4. Use `shadow_ai_hos_1` labels as concrete fixtures: onLeft.label="Shield the team", onRight.label="Give names to compliance".

    Run: `bun run test:unit -- --reporter=verbose unit/feedbackAudioChoice.test.ts` — must pass before proceeding.
  </action>
  <verify>
    <automated>bun run test:unit -- unit/feedbackAudioChoice.test.ts</automated>
  </verify>
  <done>All unit tests pass; `authoringFeedbackStem` returns slugified label strings; `slugify` is exported from `lib/feedbackAudioChoice.ts`.</done>
</task>

<task type="auto">
  <name>Task 2: Update App.tsx and useVoicePlayback.ts type signatures</name>
  <files>App.tsx, hooks/useVoicePlayback.ts</files>
  <action>
    **App.tsx** (line ~110):
    - Change `feedbackAuthoringStem: "left" | "right"` → `feedbackAuthoringStem: string` in `FeedbackOverlayState` type.
    - The call `authoringFeedbackStem(card, direction)` now returns `string` — no other App.tsx changes needed since `card` already has `onLeft`/`onRight` from the `Card` type. BUT the call signature now needs `card` to satisfy `Pick<Card, "choiceSidesSwapped" | "onLeft" | "onRight">` — verify the card object passed already has these fields (it does: `card` comes from `effectiveDeck[currentCardIndex]` which is `Card`).

    **hooks/useVoicePlayback.ts**:
    - Change `feedbackAuthoringStem?: "left" | "right" | null` → `feedbackAuthoringStem?: string | null` in `UseVoicePlaybackOptions` interface (line 15).
    - Change `feedbackVoiceTrigger(cardId: string, authoringStem: "left" | "right")` → `feedbackVoiceTrigger(cardId: string, authoringStem: string)` (line 97).
    - The internal `feedbackVoiceTrigger` body still constructs `feedback_${cardId}_${authoringStem}` for CRITICAL_HOS_CARDS — this is correct since authoringStem is now the label slug. No logic change needed, only type widening.
    - The `FEEDBACK_INSTALL_ON_RIGHT` branch still compares `authoringStem === "right"` — this MUST change. After this migration, `authoringStem` is never `"right"` for those cards; it's a slug. BUT those non-HoS cards (se_code_quality_refactor etc.) are NOT in CRITICAL_HOS_CARDS and use generic fallback clips. The `FEEDBACK_INSTALL_ON_RIGHT` logic uses `authoringStem === "right"` to determine which generic clip. After this change, `authoringStem` will be a label slug (e.g., `"add-unit-tests"`) not `"right"`. So this comparison MUST be updated.

    For `FEEDBACK_INSTALL_ON_RIGHT` cards and `se_security_patch_timeline`: The generic fallback clips (feedback_paste, feedback_debug, feedback_install, feedback_ignore) are NOT being renamed. These cards pass `authoringStem` from the same `authoringFeedbackStem` call — which now returns a label slug. Update `feedbackVoiceTrigger` to handle this: for non-critical cards, determine left vs right by checking if the stem matches the authoring-left label. But we don't have card data in `feedbackVoiceTrigger`.

    Simplest fix: add a `isLeft` boolean alongside the stem, OR change approach. The cleanest solution is to pass the `PresentationChoiceSlot` separately, but that changes the interface. Instead: these non-critical cards use generic clips, so the mapping is determined by the ORIGINAL slot, not the label. The `feedbackAuthoringStem` in `FeedbackOverlayState` is now a string — we can also store the raw slot. BUT that requires App.tsx changes.

    ACTUALLY — read the existing `feedbackVoiceTrigger` carefully. The FEEDBACK_INSTALL_ON_RIGHT comparison `authoringStem === "right"` was checking whether the authoring arm was right. After the migration, the authoring arm label slug is passed instead. For FEEDBACK_INSTALL_ON_RIGHT cards, we need to know whether the player chose the "install" outcome (right) or the "ignore" outcome (left). Since `authoringFeedbackStem` now returns the label slug, we can't compare to `"right"` anymore.

    Solution: Store BOTH the stem (label slug) AND the original PresentationChoiceSlot in FeedbackOverlayState. Update App.tsx to store `selectedSlot: PresentationChoiceSlot` in the overlay state. Pass it through to `feedbackVoiceTrigger`. This preserves the non-critical card behavior without adding complexity.

    Concrete changes:
    1. In App.tsx, add `selectedSlot: PresentationChoiceSlot` to `FeedbackOverlayState`. When setting the overlay state (after `handleChoice` / swipe), store the `direction` as `selectedSlot`.
    2. Import `PresentationChoiceSlot` from `lib/feedbackAudioChoice`.
    3. Pass `selectedSlot` through to `feedbackVoiceTrigger` via `useVoicePlayback` options.
    4. In `useVoicePlayback.ts`, add `feedbackSelectedSlot?: PresentationChoiceSlot | null` to options, update `feedbackVoiceTrigger` to take it: `feedbackVoiceTrigger(cardId, authoringStem, selectedSlot)`.
    5. Replace `authoringStem === "right"` with `selectedSlot === "RIGHT"` in the fallback branches.
    6. Update the useEffect dependency array to include `feedbackSelectedSlot`.

    Run `bun run typecheck` — must pass with 0 errors before marking done.
  </action>
  <verify>
    <automated>bun run typecheck</automated>
  </verify>
  <done>TypeScript compiles cleanly. `feedbackAuthoringStem` is `string` throughout. `feedbackVoiceTrigger` correctly uses label slug for CRITICAL_HOS_CARDS and slot for fallback cards. `bun run typecheck` exits 0.</done>
</task>

</tasks>

<verification>
- `bun run typecheck` passes with 0 errors
- `bun run test:unit -- unit/feedbackAudioChoice.test.ts` passes
- `bun run lint` passes (no Biome errors)
</verification>

<success_criteria>
- `authoringFeedbackStem(card, "LEFT")` where `card.onLeft.label = "Shield the team"` returns `"shield-the-team"`
- `FeedbackOverlayState.feedbackAuthoringStem` is typed as `string`
- `UseVoicePlaybackOptions.feedbackAuthoringStem` is typed as `string | null`
- All unit tests in `unit/feedbackAudioChoice.test.ts` pass
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/17-shuffle-aware-feedback-tts-fixes/17-04-SUMMARY.md`
</output>
