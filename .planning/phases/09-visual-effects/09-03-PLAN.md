---
phase: 09-visual-effects
plan: "03"
type: TDD
wave: 2
depends_on: ["01"]
files_modified:
  - components/game/IntroScreen.tsx
  - components/game/PersonalitySelect.tsx
  - components/game/RoleSelect.tsx
  - components/game/GameOver.tsx
  - components/game/SummaryScreen.tsx
  - components/game/Taskbar.tsx
autonomous: true
must_haves:
  truths:
    - "Menu and end-state screens feel alive through subtle ambient motion instead of sitting completely static."
    - "Ambient screen effects support atmosphere and readability without turning the UI into a seizure-powered casino."
    - "Persistent chrome like the taskbar carries a small amount of ambient life without distracting from gameplay."
    - "prefers-reduced-motion respected: optional ambient flourishes gate behind usePrefersReducedMotion; core UI remains."
  artifacts:
    - path: components/game/IntroScreen.tsx
      provides: "Ambient intro motion around the main title and primary CTA."
      contains: "Boot system"
    - path: components/game/PersonalitySelect.tsx
      provides: "Subtle card-entry or idle motion for personality selection."
      contains: "data-testid"
    - path: components/game/RoleSelect.tsx
      provides: "Subtle card-entry or idle motion for role selection."
      contains: "data-testid"
    - path: components/game/GameOver.tsx
      provides: "Failure-state ambient motion distinct from Phase 04 stress indicators."
      contains: "Reboot system"
    - path: components/game/SummaryScreen.tsx
      provides: "Success-state ambient motion that feels celebratory but restrained."
      contains: "Log off"
    - path: components/game/Taskbar.tsx
      provides: "Low-intensity persistent chrome motion or pulse treatment."
      contains: "currentTime"
  key_links:
    - from: components/game/IntroScreen.tsx
      to: components/game/Taskbar.tsx
      via: "ambient visual language feels consistent between major menu screen and persistent chrome"
      pattern: "pulse|glow|ambient"
    - from: components/game/PersonalitySelect.tsx
      to: components/game/RoleSelect.tsx
      via: "selection screens share the same ambient card treatment and timing vocabulary"
      pattern: "animationDelay|hover"
    - from: components/game/GameOver.tsx
      to: components/game/SummaryScreen.tsx
      via: "end-state screens each get mood-specific ambient treatment without reusing the same effect blindly"
      pattern: "success|failure|ambient"
---

<objective>
Add the ambient/menu motion pass for Phase 09 so the non-playing screens feel polished, atmospheric, and intentionally alive without borrowing the high-stress effects reserved for Phase 04.

Purpose: Deliver VFX-03 across the game's static-feeling screens while preserving legibility and keeping persistent motion subtle.
Output: Ambient animation treatments for menu, end-state, and taskbar UI with consistent timing and restrained intensity.
</objective>

<execution_context>
@/Users/yevgenschweden/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/yevgenschweden/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@components/game/IntroScreen.tsx
@components/game/PersonalitySelect.tsx
@components/game/RoleSelect.tsx
@components/game/GameOver.tsx
@components/game/SummaryScreen.tsx
@components/game/Taskbar.tsx
@tests/stage-snapshots.spec.ts
</context>

<tdd_config>
Use `type: execute`.

This plan is a multi-screen polish pass backed by visual regression, not a separate TDD candidate.
</tdd_config>

<tasks>

<task type="auto">
  <name>Task 1: Add restrained ambient motion to intro and selection screens</name>
  <files>components/game/IntroScreen.tsx, components/game/PersonalitySelect.tsx, components/game/RoleSelect.tsx</files>
  <action>Gate optional ambient flourishes behind usePrefersReducedMotion (from 09-01); if prefersReducedMotion return early or render without decorative motion. Enhance the intro and selection screens with subtle ambient motion such as layered glows, gentle floating, idle shimmer, or controlled card-entry sequencing that fits the current K-Maru aesthetic. Animate only transform and opacity; avoid layout-heavy properties (width, height, left, top, box-shadow, background). Keep the motion readable, avoid constant large-amplitude transforms, and preserve existing click targets, data-testid values, and copy. Reuse the app's existing animation language. No framer-motion or react-spring — CSS-only. Add stable hooks on animated wrappers for verification.</action>
  <verify>`bun run typecheck && bun run build && bunx playwright test tests/stage-snapshots.spec.ts --project=chromium-desktop --project=chromium-mobile`</verify>
  <done>The intro, personality select, and role select screens each have noticeable but restrained ambient motion, and the screens remain readable and snapshot-stable across desktop and mobile.</done>
</task>

<task type="auto">
  <name>Task 2: Add mood-specific ambient polish to end states and persistent chrome</name>
  <files>components/game/GameOver.tsx, components/game/SummaryScreen.tsx, components/game/Taskbar.tsx</files>
  <action>Gate optional flourishes behind usePrefersReducedMotion. Give `GameOver.tsx` and `SummaryScreen.tsx` distinct ambient motion treatments that match their moods: failure grim and reactive without stress-indicator patterns, success lightly celebratory without confetti spam. Extend `Taskbar.tsx` with very low-intensity persistent polish (pulse, scan, icon glow). Animate only transform and opacity. Keep motion intensity below the threshold where it competes with card reading or HUD scanning.</action>
  <verify>`bun run typecheck && bun run build && bunx playwright test tests/stage-snapshots.spec.ts`</verify>
  <done>End-state screens have distinct ambient moods, the taskbar carries subtle life, and none of these changes overwhelm the core UI or introduce obvious animation noise.</done>
</task>

</tasks>

<verification>
1. Run `bun run typecheck`.
2. Run `bun run build`.
3. Run `bunx playwright test tests/stage-snapshots.spec.ts`.
4. Confirm ambient motion remains subtle, readable, and distinct from stress-indicator work reserved for Phase 04.
5. Confirm optional ambient flourishes gate behind usePrefersReducedMotion; core UI remains when reduced. Animations use transform + opacity only. No framer-motion or react-spring.
</verification>

<success_criteria>
- VFX-03 is satisfied across menu and end-state screens, not just the main gameplay card.
- Ambient motion is visible but restrained enough to preserve readability and existing interaction affordances.
- Persistent chrome gains subtle polish without becoming a constant distraction.
</success_criteria>

<output>
After completion, create `.planning/phases/09-visual-effects/09-03-SUMMARY.md`
</output>
