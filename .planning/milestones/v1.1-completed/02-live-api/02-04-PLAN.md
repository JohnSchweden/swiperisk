---
phase: 02-live-api
plan: 04
type: TDD
wave: 4
depends_on: ["02-03"]
files_modified:
  - tests/live-api-latency.spec.ts
autonomous: false
user_setup: []

must_haves:
  truths:
    - "First token audio plays faster than standard TTS"
    - "Audio plays at correct speed (not sped up)"
    - "Fallback works when Live API unavailable"
  artifacts:
    - path: "tests/live-api-latency.spec.ts"
      provides: "Playwright test for streaming audio latency"
      measures: ["time-to-first-audio", "audio-playback-speed"]
  key_links:
    - from: "tests/live-api-latency.spec.ts"
      to: "services/roastService"
      via: "Roast workflow execution"
---

<objective>
Test real-time audio latency and verify streaming is faster than standard TTS (direct browser connection).

Purpose: Confirm VOICE-10 success criteria - first token audio plays faster than current approach.

Output: Test results showing latency improvement and audio quality.
</objective>

<execution_context>
@/Users/yevgenschweden/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/02-live-api/02-03-SUMMARY.md

# Tests the complete integration from Plans 1-3:
# - Direct service: services/geminiLive.ts (direct browser with ephemeral tokens)
# - AudioWorklet: public/audio-processor.worklet.js
# - Hook: hooks/useLiveAudio.ts
# - Service: services/roastService.ts with fallback
# - Integration: src/App.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create latency test for streaming audio</name>
  <files>tests/live-api-latency.spec.ts</files>
  <action>
Create Playwright test at `tests/live-api-latency.spec.ts` to measure and verify streaming audio latency (direct browser connection).

Test must include:
1. Navigate to app and start Roast.exe workflow
2. Measure time from "roast" request to first audio sound
3. Compare with baseline (standard TTS waiting for full response)
4. Verify audio plays at correct speed (not sped up 2x)
5. Verify fallback works when Live API unavailable (mock failure)

Measurements to capture:
- timeToFirstAudio: milliseconds from request to first sound
- totalAudioDuration: how long the audio plays
- audioSpeedCorrect: boolean (not 2x speed)
- fallbackTriggered: boolean (if Live API failed)

Baseline to beat:
- Current standard TTS: waits for full response (~2-5 seconds) then plays
- Target: first audio within 500ms-1000ms of request

Test approach:
- Use Web Audio API to detect when audio starts playing
- Or verify timing with a known test prompt
- Mock Gemini API responses for deterministic testing
- Use page.evaluate() to access AudioContext and detect playback

Note: This test may need to mock the Live API responses since real API calls are non-deterministic. Focus on:
1. Verifying the streaming flow works (chunks arrive and play)
2. Verifying fallback triggers on error
3. Verifying audio doesn't play at 2x speed
  </action>
  <verify>
Test file created at tests/live-api-latency.spec.ts and runs without errors
</verify>
  <done>
Latency test created that measures streaming audio performance (direct browser connection)
</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify streaming audio performance</name>
  <what-built>Complete Live API implementation with streaming and fallback (direct browser connection)</what-built>
  <how-to-verify>
Manual verification steps:
1. Start the app with `npm run dev`
2. Open browser to http://localhost:5173
3. Select a personality (e.g., Roaster)
4. Enter a workflow to roast
5. Measure time from submit to audio playing:
   - Should be under 2 seconds (vs 3-5 seconds for standard TTS)
6. Verify audio plays at normal speed (not chipmunk voice)
7. Test fallback: disconnect network or mock failure, verify TTS still works

Run the test:
```bash
bunx playwright test tests/live-api-latency.spec.ts
```

Expected results:
- First audio plays faster than blocking TTS
- Audio sounds normal (not sped up)
- Fallback to TTS works when Live API fails
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
Tests pass and manual verification confirms:
- Streaming audio starts faster than standard TTS (direct browser connection)
- Audio plays at correct speed (24kHz→48kHz conversion works)
- Fallback to standard TTS works
</verification>

<success_criteria>
1. First token audio plays faster than standard TTS approach (direct browser connection)
2. Audio plays at correct speed (not sped up)
3. Fallback to standard TTS works when Live API unavailable
</success_criteria>

<output>
After completion, create `.planning/phases/02-live-api/02-04-SUMMARY.md`
</output>
