---
phase: 17-shuffle-aware-feedback-tts-fixes
plan: 02
type: execute
wave: 2
depends_on:
  - "17-01"
files_modified:
  - App.tsx
  - hooks/useVoicePlayback.ts
  - components/game/FeedbackOverlay.tsx
  - unit/feedbackOverlay.spec.tsx
  - hooks/useWebMCPTools.ts
autonomous: true
requirements:
  - FA-02

must_haves:
  truths:
    - "Feedback overlay carries the authoring feedback stem (or equivalent) used for Roaster TTS triggers — derived from which visible choice was taken, not raw direction semantics alone"
    - "useVoicePlayback uses that stem when resolving `feedbackVoiceTrigger` for card-backed clips"
    - "Incident pressure sees the same card as CardStack during PLAYING"
  artifacts:
    - path: "App.tsx"
      provides: "effectiveDeck-based currentCard; feedbackAuthoringStem (left|right) in overlay state from authoringFeedbackStem(card, selectedPresentationSlot); applyChoice sets it when opening overlay"
    - path: "hooks/useVoicePlayback.ts"
      provides: "optional feedbackAuthoringStem (or prop name) drives per-card feedback trigger suffix when present"
  key_links:
    - from: "App.tsx"
      to: "hooks/useVoicePlayback.ts"
      via: "feedbackAuthoringStem prop (or equivalent)"
    - from: "App.tsx"
      to: "lib/feedbackAudioChoice.ts"
      via: "authoringFeedbackStem(card, choice) before setFeedbackOverlay; choice = selected presentation slot"
---

<objective>
Wire **choice/outcome-aware** feedback audio into the playing loop: the clip matches the **authoring arm** of the **outcome in the slot the player chose**. Align pressure/timer metadata with the shuffled deck card instance.
</objective>

<execution_context>
@/Users/yevgenschweden/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yevgenschweden/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-shuffle-aware-feedback-tts-fixes/17-CONTEXT.md
@App.tsx
@hooks/useVoicePlayback.ts
@hooks/useIncidentPressure.ts
@components/game/GameScreen.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: currentCard from effectiveDeck</name>
  <files>App.tsx</files>
  <behavior>
    - When `stage === PLAYING` and `role` set, `cards = state.effectiveDeck ?? ROLE_CARDS[state.role]`, `currentCard = cards[state.currentCardIndex] ?? null`.
    - Remove incorrect `ROLE_CARDS[state.role][state.currentCardIndex]`-only path for playing pressure.
  </behavior>
  <action>
    1. Locate `currentCard` derivation in `App.tsx`.
    2. Replace with `effectiveDeck ?? ROLE_CARDS[role]` indexing; guard null deck/role.
  </action>
  <done>
    - `useIncidentPressure` receives the same `Card` instance (including `choiceSidesSwapped` and current `onLeft`/`onRight` outcomes) as the one rendered in `CardStack`.
  </done>
  <verify>
    <automated>bun run typecheck</automated>
    <automated>Optional: small Vitest asserting playing `currentCard` is `effectiveDeck[index]` when deck set — see plan-checker note</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Overlay + voice playback</name>
  <files>App.tsx, hooks/useVoicePlayback.ts</files>
  <behavior>
    - In `applyChoice`, treat `direction` / `choice` as **selected presentation slot** (LEFT = chose the left visible `ChoiceOutcome`, RIGHT = chose the right).
    - Call `authoringFeedbackStem(card, direction)` (or same types as game choice) and pass the resulting `'left'|'right'` into playback (e.g. `feedbackAuthoringStem` on overlay state + hook prop).
    - Add overlay field such as `feedbackAuthoringStem?: 'left' | 'right'` (names may vary; keep TypeScript honest). Pass to `useVoicePlayback` so `feedbackVoiceTrigger` uses **authoring stem for the chosen outcome**, not the raw slot string for per-card files.
    - Keep stored **game choice** on overlay/history as `LEFT` | `RIGHT` for branching, debrief, and `resolveDeckWithBranching` — unchanged semantics; only the **audio trigger path** is outcome-arm keyed.
  </behavior>
  <action>
    1. Extend overlay state type and `setFeedbackOverlay` payload in `applyChoice`; compute stem via `lib/feedbackAudioChoice.ts`.
    2. Add hook option; when building roaster card feedback trigger, prefer authoring stem (or build trigger string from `feedback_${id}_${stem}` inside hook).
    3. Ensure Roaster-only guard unchanged.
  </action>
  <done>
    - With `choiceSidesSwapped === true`, choosing the **left visible outcome** loads the clip whose suffix matches the **authoring arm** of that outcome (same text the player saw), not the wrong arm’s file.
  </done>
  <verify>
    <automated>bun run test:smoke</automated>
    <automated>bun run test:area:audio</automated>
    <automated>Optional: unit assert on trigger string for swapped deck fixture</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: FeedbackOverlay / WebMCP if needed</name>
  <files>components/game/FeedbackOverlay.tsx, hooks/useWebMCPTools.ts, unit/feedbackOverlay.spec.tsx</files>
  <behavior>
    - Only change if something reads overlay shape; update unit tests if props change.
  </behavior>
  <action>
    1. Grep for `FeedbackOverlayState` / overlay props; adjust only if TypeScript requires new fields on child components.
  </action>
  <done>
    - `bun run typecheck` clean; FeedbackOverlay tests pass or unchanged with no prop drift.
  </done>
  <verify>
    <automated>bun run vitest:run -- unit/feedbackOverlay.spec.tsx</automated>
  </verify>
</task>

</tasks>
