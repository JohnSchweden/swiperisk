---
phase: 10-background-audio
plan: "03"
type: TDD
wave: 2
depends_on: ["01"]
files_modified:
  - components/game/GameScreen.tsx
  - components/game/CardStack.tsx
  - components/game/FeedbackOverlay.tsx
  - tests/card-audio.spec.ts
autonomous: true
must_haves:
  truths:
    - "Card interactions produce distinct non-voice cues for action commit and card progression."
    - "Outcome reveal and progression sounds complement the existing voice commentary instead of drowning it out."
    - "Rapid gameplay interaction does not spam broken overlapping SFX."
  artifacts:
    - path: "components/game/CardStack.tsx"
      provides: "Swipe/button interaction cues for the incident card."
      contains: "onSwipeLeft"
    - path: "components/game/FeedbackOverlay.tsx"
      provides: "Outcome progression cue when advancing past feedback."
      contains: "onNext"
    - path: "components/game/GameScreen.tsx"
      provides: "Gameplay-level audio wiring for card interaction semantics."
      contains: "playCue"
    - path: "tests/card-audio.spec.ts"
      provides: "Playwright coverage for gameplay cue behavior."
  key_links:
    - from: "components/game/CardStack.tsx"
      to: "services/gameAudio.ts"
      via: "button clicks and committed swipe actions trigger semantic card cues"
      pattern: "playCue|swipe|card"
    - from: "components/game/FeedbackOverlay.tsx"
      to: "services/gameAudio.ts"
      via: "Next ticket progression triggers a reveal/advance cue"
      pattern: "playCue|next|reveal"
    - from: "components/game/GameScreen.tsx"
      to: "components/game/CardStack.tsx"
      via: "gameplay container passes stable audio-aware handlers to the card UI"
      pattern: "onSwipeLeft|onSwipeRight"
---

<objective>
Add the gameplay-specific non-voice cues for card handling so swipes, button commits, and ticket progression feel intentional without conflicting with the personality voice layer.

Purpose: Deliver AUDIO-03 while keeping gameplay cue logic local to the card flow and compatible with the mix hierarchy established in plan 01.
Output: Card interaction audio wiring plus regression coverage for gameplay cue behavior.
</objective>

<execution_context>
@/Users/yevgenschweden/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/yevgenschweden/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-background-audio/10-01-PLAN.md
@App.tsx
@components/game/GameScreen.tsx
@components/game/CardStack.tsx
@components/game/FeedbackOverlay.tsx
@hooks/useSwipeGestures.ts
</context>

<tdd_config>
Use `type: execute`.

This is interaction wiring around existing UI and gesture logic. Add regression tests, but keep the plan as a standard execution slice.
</tdd_config>

<tasks>

<task type="auto">
  <name>Task 1: Add semantic card-action and progression cues to gameplay</name>
  <files>components/game/GameScreen.tsx, components/game/CardStack.tsx, components/game/FeedbackOverlay.tsx</files>
  <action>Wire semantic cue playback into the card loop at the points users actually commit actions: left/right button clicks, successful swipe commits, and advancing from the feedback overlay to the next ticket. If the implementation needs one cue for commit and another for reveal/progression, keep those semantics explicit. Do not fire sound on every drag frame or duplicate cues for the same action path. Preserve current keyboard, touch, and mouse behavior.</action>
  <verify>`bun run typecheck && bun run build`</verify>
  <done>Gameplay interactions now emit intentional non-voice cues for commit and progression, with no drag-frame spam and no behavior regressions in the card flow.</done>
</task>

<task type="auto">
  <name>Task 2: Add deterministic browser coverage for gameplay cue behavior</name>
  <files>tests/card-audio.spec.ts</files>
  <action>Create a focused Playwright suite that validates cue triggering for at least one left/right card action and one feedback-overlay progression event. Use deterministic observability such as console markers, exposed semantic events, or test-safe state rather than relying on actual audio hardware output. Include a rapid-interaction assertion that proves the implementation does not emit duplicate cues for a single committed action.</action>
  <verify>`bunx playwright test tests/card-audio.spec.ts`</verify>
  <done>Gameplay audio behavior is regression-tested for button/swipe commits, overlay progression, and duplicate-trigger protection.</done>
</task>

</tasks>

<verification>
1. Run `bun run typecheck`.
2. Run `bun run build`.
3. Run `bunx playwright test tests/card-audio.spec.ts`.
4. Run `bunx playwright test` after this plan if executed in isolation from the rest of the phase.
</verification>

<success_criteria>
- AUDIO-03 is satisfied through card commit/progression cues in the core gameplay loop.
- Cue triggering is tied to committed interactions, not raw drag movement.
- Gameplay cue behavior is covered with deterministic browser automation.
</success_criteria>

<output>
After completion, create `.planning/phases/10-background-audio/10-03-SUMMARY.md`
</output>
