---
phase: 19-refactor-the-design
plan: "13"
type: execute
wave: 1
depends_on: []
files_modified:
  - components/game/FeedbackOverlay.tsx
  - components/game/debrief/DebriefPage2AuditTrail.tsx
autonomous: true
gap_closure: true
requirements: [DESIGN-01]

must_haves:
  truths:
    - "FeedbackOverlay shows Fine consequence always (fine amount or $0)"
    - "FeedbackOverlay shows Heat consequence always (current heat % with icon)"
    - "FeedbackOverlay shows Hype consequence always (current hype % with icon)"
    - "Consequences displayed in consistent order: Fine → Heat → Hype"
    - "DebriefPage2 audit entries show all three consequences in same order"

---

<objective>
Show all three consequence metrics (Fine, Heat, Hype) for every outcome in FeedbackOverlay, not just in the escalation banner when critical. Also ensure DebriefPage2 audit trail shows the same consequences in the same order for consistency.
</objective>

<execution_context>
@/Users/yevgenschweden/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/yevgenschweden/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md

# Gap Context
Currently:
1. FeedbackOverlay shows escalation banner ONLY when Budget/Heat/Hype are critical (>=85 or <2000000)
2. The checkmark/warning icon shows if fine > 0, but doesn't show the fine amount for zero-fine outcomes
3. Heat and Hype values are only shown when critical, not always

User wants:
- Always show Fine consequence (amount or $0)
- Always show Heat consequence (current %, with fire icon)
- Always Show Hype consequence (current %, with bullhorn icon)
- Order: Fine → Heat → Hype
- Same display in DebriefPage2 audit trail

Reference current FeedbackOverlay structure (lines 188-209):
- Icon (checkmark/warning based on fine > 0)
- Violation block (only if fine > 0)
- Lesson text

Add a "Consequences" section after the icon/violation and before the lesson text that shows all three metrics always.
</context>

<tasks>

<task type="auto">
  <name>Add always-visible consequences display to FeedbackOverlay</name>
  <files>components/game/FeedbackOverlay.tsx</files>
  <action>
Add a "Consequences" section after the icon/violation block (line 209) and before the lesson text (line 211):

Display three consequence rows in order:
1. Fine: "-$X" in red if fine > 0, or "$0" in green if fine === 0, with fa-coins icon
2. Heat: "X%" with color (red >=85, amber >=70, else green/cyan), with fa-fire icon
3. Hype: "X%" with color (red >=85, amber >=70, else cyan), with fa-bullhorn icon

Use consistent styling: small text, inline-flex with icon, color-coded by severity.

Example structure:
```
<div className="flex justify-center gap-4 mb-4 text-xs">
  <span className={fine > 0 ? "text-red-400" : "text-green-400"}>
    <i className="fa-solid fa-coins"></i> {fine > 0 ? `-${formatBudget(fine)}` : '$0'}
  </span>
  <span className={heatColor}>
    <i className="fa-solid fa-fire"></i> {heat}%
  </span>
  <span className={hypeColor}>
    <i className="fa-solid fa-bullhorn"></i> {hype}%
  </span>
</div>
```

This appears for EVERY outcome, regardless of whether metrics are critical.
  </action>
  <verify>Typecheck passes, consequences section visible in all outcomes</verify>
  <done>FeedbackOverlay always displays Fine, Heat, and Hype consequences in order</done>
</task>

<task type="auto">
  <name>Update DebriefPage2 audit entries to show same consequences</name>
  <files>components/game/debrief/DebriefPage2AuditTrail.tsx</files>
  <action>
Find the audit entry rendering in DebriefPage2AuditTrail.tsx and ensure each audit entry displays the three consequences in the same order: Fine → Heat → Hype.

Check current audit entry structure (likely showing fine, heat, hype via outcome data). Add any missing consequence displays to match FeedbackOverlay.

The ForkSegment or fork UI should show for each choice: the fine/heat/hype delta or current values.
  </action>
  <verify>Typecheck passes, audit entries show Fine → Heat → Hype</verify>
  <done>DebriefPage2 shows same consequence order as FeedbackOverlay</done>
</task>

</tasks>

<verification>
- [ ] Typecheck passes
- [ ] FeedbackOverlay shows Fine always
- [ ] FeedbackOverlay shows Heat always
- [ ] FeedbackOverlay shows Hype always
- [ ] Order is Fine → Heat → Hype
- [ ] DebriefPage2 audit entries match
</verification>

<success_criteria>
All outcomes show Fine/Heat/Hype consequences in consistent order across both FeedbackOverlay and DebriefPage2.
</success_criteria>

<output>
After completion, create `.planning/phases/19-refactor-the-design/19-13-SUMMARY.md`
</output>
