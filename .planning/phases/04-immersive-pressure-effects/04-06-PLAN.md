---
phase: 04-immersive-pressure-effects
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - hooks/useCountdown.ts
autonomous: true
gap_closure: true
requirements:
  - IMMERSE-01

must_haves:
  truths:
    - "Timer expiry resolves incident after countdown reaches zero; no restart"
  artifacts:
    - path: "hooks/useCountdown.ts"
      provides: "Countdown with onComplete on natural expiry only"
  key_links:
    - from: "hooks/useCountdown.ts"
      to: "onComplete"
      via: "Called only when count reached 0 after ticking (hasTickedWhileActive)"
---

<objective>
Fix timer expiry: call onComplete when countdown naturally reaches zero, not restart. The effect currently treats natural expiry (count 1→0) and fresh activation (isActive false→true, count 0) identically — both hit count===0 && startFrom>0 and restart.
</objective>

<context>
@.planning/phases/04-immersive-pressure-effects/04-UAT.md

Round 2 Gap (test 2): User reported "timer just restarts" instead of auto-resolving.
Root cause: useCountdown treats natural expiry same as fresh activation. Both hit count===0 && startFrom>0 and reset; onComplete never called.
</context>

<interfaces>
From hooks/useCountdown.ts:
- useCountdown({ startFrom, onComplete, isActive }) → { count, reset }
- Effect: when !isActive → setCount(startFrom), return. When count===0 && startFrom>0 → currently setCount(startFrom); should be: if natural expiry → onComplete(), else setCount(startFrom)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Distinguish natural expiry from fresh activation</name>
  <files>hooks/useCountdown.ts</files>
  <action>
Add a ref hasTickedWhileActive to distinguish:
- Fresh activation: isActive just turned true, count is 0, we haven't ticked → setCount(startFrom), do NOT call onComplete
- Natural expiry: count reached 0 after ticking 1→0 → call onComplete, do NOT restart

Implementation:
1. Add useRef&lt;boolean&gt;(false) for hasTickedWhileActive
2. In effect, when !isActive: setCount(startFrom), hasTickedWhileActive.current = false, return
3. When count === 0 && startFrom &gt; 0: if hasTickedWhileActive.current then onComplete(); else setCount(startFrom). Return in both cases
4. When count &gt; 0: set hasTickedWhileActive.current = true before scheduling setTimeout. This marks that we will tick; when tick fires and count becomes 0, next effect run sees hasTickedWhileActive true → onComplete
5. When count === 0 && startFrom === 0: onComplete() (unchanged)
</action>
  <verify>
    <automated>bunx playwright test tests/immersive-pressure-cues.spec.ts -x</automated>
  </verify>
  <done>Let countdown reach zero on urgent card → incident auto-resolves to configured outcome; no restart.</done>
</task>

</tasks>

<verification>
Urgent card: start game with Dev role, first card (dev_1). Let timer reach 0 without swiping. Feedback overlay shows with auto-resolved outcome. Timer does not restart.
</verification>

<success_criteria>
- count 1→0 invokes onComplete (natural expiry)
- isActive false→true with count 0 invokes setCount(startFrom) only (fresh activation)
</success_criteria>

<output>
After completion, create .planning/phases/04-immersive-pressure-effects/04-06-SUMMARY.md
</output>
