---
phase: 06-debrief-and-replay-system
plan: 06
type: auto
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - components/game/debrief/DebriefPage2AuditTrail.tsx
autonomous: true

must_haves:
  truths:
    - "Audit trail shows human-readable decision labels (e.g., 'Paste', 'Debug') instead of LEFT/RIGHT"
  artifacts:
    - path: "components/game/debrief/DebriefPage2AuditTrail.tsx"
      provides: "Decision labels mapped from card.onRight.label / card.onLeft.label"
  key_links:
    - from: "entry.choice"
      to: "outcome.label"
      via: "Conditional mapping: RIGHT→onRight.label, LEFT→onLeft.label"

gap_reference:
  test: 2
  gap_id: 1
  truth: "Audit trail clearly shows user's decisions in understandable format"
  severity: major
---

<objective>
Fix audit trail UX: Replace raw LEFT/RIGHT text with human-readable decision labels from card definitions.

Current issue: Users see "LEFT" or "RIGHT" which doesn't tell them what decision they actually made (e.g., "Paste" vs "Debug").

Fix: Map entry.choice to card.onRight.label or card.onLeft.label for display.
</objective>

<files>
- components/game/debrief/DebriefPage2AuditTrail.tsx
</files>

<implementation_notes>
The audit trail displays decisions with their outcomes. Currently it shows the raw choice direction ("LEFT" or "RIGHT"). 

The card object has:
- onRight: { label: string, effects: EffectBlock }
- onLeft: { label: string, effects: EffectBlock }

When entry.choice === "RIGHT", display card.onRight.label
When entry.choice === "LEFT", display card.onLeft.label

This gives users meaningful context about what choice they actually made.
</implementation_notes>

<verify>
1. Play through game making several decisions
2. Reach audit trail (Page 2 of debrief)
3. Verify each decision shows the label (e.g., "Paste", "Debug", "Install") not "LEFT"/"RIGHT"
</verify>
