---
phase: 09-visual-effects
plan: "02"
type: TDD
wave: 1
depends_on: []
files_modified:
  - App.tsx
  - hooks/useStageReady.ts
  - components/LayoutShell.tsx
  - components/game/InitializingScreen.tsx
  - index.html
autonomous: true
must_haves:
  truths:
    - "Moving between game stages feels like an intentional transition instead of an abrupt content swap."
    - "Stage transitions do not visually fight swipe direction or reintroduce ghost-click regressions."
    - "The initializing countdown hands off into gameplay with a stronger staged reveal than the current plain fade."
    - "prefers-reduced-motion respected for optional transition flourishes; core stage content remains."
  artifacts:
    - path: App.tsx
      provides: "Top-level stage transition orchestration and stage-aware wrapper attributes."
      contains: "stage-transition"
    - path: hooks/useStageReady.ts
      provides: "Transition-settling guard that still blocks premature interaction while animations complete."
      exports: ["useStageReady"]
    - path: index.html
      provides: "Shared transition tokens and keyframes for stage-level polish."
      contains: "@keyframes"
  key_links:
    - from: App.tsx
      to: index.html
      via: "root stage wrapper uses shared transition classes or data attributes defined in global CSS"
      pattern: "stage-transition|data-stage"
    - from: App.tsx
      to: hooks/useStageReady.ts
      via: "transition timing remains aligned with interaction readiness"
      pattern: "useStageReady"
    - from: components/game/InitializingScreen.tsx
      to: App.tsx
      via: "countdown screen uses the same stage-transition vocabulary as the root stage handoff"
      pattern: "countdown|transition"
---

<objective>
Upgrade stage-to-stage motion so screen changes feel deliberate, readable, and more cinematic without confusing them with swipe-direction feedback.

Purpose: Deliver VFX-02 with one consistent transition system owned at the app shell rather than ad hoc per-screen animations.
Output: Shared stage transition tokens, app-level transition orchestration, and a stronger initializing-to-playing handoff that still respects interaction timing.
</objective>

<execution_context>
@/Users/yevgenschweden/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/yevgenschweden/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@App.tsx
@hooks/useStageReady.ts
@components/LayoutShell.tsx
@components/game/InitializingScreen.tsx
@index.html
@tests/stage-snapshots.spec.ts
@tests/layout-overlay-touch.spec.ts
</context>

<tdd_config>
Use `type: execute`.

This is transition choreography plus guardrail alignment, not a standalone test-first feature.
</tdd_config>

<tasks>

<task type="auto">
  <name>Task 1: Create a shared stage-transition system with stage-aware wrapper metadata</name>
  <files>App.tsx, components/LayoutShell.tsx, index.html</files>
  <action>Refine the current root transition approach into a stage-aware system owned by `App.tsx` and backed by shared tokens/keyframes in `index.html`. Use a neutral transition language that emphasizes fade, scale, depth, or reveal rather than left/right movement so it does not conflict with swipe semantics. Optionally adopt View Transition API: wrap stage dispatch in `if (!document.startViewTransition) { callback(); return; } document.startViewTransition(() => callback());` for Safari 18+; fallback runs callback directly for Safari &lt;18. Animate only transform and opacity; avoid layout-heavy properties. If `LayoutShell` needs wrapper attributes or classes to normalize how stage transitions apply across screens, make that change there. Add stable stage wrapper hooks or `data-stage` attributes for verification.</action>
  <verify>`bun run typecheck && bun run build`</verify>
  <done>Stage rendering is driven by one consistent transition system, screens share the same wrapper contract, and transition semantics no longer depend on ad hoc component behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Align transition timing with readiness guards and strengthen the initializing handoff</name>
  <files>hooks/useStageReady.ts, components/game/InitializingScreen.tsx, App.tsx</files>
  <action>Update `useStageReady` so readiness timing still blocks ghost clicks while accounting for the enhanced transition duration or settling behavior introduced by Task 1. Then improve `InitializingScreen.tsx` so the countdown feels like a proper launch sequence with a more pronounced reveal into gameplay, using the shared transition language instead of a one-off animation stack. Preserve the current countdown logic and stage flow; do not add stress/failure effects from Phase 04, and do not make the initializing screen depend on swipe state.</action>
  <verify>`bun run typecheck && bun run build && bunx playwright test tests/layout-overlay-touch.spec.ts tests/stage-snapshots.spec.ts`</verify>
  <done>Interaction readiness still works after the transition upgrade, the initializing countdown hands off cleanly into gameplay, and stage snapshots remain stable enough for automated regression coverage.</done>
</task>

</tasks>

<verification>
1. Run `bun run typecheck`.
2. Run `bun run build`.
3. Run `bunx playwright test tests/layout-overlay-touch.spec.ts tests/stage-snapshots.spec.ts`.
4. Confirm stage transitions use non-directional motion language and do not regress the current ghost-click protections.
5. If View Transition API used: confirm fallback runs callback directly when `!document.startViewTransition` (Safari &lt;18). Transitions animate only transform + opacity.
</verification>

<success_criteria>
- VFX-02 is satisfied through a coherent app-level transition system rather than isolated screen effects.
- Transitions remain distinct from swipe gestures because they avoid left/right motion metaphors.
- Stage readiness and countdown handoff remain functional after the visual upgrade.
</success_criteria>

<output>
After completion, create `.planning/phases/09-visual-effects/09-02-SUMMARY.md`
</output>
